\documentclass[11pt]{article}
\usepackage[a4paper,margin=1in]{geometry}
\usepackage{amsmath,amssymb,amsthm,bm,mathtools}
\usepackage{booktabs}
\usepackage{hyperref}
\usepackage{enumitem}
\usepackage{array}
\usepackage{xcolor}
\usepackage{cite}
\usepackage{graphicx}
\graphicspath{{../plots/}}
\usepackage{listings}
\lstdefinestyle{nb}{
  basicstyle=\ttfamily\small,
  breaklines=true,
  columns=fullflexible,
  keepspaces=true,
  showstringspaces=false,
  keywordstyle=\color{blue!60!black},
  commentstyle=\color{gray!70!black},
  stringstyle=\color{purple!60!black}
}

\hypersetup{
  colorlinks=true,
  linkcolor=blue,
  urlcolor=blue,
  citecolor=blue
}

\newcommand{\EE}{\mathbb{E}}
\newcommand{\Var}{\mathrm{Var}}
\newcommand{\RR}{\mathbb{R}}
\newcommand{\T}{\mathrm{T}}
\newcommand{\e}{\varepsilon}
\newcommand{\dd}{\mathrm{d}}
\DeclareRobustCommand{\uvec}{\bm{u}}
\DeclareRobustCommand{\xvec}{\bm{x}}
\DeclareRobustCommand{\zvec}{\bm{z}}
\DeclareRobustCommand{\thetavec}{\bm{\theta}}
\newcommand{\grad}{\nabla}
\newcommand{\pinit}{p_{\rm init}}
\newcommand{\pdata}{p_{\rm data}}
\newcommand{\Ndata}{\mathbb{R}^{D}}

\theoremstyle{definition}
\newtheorem{definition}{Definition}[section]
\newtheorem{proposition}[definition]{Proposition}
\newtheorem{theorem}[definition]{Theorem}
\newtheorem{remark}[definition]{Remark}

\title{A Mathematical Note on \texttt{dm\_cmb.ipynb}:\\
Patch-Based CMB Prior Learning with Flow Matching}
\author{Technical note for the diffusion-model CMB notebook}
\date{\today}

\begin{document}
\maketitle
\begin{abstract}
We document the mathematical content and implementation choices of the notebook \texttt{dm\_cmb.ipynb}, which learns generative priors for $(T,Q,U)$ patches of the cosmic microwave background (CMB) and Galactic foregrounds at $143\,\mathrm{GHz}$ and validates them through flat-sky spectral diagnostics. The workflow combines standard CMB harmonic methods---spin-weighted polarisation decompositions, full-sky simulations from CAMB power spectra, and curved-sky non-Gaussian lensing of the CMB primary by an independently sampled lensing potential through the \texttt{lenspyx}/\texttt{DUCC} remapping engine \cite{zaldarriaga1997,kamionkowski1997,gorski2005,lewis2000camb,sullivan2024methods,scott2024pdg,reinecke2023ducc}---with a modern path-based generative model trained by direct flow matching \cite{ho2020ddpm,song2021sde,lipman2023flowmatching}. We describe in detail the pipeline used to bring Planck NPIPE / PR4 half-ring data and simulated priors to a common, well-defined representation: HEALPix pixelisation, harmonic downgrading, beam and pixel-window matching, sky masking, gnomonic patch extraction, spin-2 polarisation rotation, per-channel standardisation, and flat-sky spectrum estimation. The diffusion model is then specified completely: the Gaussian conditional probability path, the choice of signal/noise schedules, the U-Net vector-field architecture, the auxiliary radial Fourier-power regulariser used at training time, the deterministic Heun ODE sampler, and the spectral-validation diagnostics. The eventual scientific goal is posterior sampling for CMB component separation; this note focuses on the prior-learning stage, which is a prerequisite for any likelihood-based inference and which fixes the experimental conventions used in the rest of the work.
\end{abstract}
\tableofcontents
\bigskip







\section{Introduction}
\label{sec:intro}

The cosmic microwave background (CMB) is a relic blackbody radiation field whose temperature and polarisation anisotropies encode the statistical properties of the very early universe and are central to modern cosmology \cite{scott2024pdg,planck2018overview}. CMB observations are however contaminated by Galactic foregrounds (most notably thermal dust and synchrotron emission), instrumental noise, and the angular smoothing and sky masking imposed by realistic telescopes. Recovering the underlying CMB signal from the observed maps is therefore a component-separation problem.

In this work we expand on recent applications of deep generative modelling to CMB component separation \cite{heurtel2023dust} by exploring the use of \emph{flow-matching} diffusion-style models \cite{ho2020ddpm,song2021sde,lipman2023flowmatching} for joint inference of the foreground signal and the true CMB. Let $y$ denote the observed map, $x$ the foreground signal, $f$ the \emph{lensed} CMB, $n$ the instrumental noise, and $A$ the linear instrumental response (effective beam, pixel window, and sky mask). The observed signal model is
\begin{align}
    y = A\,(f + x) + n,
    \label{eq:fwd_model}
\end{align}
where the gravitational lensing of the CMB is treated implicitly through the prior $p(f)$: the simulated $f$-prior used for training is constructed by drawing unlensed Gaussian primary CMB realisations together with an independent realisation of the lensing potential $\phi$ from a fiducial $C_L^{\phi\phi}$ and then remapping the unlensed sky onto the deflected sphere with the \texttt{lenspyx} curved-sky lensing engine \cite{reinecke2023ducc} (Sec.~\ref{sec:data_prior_f}). The forward operator $A$ therefore remains a linear post-lensing operator (effective beam, pixel window, and sky mask), while the non-Gaussianity induced by lensing on the CMB acoustic peaks, on the $E\!\to\!B$ leakage and on the $T$/$E\!\times\!\phi$ four-point function is carried by the simulated prior. Assuming a Gaussian noise model, the likelihood reads
\begin{equation}
    p(y\mid x,f) \propto \exp\!\left[-\tfrac{1}{2}\,(y-A(f+x))^\T N^{-1}(y-A(f+x))\right],
\end{equation}
with $N$ the noise covariance, and the posterior of interest factorises as
\begin{align}
    p(x,f\mid y) \;\propto\; p(y\mid x,f)\,p(x)\,p(f).
\end{align}
Component separation thus requires (i) a tractable likelihood, which is fixed once the instrumental pipeline and noise model are specified, and (ii) data-driven priors $p(x)$ and $p(f)$, which we learn from realistic simulations. We train two independent U-Net models to parametrise vector fields $u^\theta_t$ that generate probability paths from a simple Gaussian base distribution to each prior, and we sample those priors by integrating the corresponding ODE/SDE \cite{lipman2023flowmatching}. Posterior sampling is then obtained by augmenting the prior sampler with likelihood gradients $\nabla_x \log p(y\mid x,f)$ and $\nabla_f \log p(y\mid x,f)$ during integration; this is the only stage at which the observed CMB data $y$ enters the inference.

We work at a single frequency, $143\ \mathrm{GHz}$, using Planck NPIPE / PR4 half-ring maps as the observed sky $y$ and the publicly released $143\ \mathrm{GHz}$ confidence mask as $A$'s sky cut \cite{planck2018overview}. Theoretical CMB spectra are computed with CAMB \cite{lewis2000camb} for a fiducial $\Lambda$CDM cosmology and used, together with the lensing-potential auto-spectrum $C_L^{\phi\phi}$, to draw lensed-CMB $f$-priors that share the same effective beam, pixel window, and sky footprint as the observations \cite{sullivan2024methods,reinecke2023ducc}. Galactic foreground priors are obtained from PySM\,3 \cite{thorne2017pysm,zonca2021pysm3}, which provides full-sky $(I,Q,U)$ simulations of dust and synchrotron emission consistent with Planck data products; we use the small-scale stochastic presets \texttt{d10} and \texttt{s5}, so that successive foreground realisations are statistically independent draws of the small-scale Galactic field rather than rotated copies of a fixed template. All maps are processed in the HEALPix pixelisation \cite{gorski2005,zonca2019healpy} and the diffusion model is trained on flat-sky $64\times 64$ patches obtained by gnomonic projection.

The scientific motivation is twofold. First, by learning $p(f)$ and $p(x)$ from data we obtain priors that capture non-Gaussian morphology that simple parametric models cannot represent, in particular for Galactic foregrounds. Second, the resulting posterior sampler outputs an ensemble of plausible component maps consistent with the data, providing both a point estimate of $(x,f)$ and a calibrated uncertainty on it. This is a prerequisite for unbiased downstream cosmological inference, including the search for primordial $B$-modes for which residual foreground contamination is the dominant systematic.




\section{Idealized CMB Power Spectra}

We briefly review the spherical-harmonic description of CMB temperature and polarisation anisotropies, since this is the natural language in which our priors are constructed and validated. A more pedagogical exposition can be found in Refs.~\cite{scott2024pdg,sullivan2024methods,gorski2005}.

The CMB temperature anisotropy is a scalar field on the sphere and is therefore naturally expanded in spherical harmonics \cite{zaldarriaga1997,gorski2005,scott2024pdg}:
\begin{equation}
  T(\hat n) = \sum_{\ell=0}^\infty \sum_{m=-\ell}^{\ell} a_{\ell m}^T\,Y_{\ell m}(\hat n).
\end{equation}
From the orthonormality of the spherical-harmonics
\begin{equation}
  \int_{\mathbb S^2} Y_{\ell m}(\hat n)\,Y_{\ell' m'}^\ast(\hat n)\,\dd\Omega
  =
  \delta_{\ell\ell'}\delta_{mm'},
\end{equation}
the harmonic coefficients are obtained by
\begin{equation}
  a_{\ell m}^T
  =
  \int_{\mathbb S^2} T(\hat n)\,Y_{\ell m}^\ast(\hat n)\,\dd\Omega.
\end{equation}
The coefficient \(a_{\ell m}^T\) is therefore the amplitude with which the angular pattern \(Y_{\ell m}\) appears in the sky, and in a statistically isotropic theory the spherical-harmonic basis is preferred because two-point statistics become diagonal in \((\ell,m)\). The multipole \(\ell\) controls angular scale through the rough correspondence
\begin{equation}
  \theta \sim \frac{\pi}{\ell},
\end{equation}
so small \(\ell\) corresponds to large coherent angular structures and large \(\ell\) to fine structure; the azimuthal index \(m\) resolves the angular dependence within a fixed \(\ell\).

CMB polarisation is described by the Stokes parameters \(Q\) and \(U\) defined relative to a chosen local basis on the sphere: \(Q\) measures the linear polarisation aligned with the basis (up/down minus left/right), and \(U\) the same quantity rotated by \(45^\circ\). Unlike \(T\), the pair \((Q,U)\) is not a scalar but transforms as a \emph{spin-\(\pm 2\)} field under local rotations of the basis, and its natural harmonic expansion uses spin-weighted spherical harmonics \cite{zaldarriaga1997,kamionkowski1997,sullivan2024methods}:
\begin{equation}
  (Q \pm iU)(\hat n)
  =
  \sum_{\ell m}
  a_{\ell m}^{\pm 2}\,{}_{\pm 2}Y_{\ell m}(\hat n),
\end{equation}
with coefficients obtained by projection,
\begin{equation}
  a_{\ell m}^{\pm 2}
  =
  \int_{\mathbb S^2}
  (Q\pm iU)(\hat n)\,{}_{\pm 2}Y_{\ell m}^\ast(\hat n)\,\dd\Omega.
\end{equation}
The scalar \(E\)- and pseudo-scalar \(B\)-mode coefficients are defined by \cite{zaldarriaga1997,kamionkowski1997}
\begin{equation}
  a_{\ell m}^E = -\frac12\left(a_{\ell m}^{2}+a_{\ell m}^{-2}\right),
  \qquad
  a_{\ell m}^B = \frac{i}{2}\left(a_{\ell m}^{2}-a_{\ell m}^{-2}\right).
\end{equation}
\(E\) is the curl-free, parity-even component of the polarisation and \(B\) the divergence-free, parity-odd component. The decomposition is local in harmonic space but non-local in pixel space, so finite sky coverage induces \(E\)/\(B\) mode mixing in any flat-sky or masked estimator.

For a statistically isotropic Gaussian CMB, the harmonic coefficients satisfy
\begin{equation}
  \EE\!\left[a_{\ell m}^X\,(a_{\ell' m'}^Y)^\ast\right]
  =
  C_\ell^{XY}\,\delta_{\ell\ell'}\delta_{mm'},
  \qquad
  X,Y\in\{T,E,B\}
\end{equation}
and all statistical information is contained in the covariance matrix at each \(\ell\):
\begin{equation}
  \Sigma_\ell
  =
  \begin{pmatrix}
    C_\ell^{TT} & C_\ell^{TE} & 0\\
    C_\ell^{TE} & C_\ell^{EE} & 0\\
    0 & 0 & C_\ell^{BB}
  \end{pmatrix}.
\end{equation}

We can compute two-point functions in terms of the ${C^{XY}_\ell}$ coefficients. The full temperature two-point function for instance is:
\begin{align}
  \xi_T(\hat n,\hat n')
  &:= \EE[T(\hat n)T(\hat n')] \\ \nonumber
  &= \EE\!\left[
    \sum_{\ell m} a_{\ell m}^T Y_{\ell m}(\hat n)
    \sum_{\ell' m'} (a_{\ell' m'}^T)^\ast Y_{\ell' m'}^\ast(\hat n')
  \right] \\ \nonumber
  &= \sum_{\ell m}\sum_{\ell' m'}
     \EE[a_{\ell m}^T(a_{\ell' m'}^T)^\ast]\,
     Y_{\ell m}(\hat n)Y_{\ell' m'}^\ast(\hat n') \nonumber \\
  & = \sum_{\ell=0}^\infty C_\ell^{TT}
  \sum_{m=-\ell}^{\ell} Y_{\ell m}(\hat n)Y_{\ell m}^\ast(\hat n') \nonumber \\
  & =
  \sum_{\ell=0}^\infty \frac{2\ell+1}{4\pi} C_\ell^{TT}
  P_\ell(\hat n\!\cdot\!\hat n').
\end{align}
where we used
\begin{equation}
  \sum_{m=-\ell}^{\ell} Y_{\ell m}(\hat n)Y_{\ell m}^\ast(\hat n')
  =
  \frac{2\ell+1}{4\pi}P_\ell(\hat n\!\cdot\!\hat n').
\end{equation}



Instead of $C_\ell^{XY}$ it is common to plot
\begin{equation}
  D_\ell^{XY} \equiv \frac{\ell(\ell+1)}{2\pi}\, C_\ell^{XY},
\end{equation}
which is visually more informative because the prefactor \(\ell(\ell+1)\) roughly cancels the leading \(\ell^{-2}\) decline of an approximately scale-invariant \(C_\ell\) and makes the acoustic peaks easier to see \cite{sullivan2024methods}.

In the large-\(\ell\) limit, \(D_\ell^{TT}\) admits a clean physical interpretation as the temperature variance per logarithmic \(\ell\)-interval. Setting \(\hat n'=\hat n\) in the two-point function and using \(P_\ell(1)=1\), we have
\begin{equation}
  \Var[T(\hat n)]
  =
  \sum_{\ell=0}^\infty \frac{2\ell+1}{4\pi} C_\ell^{TT}.
\end{equation}
For \(\ell \gg 1\), approximate the sum by an integral and use \(2\ell+1\simeq 2\ell\):
\begin{align}
  \Var[T(\hat n)]
  & \simeq \int_0^\infty \frac{\ell\,C_\ell^{TT}}{2\pi}\,\dd\ell
    = \int_0^\infty \frac{\ell^2\, C_\ell^{TT}}{2\pi}\,\dd\ln\ell \\
  & \simeq \int_0^\infty \frac{\ell(\ell+1)\, C_\ell^{TT}}{2\pi}\,\dd\ln\ell
    = \int_0^\infty D_\ell^{TT}\,\dd\ln\ell. \nonumber
\end{align}
The same argument applies to \(D_\ell^{EE}\) and \(D_\ell^{BB}\), so the four spectra \(\{D_\ell^{TT},D_\ell^{EE},D_\ell^{BB},D_\ell^{TE}\}\) provide an interpretable, scale-resolved summary of the second-order statistics of the Gaussian CMB.

\paragraph{Cosmic variance.} Even with a perfect noiseless full-sky map, the empirical estimator $\widehat C_\ell^{XY}$ has an irreducible variance \cite{scott2024pdg,sullivan2024methods}
\begin{equation}
  \Var\!\left[\widehat C_\ell^{XY}\right]
  = \frac{1}{2\ell+1}\!\left[C_\ell^{XX}\,C_\ell^{YY} + (C_\ell^{XY})^2\right],
\end{equation}
which arises from the finite number $(2\ell+1)$ of independent $m$-modes available at each multipole. This sets a fundamental floor for the precision with which any model, including the present one, can be tested at a given $\ell$, and is largest at low $\ell$. On a fraction $f_{\rm sky}$ of the sky, the variance is enlarged by approximately $1/f_{\rm sky}$, defining the so-called \emph{sample variance} regime relevant to masked analyses \cite{sullivan2024methods}.



\section{Realistic CMB Power Spectra}
\label{sec:realistic_spectra}
In practice the idealised description above must be modified to reflect (i) finite-resolution map representations, (ii) instrumental smoothing by the telescope beam, (iii) angular averaging into pixels, (iv) sky cuts that null contaminated regions, and (v) the geometry of patch-based analyses. We summarise here the formalism associated with each of these effects in general terms, independent of any particular data set or simulation; the concrete numerical choices, instrumental parameters, and software implementations adopted in this work are deferred to Sec.~\ref{sec:data}. The presentation closely follows the conventions of Refs.~\cite{gorski2005,sullivan2024methods,scott2024pdg}.

\subsection{HEALPix pixelisation}
\label{sec:healpix}

We adopt the HEALPix scheme \cite{gorski2005,zonca2019healpy} as the all-sky pixelisation throughout. A HEALPix map discretises the sphere into pixels of \emph{equal area} arranged on isolatitude rings, controlled by a single resolution parameter \(N_{\rm side}\) (a power of two). The number of pixels and the pixel angular size are
\begin{equation}
  N_{\rm pix} = 12\,N_{\rm side}^2,
  \qquad
  \theta_r = \sqrt{\frac{4\pi}{N_{\rm pix}}}
           = \sqrt{\frac{\pi}{3\,N_{\rm side}^2}},
  \label{eq:healpix_nside}
\end{equation}
where \(\theta_r\) is the side-length of a pixel in radians \cite{gorski2005,sullivan2024methods}. Equal-area pixels permit the spherical-harmonic transform to be computed by a single ring-by-ring quadrature without per-pixel weighting, and allow second-order statistics to be averaged uniformly across the sky. HEALPix maps may be stored in either ``RING'' or ``NESTED'' ordering; the spherical-harmonic transforms used below assume the RING convention, which is the default for most CMB data products and software \cite{gorski2005}.

\subsection{Band-limit and downgrading}
\label{sec:downgrade}

A continuous field on the sphere admits a spherical-harmonic expansion up to \(\ell_{\max}=\infty\). A HEALPix map is intrinsically discrete and therefore band-limited; following standard practice one adopts
\begin{equation}
  \ell_{\max} = 3\,N_{\rm side} - 1,
\end{equation}
above which the spherical-harmonic transform is no longer reliable due to aliasing of unresolved structure into the represented modes \cite{gorski2005,sullivan2024methods}. Ref.~\cite{sullivan2024methods} also recommends the more conservative choice \(\ell_{\max}=2\,N_{\rm side}\) when transitioning between resolutions, an inequality that is essentially saturated whenever the beam suppression at the upper end of the represented multipole range is severe.

\paragraph{Harmonic-space downgrading.} Bringing a map from a high input resolution \(N_{\rm side}^{\rm in}\) to a lower target resolution \(N_{\rm side}^{\rm out}\) is conceptually a band-limit followed by a resampling. The recommended procedure \cite{sullivan2024methods} is to perform the downgrade entirely in harmonic space rather than through pixel-space averaging, because pixel-space routines such as \texttt{ud\_grade} implicitly convolve with the new pixel window and, more importantly, treat \(Q\) and \(U\) as scalars, breaking the spin-2 transformation law of polarisation and producing biased \(E\)/\(B\) power. Concretely, one
\begin{enumerate}[leftmargin=2.0em]
  \item computes polarised harmonic coefficients \((a_{\ell m}^T,a_{\ell m}^E,a_{\ell m}^B)\) of the input map up to
  \begin{equation}
    \ell_{\max}^{\rm in} = \min(3\,N_{\rm side}^{\rm in}-1,\ \ell_{\max}^{\rm out}),
  \end{equation}
  i.e.~explicitly nulls all multipoles above the target band-limit, as required when synthesising at the lower \(N_{\rm side}\) \cite{sullivan2024methods};
  \item rescales the harmonic coefficients to the desired output beam and pixel window, $a_{\ell m}^{\rm out} = (B_\ell^{\rm out}P_\ell^{\rm out})/(B_\ell^{\rm in}P_\ell^{\rm in})\,a_{\ell m}^{\rm in}$ (the Sullivan--Hergt--Scott downgrading recipe \cite{sullivan2024methods}), where \(B_\ell\) is the beam transfer function of Sec.~\ref{sec:beam} and \(P_\ell\) is the HEALPix pixel-window function of Sec.~\ref{sec:pixwin};
  \item synthesises the output \((T,Q,U)\) maps at \(N_{\rm side}^{\rm out}\) using the rescaled coefficients.
\end{enumerate}
When the input pixel window is sufficiently close to unity in the represented multipole range the second factor reduces to a pure beam rescaling, $a_{\ell m}^{\rm out}\!=\!(B_\ell^{\rm out}/B_\ell^{\rm in})\,a_{\ell m}^{\rm in}$; this approximation should be checked rather than assumed.



\subsection{Beam}
\label{sec:beam}

The optics of any real CMB telescope smear the underlying sky with a finite-width beam, which we model as a circular Gaussian of full-width-half-maximum \(\theta_{\rm FWHM}\) and Gaussian width \(\sigma\) related by \cite{sullivan2024methods}
\begin{equation}
  \sigma = \frac{\theta_{\rm FWHM}}{\sqrt{8\ln 2}}.
\end{equation}
For an azimuthally symmetric beam, real-space convolution becomes multiplication in harmonic space, with the scalar transfer function
\begin{equation}
  B_\ell = \exp\!\left[-\tfrac{1}{2}\,\ell(\ell+1)\,\sigma^2\right],
  \label{eq:beam_Bl}
\end{equation}
so that observed temperature spectra are damped at high \(\ell\) according to
\begin{equation}
  C_\ell^{TT,\,\rm obs} = B_\ell^2\, C_\ell^{TT}.
\end{equation}
This is the same $B_\ell$ that appears in the harmonic downgrading formula of Sec.~\ref{sec:downgrade} and in the combined transfer-function model of Sec.~\ref{sec:pixwin} (Eq.~\ref{eq:obs_spectrum}).
Polarisation, being a spin-2 field, picks up a small additional correction \cite{challinor2000beam,sullivan2024methods}: the polarised beam is approximately \({}_2 B_\ell = B_\ell\,e^{2\sigma^2}\). At sub-degree resolution this correction is exponentially suppressed and the scalar form (\ref{eq:beam_Bl}) is sufficient for both temperature and polarisation; we adopt this approximation in all subsequent expressions.

When data and simulations must be combined coherently, all maps should be brought to a common effective beam. If a map carries an input beam of FWHM \(\theta_{\rm in}\) and the target resolution is \(\theta_{\rm out}\geq\theta_{\rm in}\), this is achieved by multiplying its harmonic coefficients by the residual Gaussian
\begin{equation}
  B_\ell^{\rm add}
  = \exp\!\left[-\tfrac{1}{2}\,\ell(\ell+1)\,\sigma_{\rm add}^2\right],
  \qquad
  \sigma_{\rm add} = \tfrac{1}{\sqrt{8\ln 2}}\sqrt{\theta_{\rm out}^2-\theta_{\rm in}^2}.
  \label{eq:beam_residual}
\end{equation}
Enforcing \(\theta_{\rm out}\geq\theta_{\rm in}\) keeps the operation purely additional smoothing, with no deconvolution and therefore no noise amplification at high \(\ell\). Bringing the data and the prior simulations to a single, well-defined effective resolution is essential for any likelihood-based component-separation analysis, where the residual \(y - A(f+x)\) of the forward model in Sec.~1 must live at a common resolution \cite{sullivan2024methods}.

\subsection{Pixel window}
\label{sec:pixwin}

Beam smoothing accounts for the optical blur of the telescope; \emph{pixelisation} introduces an additional, distinct angular smoothing that arises from averaging the sky over the area of each pixel \cite{sullivan2024methods}. Working at fixed \(N_{\rm side}\), the corresponding harmonic transfer function \(P_\ell^{N_{\rm side}}\) is provided by HEALPix \cite{gorski2005} and is well approximated at moderate \(\ell\) by a sinc,
\begin{equation}
  P_\ell^{\rm sinc}
  = \mathrm{sinc}\!\left(\frac{\ell\,\theta_r}{2\pi}\right),
\end{equation}
where \(\theta_r\) is the pixel size from Eq.~(\ref{eq:healpix_nside}). The full HEALPix function is computed by spherical-harmonic transform of an average pixel for \(N_{\rm side}\leq 128\) and extrapolated by a top-hat approximation at larger \(N_{\rm side}\) \cite{gorski2005}. Combining beam and pixel-window contributions, the model for an observed power spectrum becomes
\begin{equation}
  C_\ell^{\rm obs}
  = P_\ell^2\,B_\ell^2\,C_\ell^{\rm true}.
  \label{eq:obs_spectrum}
\end{equation}
Here $B_\ell$ is the Gaussian beam transfer function of Sec.~\ref{sec:beam} (Eq.~\ref{eq:beam_Bl}) and $P_\ell \equiv P_\ell^{N_{\rm side}}$ is the HEALPix pixel-window function defined above; both operators also appear explicitly in the harmonic downgrading formula of Sec.~\ref{sec:downgrade}. HEALPix \cite{gorski2005} provides separate pixel windows $P_\ell^T$ and $P_\ell^P$ for scalar and spin-2 fields, and a careful analysis applies the appropriate one to $a_{\ell m}^T$ and to $\{a_{\ell m}^E,a_{\ell m}^B\}$ respectively. The relative magnitudes of $B_\ell$ and $P_\ell$ depend on the specific beam and resolution chosen; in regimes where the beam smoothing dominates, the pixel-window correction can be subdominant but is rarely negligible at the highest represented multipoles \cite{sullivan2024methods}, and should be evaluated quantitatively before being neglected.

\subsection{Masking}
\label{sec:masking}

The Galactic plane and bright extragalactic point sources contaminate the CMB at all frequencies, and any analysis on real data must restrict itself to a sufficiently clean fraction of the sky \cite{planck2018overview,sullivan2024methods}. This is encoded by a sky mask $M(\hat n)\in[0,1]$, typically a HEALPix map at the resolution of the data, where $M=1$ flags pixels retained by the analysis and $M<1$ flags partially or fully contaminated regions. When the mask is provided at higher resolution than the analysis pixelisation, it is downgraded with a scalar pixel-space averaging (\texttt{ud\_grade}) -- legitimate here because $M$ is a scalar weighting and not a spin field -- and typically thresholded above some fraction $M_{\rm min}\!\in\![0,1]$ to enforce a deterministic clean-sky/contaminated dichotomy.

The choice of how to combine the mask with the power-spectrum estimator is non-trivial. A naive multiplication \(\tilde T = M\,T\) followed by a full-sky harmonic transform biases all spectra by approximately a factor of the unmasked sky fraction
\begin{equation}
  f_{\rm sky}
  = \frac{1}{N_{\rm pix}}\sum_{i=1}^{N_{\rm pix}} M_i^2,
\end{equation}
and additionally couples power across multipoles, leading to ringing and large biases especially for \(EE\)/\(BB\) where the much weaker polarisation signal sits underneath leakage from the masked-out regions. State-of-the-art alternatives include the pseudo-\(C_\ell\) \texttt{MASTER} estimator \cite{hivon2002master} as implemented in \texttt{NaMaster} \cite{alonso2023namaster}, and quasi-maximum-likelihood estimators for the lowest \(\ell\) \cite{sullivan2024methods}.

A simpler alternative, applicable to patch-based analyses (Sec.~\ref{sec:flatsky}), is to bypass pseudo-\(C_\ell\) altogether by restricting attention to fully-clean patches: candidate patch \emph{centres} are drawn from $\{i:M_i\geq M_{\rm min}\}$ and any candidate whose projected footprint contains a pixel with $M<M_{\rm min}$ is rejected. The retained ensemble is then effectively unmasked, and the flat-sky FFT-based spectral estimators of Sec.~\ref{sec:flatsky} are not biased by mode-coupling. The cost is a non-trivial reduction of usable sky and a small mask-induced bias in the patch ensemble's prior over sky position, both of which are easy to quantify directly. Apodising the mask before per-patch acceptance and computing pseudo-\(C_\ell\) on each patch with \texttt{NaMaster} \cite{alonso2023namaster} would relax the acceptance criterion and recover usable sky in the partially apodised border regions, at the price of restoring a small mode-coupling correction.


\subsection{Flat-sky projection and patch spectra}
\label{sec:flatsky}

When a generative model or any other translation-invariant learner is trained on small angular regions rather than full-sky maps, the natural data representation is a flat two-dimensional patch. The angular size of a patch is small enough that curvature corrections are subdominant, while still being large enough to resolve the multipoles of interest, so spherical-harmonic operators can be replaced by translation-invariant 2D Fourier operators at the price of a controlled approximation error.

A patch is obtained by a \emph{gnomonic} (tangent-plane) projection of the underlying HEALPix map onto a square grid of $N_{\rm patch}\times N_{\rm patch}$ pixels at angular sampling $\theta_{\rm patch}$, centred on a chosen sky direction $\hat n_0$, with linear extent
\begin{equation}
  L = N_{\rm patch}\,\theta_{\rm patch}.
\end{equation}
For $L\!\lesssim\!10^\circ$, curvature corrections to the spherical metric are of order $L^2\!\sim\!\mathrm{few}\times 10^{-2}$ and the flat-sky approximation is quantitatively accurate \cite{sullivan2024methods,seljak1997}. The temperature channel is resampled by simple scalar interpolation (e.g.~\texttt{healpy.projector.GnomonicProj}). For polarisation, however, a naive scalar resampling of \(Q\) and \(U\) is incorrect: under the change of basis induced by the projection, \((Q,U)\) acquires a position-dependent rotation by an angle \(\psi(\hat n)\), and a \emph{spin-2 rotation}
\begin{equation}
  \begin{pmatrix} Q' \\ U' \end{pmatrix}
  =
  \begin{pmatrix} \cos 2\psi & \sin 2\psi \\ -\sin 2\psi & \cos 2\psi \end{pmatrix}
  \begin{pmatrix} Q \\ U \end{pmatrix}
\end{equation}
must be applied at each pixel \cite{kamionkowski1997,seljak1997}. The angle \(\psi(\hat n)\) can be computed analytically or numerically by finite-differencing the projection along the local north direction. Without this rotation, $E$ and $B$ constructed from the patch FFT contain a spurious geometric mixing of the two parities that biases polarised spectra, especially in $BB$ where the signal is intrinsically smaller.

On each patch one estimates spectra via the flat-sky power spectrum estimator \cite{seljak1997,sullivan2024methods}: for an angular footprint of side \(L\) and pixel size $\theta_{\rm patch}$, the discrete two-dimensional Fourier modes are
\begin{equation}
  \bm{\ell} = 2\pi\,\bm{k},
  \qquad
  \bm{k} = \left(\frac{n_x}{L},\frac{n_y}{L}\right)\,\mathrm{rad}^{-1},
  \qquad n_x,n_y\in\mathbb{Z},
\end{equation}
and the spin-2 field decomposes into flat-sky $E$/$B$ via
\begin{equation}
  \tilde E(\bm{\ell}) = -\tilde Q(\bm{\ell})\cos 2\phi_\ell - \tilde U(\bm{\ell})\sin 2\phi_\ell,
  \qquad
  \tilde B(\bm{\ell}) =  \tilde Q(\bm{\ell})\sin 2\phi_\ell - \tilde U(\bm{\ell})\cos 2\phi_\ell,
\end{equation}
where \(\phi_\ell\) is the angle of \(\bm{\ell}\) in the patch frame \cite{kamionkowski1997,seljak1997}. The estimator
\begin{equation}
  \widehat C_\ell^{XY}
  = \frac{\theta_{\rm patch}^{\,2}}{N_{\rm patch}^{\,2}}
    \big\langle\, \tilde X(\bm{\ell})\,\tilde Y^\ast(\bm{\ell})\,\big\rangle_{|\bm{\ell}|\in\Delta\ell}
  \label{eq:flatsky_estimator}
\end{equation}
averages cross-products in annular bins of \(|\bm{\ell}|\) \cite{sullivan2024methods}, where the angular range is bounded from below by the fundamental mode $\ell_{\rm min}\!\simeq\!2\pi/L$ and from above by the Nyquist multipole $\ell_{\rm max}\!\simeq\!\pi/\theta_{\rm patch}$. When patch centres are restricted to clean sky and full-patch validity is enforced (Sec.~\ref{sec:masking}), no apodisation or pseudo-\(C_\ell\) correction is required at the patch level; the mode-coupling matrix reduces to the flat-sky identity for an isolated, fully-unmasked patch, and the bias for $f_{\rm sky}^{\rm patch}=1$ is negligible.

\subsection{Patch-level standardisation}
\label{sec:normalisation}

Independently of the spherical-harmonic formalism, training a generative model requires that the input fields have well-conditioned numerical scales. Raw $T$, $Q$, $U$ patches in $\mu\mathrm{K}_{\rm CMB}$ have very different per-channel variances ($\sigma_T \gg \sigma_{Q,U}$ at intermediate frequencies, where the temperature acoustic peaks dominate over the polarised signal), which would force a generative model to learn this trivial scale hierarchy at the cost of higher-order structure and can induce unstable training near schedule endpoints \cite{ho2020ddpm,heurtel2023dust}. A standard remedy is the deterministic per-channel standardisation
\begin{equation}
  \tilde X_c(\bm{x}) = \frac{X_c(\bm{x})-\mu_c}{\sigma_c},
  \qquad
  c\in\{T,Q,U\},
\end{equation}
where $\mu_c$ and $\sigma_c$ are computed once on the training split only and then applied verbatim to any held-out splits. The generator is trained on $\tilde X$; at inference time samples are de-normalised by inverting this transformation before computing physical-units diagnostics. The procedure is lossless in the sense that a perfect generator of $p(\tilde X)$ yields a perfect generator of $p(X)$; it does mean, however, that intermediate spectra plotted in normalised units are scale-rescaled versions of the physical $C_\ell$, and the two domains must be distinguished explicitly in any diagnostic.

\subsection{Sum--difference noise estimates from independent splits}
\label{sec:halfring}

Modern CMB experiments commonly deliver pairs of independent map-level splits constructed from disjoint subsets of the time-ordered data, e.g.\ half-mission, half-ring, or odd/even-detector splits. Given two such splits $h_1$ and $h_2$ of an observed sky, one forms the standard sum and difference combinations
\begin{equation}
  y = \tfrac{1}{2}(h_1 + h_2),
  \qquad
  d = \tfrac{1}{2}(h_1 - h_2).
\end{equation}
Under the assumption that the sky signal is identical in both halves and the noise is independent between them, $y$ retains the full sky signal and approximately half the noise power, while $d$ is a sky-signal-free realisation of the noise on the map. The map $y$ is therefore the natural observable on which to base inference, while $d$ provides a model-independent diagnostic of the instrumental noise: its empirical variance estimates the diagonal of the per-pixel noise covariance, its auto-spectrum estimates the noise power spectrum $N_\ell$, and its lack of cross-correlation with any clean simulation of the sky provides a sanity check on the split assumption.




\section{Data}
\label{sec:data}

This section instantiates the general formalism of Sec.~\ref{sec:realistic_spectra} on the three data products fed to the prior-learning pipeline: the observed Planck $143\,\mathrm{GHz}$ sky $y$, the simulated lensed-CMB prior $p(f)$, and the simulated Galactic-foreground prior $p(x)$. We work at a single observing frequency, $143\,\mathrm{GHz}$, where the $143\,\mathrm{GHz}$ channel of Planck has favourable signal-to-noise on the CMB and where the Galactic-foreground contamination is dominated by a narrow set of components (thermal dust and synchrotron) for which standard simulators exist. All maps are stored in HEALPix \texttt{RING} ordering and in $\mu\mathrm{K}_{\rm CMB}$ throughout.

\paragraph{Common processing parameters.}
By construction all three products live in the same numerical and angular domain (Table~\ref{tab:processing_params} summarises the choices). We adopt:
\begin{itemize}[leftmargin=1.5em,itemsep=0.1em]
  \item HEALPix resolution $N_{\rm side}=512$, corresponding to $N_{\rm pix}=12N_{\rm side}^2=3{,}145{,}728$ pixels and a pixel scale $\theta_r\simeq 6.87'$ (Sec.~\ref{sec:healpix}). This is the lowest power-of-two $N_{\rm side}$ that comfortably band-limits maps smoothed to the target $10'$ beam, while keeping the cost of training a U-Net on patches manageable.
  \item Harmonic cutoff $\ell_{\max}=3N_{\rm side}-1=1535$ in all spherical-harmonic transforms (Sec.~\ref{sec:downgrade}).
  \item Common effective beam: a circular Gaussian of FWHM $\theta_{\rm out}=10'$ applied to all three products via Eq.~(\ref{eq:beam_Bl}) (Sec.~\ref{sec:beam}). This common-beam choice is what makes the data and the prior simulations harmonically compatible.
  \item Pixel windows: the polarisation-aware HEALPix windows $P_\ell^{T}$ and $P_\ell^{P}$ at $N_{\rm side}=512$ are applied uniformly to both simulated priors $p(f)$ and $p(x)$ via a common harmonic-space step (Sec.~\ref{sec:pixwin}), so that $p(f)$ and $p(x)$ share an identical effective transfer function. At this configuration $P_\ell^{T,P}$ contributes at most an extra $\sim 8\%$ damping at $\ell=\ell_{\max}$ relative to the beam alone. Pixel-window deconvolution is not applied to the observed maps; the corresponding mismatch is bounded by the input pixel-window correction at $N_{\rm side}^{\rm in}=2048$, $\lesssim 0.3\%$ over the represented multipole range.
  \item Common sky mask: the public Planck $143\,\mathrm{GHz}$ confidence mask $M(\hat n)$, downgraded to $N_{\rm side}=512$ with \texttt{healpy.ud\_grade} and thresholded at $M_{\rm min}=0.9$ to define the clean-sky footprint (Sec.~\ref{sec:masking}).
  \item Patch geometry: square $N_{\rm patch}\!\times\!N_{\rm patch}=64\!\times\!64$ flat-sky patches obtained by gnomonic projection at angular sampling $\theta_{\rm patch}=6.7'$/pixel, giving a linear extent
  \begin{equation}
    L = N_{\rm patch}\,\theta_{\rm patch}\simeq 7.15^\circ
    \quad\Rightarrow\quad
    L^2\simeq 51\ \mathrm{deg}^2,
  \end{equation}
  i.e.\ $f_{\rm sky}^{\rm patch}\simeq 0.12\%$ per patch. Curvature corrections are of order $L^2\!\sim\!1.5\times 10^{-2}$ and the flat-sky approximation is accurate. Patch projection is implemented by \texttt{healpy.projector.GnomonicProj}: the temperature channel is resampled by scalar interpolation, while $(Q,U)$ are first resampled and then spin-2 rotated to the local patch frame using Eq.~(\ref{eq:flatsky_estimator})\,'s spin-2 matrix, with rotation angle $\psi(\hat n)$ computed numerically by finite-differencing the projection along the local north direction (function \texttt{\_project\_patch\_tqu}, flag \texttt{rotate\_pol=True}; Sec.~\ref{sec:flatsky}). Non-finite values introduced at projection edges are explicitly replaced by zero.
  \item Patch-centre selection and full-patch validity: candidate centres are drawn from the set $\{i:M_i\geq 0.9\}$ at $N_{\rm side}=512$. For each candidate, the mask is itself first projected onto the same $64\!\times\!64$ gnomonic grid (function \texttt{\_project\_patch\_scalar}), and the candidate is rejected unless every pixel of the projected footprint satisfies $M\!\geq\!0.9$ (function \texttt{\_extract\_random\_patches}). This produces an ensemble of effectively unmasked patches at the cost of a non-trivial reduction of usable sky, with an empirically observed acceptance rate of order $50$--$60\%$ at $N_{\rm side}=512$ for the $143\,\mathrm{GHz}$ mask. A target of $N_{\rm patches/map}$ patches is collected per full-sky realisation via rejection sampling, with a hard cap of $40\,N_{\rm patches/map}$ attempts before raising an error.
  \item Splits and standardisation: the train/validation split is performed at the level of full-sky realisations rather than at the patch level (a $90/10$ split of the $N_{\rm maps}$ realisations, followed by concatenation of the corresponding per-realisation patch banks), so that no large-scale Fourier modes are shared between training and validation; per-channel statistics $\mu_c,\sigma_c$ ($c\in\{T,Q,U\}$) are computed once on the training split only and applied verbatim to the validation split (Sec.~\ref{sec:normalisation}, \ref{sec:data_savedpriors}). To guard against degenerate channels, $\sigma_c$ is floored at $10^{-6}$ before inversion.
  \item Bandpower binning: when validating a learned prior with the flat-sky estimator (\ref{eq:flatsky_estimator}), we bin into $\sim 30$ linear or log-spaced annuli of $|\bm\ell|$ between $\ell_{\rm min}\simeq 2\pi/L\approx 50$ and $\ell_{\rm max}\simeq \pi/\theta_{\rm patch}\approx 1500$.
\end{itemize}

\begin{table}[h]
  \centering
  \begin{tabular}{ll}
    \toprule
    Parameter & Value \\
    \midrule
    HEALPix resolution $N_{\rm side}$ & $512$ \\
    Harmonic cutoff $\ell_{\max}$ & $1535$ \\
    Common effective beam $\theta_{\rm FWHM}$ & $10'$ Gaussian \\
    Frequency & $143\,\mathrm{GHz}$ \\
    Sky mask & Planck $143\,\mathrm{GHz}$ confidence, $M\geq 0.9$ \\
    Patch grid & $64\times 64$ \\
    Patch sampling $\theta_{\rm patch}$ & $6.7'$/pixel \\
    Patch extent $L$ & $\simeq 7.15^\circ$ ($\simeq 51\,\mathrm{deg}^2$) \\
    Train/val split & $90/10$ \\
    Bandpower bins & $\sim 30$, $\ell\in[50,1500]$ \\
    Units & $\mu\mathrm{K}_{\rm CMB}$ \\
    \bottomrule
  \end{tabular}
  \caption{Summary of common processing parameters used uniformly for the observed sky $y$, the simulated CMB prior $p(f)$ (Sec.~\ref{sec:data_prior_f}), and the simulated foreground prior $p(x)$ (Sec.~\ref{sec:data_prior_x}).}
  \label{tab:processing_params}
\end{table}

The remainder of this section documents the product-specific processing on top of these common conventions: the Planck half-ring preprocessing (Sec.~\ref{sec:data_obs}), the CAMB-based CMB simulation (Sec.~\ref{sec:data_prior_f}), and the PySM-based foreground simulation (Sec.~\ref{sec:data_prior_x}).

\subsection{Observed CMB data}
\label{sec:data_obs}

We use the publicly released Planck PR4/NPIPE \cite{planck2018overview} half-ring maps at $143\,\mathrm{GHz}$, $h_1$ and $h_2$, and the corresponding $143\,\mathrm{GHz}$ confidence mask, all distributed as HEALPix \texttt{RING}-ordered FITS files at $N_{\rm side}^{\rm in}=2048$. Each half-ring file contains the three Stokes parameters $(I,Q,U)$ in $\mathrm{K}_{\rm CMB}$.

The two half-ring maps are processed independently by the routine \texttt{preprocess\_pr4\_hr\_to\_nside512}, which implements the harmonic-space ``smooth-and-downgrade'' procedure of Sec.~\ref{sec:downgrade}:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item read $(I,Q,U)$, replace \texttt{HEALPix UNSEEN} sentinels and non-finite values by zero, and rescale by $10^6$ to convert $\mathrm{K}_{\rm CMB}\!\to\!\mu\mathrm{K}_{\rm CMB}$ (units inferred from the FITS \texttt{TUNIT}/\texttt{BUNIT} header or, as a fallback, from the map RMS);
  \item compute polarised harmonic coefficients $(a^T_{\ell m},a^E_{\ell m},a^B_{\ell m})$ with \texttt{healpy.map2alm} (\texttt{pol=True}, \texttt{iter=3}) up to $\ell_{\max}=3N_{\rm side}-1=1535$;
  \item multiply each coefficient by the residual Gaussian transfer function
  \begin{equation}
    B^{\rm add}_\ell = \exp\!\left[-\tfrac12\,\ell(\ell+1)\,\sigma^2_{\rm add}\right],\qquad
    \sigma_{\rm add}=\frac{1}{\sqrt{8\ln 2}}\sqrt{\theta_{\rm out}^2-\theta_{\rm in}^2},
  \end{equation}
  with $\theta_{\rm in}=7.27'$ and $\theta_{\rm out}=10'$ as in Sec.~\ref{sec:beam};
  \item synthesise $(T,Q,U)$ at $N_{\rm side}=512$ via \texttt{healpy.alm2map} (\texttt{pol=True}).
\end{enumerate}
Pixel-window deconvolution is omitted at this stage, an approximation justified in Sec.~\ref{sec:data}: the input pixel-window correction at $N_{\rm side}^{\rm in}=2048$ is $\lesssim 0.3\%$ throughout the represented multipole range.

From the two preprocessed half-ring maps we form the half-mission-style sum and difference of Sec.~\ref{sec:halfring},
\begin{equation}
  y = \tfrac12(h_1+h_2),\qquad d = \tfrac12(h_1-h_2),
\end{equation}
and use $y$ as the observed sky entering the likelihood of Sec.~1 and $d$ as a sky-signal-free realisation of the instrumental noise. On the clean-sky footprint defined in Sec.~\ref{sec:data} we verify the expected coherence properties: $h_1$ and $h_2$ are positively correlated pixel-by-pixel in all three Stokes channels; the half-ring difference satisfies $\mathrm{RMS}(d)\!\ll\!\mathrm{RMS}(y)$, with the noise-to-signal RMS ratio of order $10\%$ in $T$ and order $1$ in $Q,U$ at the $10'$ resolution; and the binned cross-spectrum $\widehat C_\ell^{h_1\times h_2}$ tracks the fiducial CMB spectrum (Sec.~\ref{sec:data_prior_f}) across $30\!\le\!\ell\!\le\!\ell_{\max}$ in all four diagonal channels $TT$/$EE$/$BB$/$TE$. These checks fix the data-domain conventions used identically for the two simulated priors below.

\subsection{\texorpdfstring{$p(f)$}{p(f)}: simulated CMB priors}
\label{sec:data_prior_f}

The CMB prior $p(f)$ is constructed at the level of \emph{maps} that are physically lensed by an independently sampled realisation of the lensing potential $\phi$, rather than by drawing Gaussian fields with the lensed angular power spectrum directly. This sub-section spells out the simulation pipeline, motivates the choice over the simpler Gaussian-from-lensed-$C_\ell$ alternative (which is retained as a legacy/ablation path), and discusses the path towards a fully self-consistent treatment of lensing as an explicit forward-model operator.

\paragraph{Cosmology and CAMB call.}
We adopt a fiducial flat $\Lambda$CDM cosmology with $H_0=67.5\,\mathrm{km\,s^{-1}\,Mpc^{-1}}$, $\Omega_b h^2=0.022$, $\Omega_c h^2=0.122$, $\sum m_\nu=0.06\,\mathrm{eV}$, $\tau=0.06$, $A_s=2.1\times 10^{-9}$, $n_s=0.965$, $r=0$. A single call to the Boltzmann solver \texttt{CAMB} \cite{lewis2000camb} with \texttt{lens\_potential\_accuracy}$=1$ provides, in $\mu\mathrm{K}^2$ and on the integer grid $L=0,\dots,\ell_{\max}$:
\begin{itemize}[leftmargin=1.5em,itemsep=0.1em]
  \item the unlensed scalar CMB spectra $\{C_\ell^{TT,\,\rm u},C_\ell^{EE,\,\rm u},C_\ell^{BB,\,\rm u},C_\ell^{TE,\,\rm u}\}$ via \texttt{get\_cmb\_power\_spectra(..., raw\_cl=True)['unlensed\_scalar']}; with $r=0$ the unlensed $C_\ell^{BB,\,\rm u}$ is identically zero;
  \item the lensing-potential auto-spectrum $C_L^{\phi\phi}$ via \texttt{get\_lens\_potential\_cls(..., raw\_cl=True)[:,0]};
  \item for diagnostic purposes only, the lensed total spectra $\{C_\ell^{TT},C_\ell^{EE},C_\ell^{BB},C_\ell^{TE}\}$, which provide the analytic 2-point target that the lensed sims must match in expectation.
\end{itemize}
The unlensed spectra plus $C_L^{\phi\phi}$ are the only inputs to the lensing pipeline below; the lensed CAMB spectra are retained as a validation target only.

\paragraph{Lensed-map sampler.}
We use the curved-sky CMB-lensing remapping engine \texttt{lenspyx}~\cite{reinecke2023ducc}, which wraps the high-performance non-uniform spherical-harmonic transforms of \texttt{DUCC} and computes the deflected sky to a user-controlled accuracy with cost essentially equal to two standard pol-aware SHTs. Each independent map realisation in the prior bank is generated by the routine \texttt{simulate\_lensed\_cmb\_tqu\_lenspyx}, which proceeds in five harmonic-space steps:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item \emph{Bandlimits.} Set the lensing-remap bandlimit to $\ell_{\max}=3N_{\rm side}-1=1535$ and zero the unphysical $\ell=0,1$ entries of $C_\ell^{TT,\,\rm u}$, $C_\ell^{EE,\,\rm u}$, $C_\ell^{BB,\,\rm u}$, $C_\ell^{TE,\,\rm u}$, $C_L^{\phi\phi}$ before any sampling.
  \item \emph{Unlensed primary draw.} Draw a single correlated polarised realisation of the unlensed CMB,
  \begin{equation}
    (a^{T,\rm u}_{\ell m},a^{E,\rm u}_{\ell m},a^{B,\rm u}_{\ell m})
    \sim
    \mathcal N\!\left(0,
    \begin{pmatrix}C_\ell^{TT,\,\rm u} & C_\ell^{TE,\,\rm u} & 0\\
                   C_\ell^{TE,\,\rm u} & C_\ell^{EE,\,\rm u} & 0\\
                   0 & 0 & C_\ell^{BB,\,\rm u}\end{pmatrix}\right),
  \end{equation}
  with \texttt{healpy.synalm} (\texttt{new=True}), exactly as in the legacy Gaussian sampler but with the \emph{unlensed} CAMB spectra.
  \item \emph{Lensing-potential draw.} Independently draw a Gaussian realisation of the lensing potential,
  \begin{equation}
    \phi_{LM}\sim\mathcal N(0,C_L^{\phi\phi}),
  \end{equation}
  by a separate \texttt{healpy.synalm} call. The CMB$\times\phi$ cross-spectra $C_L^{\phi T}$ and $C_L^{\phi E}$ are not imposed here, an approximation discussed below.
  \item \emph{Deflection field.} The gravitational deflection vector is the gradient of $\phi$, $\bm\alpha(\hat n)=\nabla\phi(\hat n)$, with curl-free gradient-mode alm $d_{LM}^{\rm grad}=\sqrt{L(L+1)}\,\phi_{LM}$ (curl-mode $\Omega_{LM}=0$ for scalar lensing). The factor $\sqrt{L(L+1)}$ is precisely the lenspyx convention for the input deflection alm and is applied via \texttt{healpy.almxfl}.
  \item \emph{Remap onto the deflected sphere.} A single call
  \begin{center}
    \texttt{lenspyx.alm2lenmap}$\bigl([a^{T,\rm u},\,a^{E,\rm u},\,a^{B,\rm u}],\,[d^{\rm grad}]\bigr)$
  \end{center}
  with \texttt{geometry=('healpix', \{nside: 512\})}, \texttt{pol=True} and \texttt{epsilon}$=10^{-6}$ produces a \emph{truly lensed} $(T_{\rm lens},Q_{\rm lens},U_{\rm lens})$ realisation: the remap is implemented by ducc0 as a pair of non-uniform SHTs that evaluate the unlensed CMB at the deflected positions and rotate $(Q,U)$ by the appropriate spin-2 phase along the deflection direction.
\end{enumerate}
The output of step (5) is a HEALPix map at $N_{\rm side}=512$ that carries the full lensing imprint of the sampled $\phi$, not just its diagonal power-spectrum signature. By construction it inherits, in expectation, the lensed CAMB power spectra of step (1), but at the level of an individual realisation it also contains all the lensing-induced non-Gaussian structure: the connected 4-point function (trispectrum) that underpins quadratic CMB-lensing reconstruction, the non-Gaussian texture of lensing $B$-modes generated by $E\!\to\!B$ mode coupling, and the lensing modulation of the small-scale acoustic structure (Sec.~\ref{sec:realistic_spectra}).

\paragraph{Post-lensing instrumental response.}
The remap is performed at the native unbeamed, unwindowed resolution. The common observational response is then applied to the lensed map in harmonic space, matching the convention used for $y$ in Sec.~\ref{sec:data_obs}:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item compute $(a^T_{\ell m},a^E_{\ell m},a^B_{\ell m})$ of $(T_{\rm lens},Q_{\rm lens},U_{\rm lens})$ via \texttt{healpy.map2alm} (\texttt{pol=True}, \texttt{iter=3});
  \item multiply by the joint beam-and-pixel-window transfer functions
  \begin{equation}
    a_{\ell m}^T \!\to\! B_\ell\,P_\ell^{T}\,a_{\ell m}^T,
    \qquad
    a_{\ell m}^{E,B} \!\to\! B_\ell\,P_\ell^{P}\,a_{\ell m}^{E,B},
  \end{equation}
  with $B_\ell$ from Eq.~(\ref{eq:beam_Bl}) at $\theta_{\rm FWHM}=10'$ and $P_\ell^{T,P}$ from \texttt{healpy.pixwin($\cdot$, pol=True)};
  \item synthesise the final $(T,Q,U)$ at $N_{\rm side}=512$ via \texttt{healpy.alm2map} (\texttt{pol=True}).
\end{enumerate}
This ordering---lens \emph{first}, beam and pixel-window \emph{after}---is the physically correct one: the beam and the HEALPix pixel window act on the observed (and therefore lensed) sky, not on the underlying primary CMB. Each realisation is generated from an independent random seed that drives both the unlensed $(a^{T,\rm u},a^{E,\rm u},a^{B,\rm u})$ draw and the independent $\phi$ draw, so $N_f$ runs yield a statistically independent ensemble at fixed cosmology.

\paragraph{Why not just Gaussian draws from the lensed $C_\ell$?}
The legacy sampler \texttt{simulate\_cmb\_tqu\_from\_cls}, which draws Gaussian alm from the \emph{lensed} CAMB spectra and applies beam/pixel-window in one shot, is retained in the codebase for back-compatibility and as a controlled ablation. It is however strictly mis-specified once any non-2-point statistic is exposed to inference:
\begin{itemize}[leftmargin=1.5em,itemsep=0.1em]
  \item It reproduces $\langle C_\ell^{TT/EE/BB/TE}\rangle$ by construction but has identically Gaussian one-point and higher-order $n$-point statistics, with vanishing lensing trispectrum and vanishing $T,E,\phi$-bispectrum at fixed $\phi$.
  \item The lensing-induced $B$-modes generated by $E\!\to\!B$ leakage have, in real lensed skies, a characteristic non-Gaussian morphology that traces the underlying $\phi$ field; a Gaussian draw from the same $C_\ell^{BB}$ produces $B$-modes with no such structure and is observationally indistinguishable from primordial $r$-driven $B$ at the 2-point level only.
  \item Cosmology-marginalisation extensions (varying $\Omega_m$, $\Omega_\Lambda$, etc.) shift both $C_\ell^{\rm unl}$ and $C_L^{\phi\phi}$ \emph{consistently}, so the lenspyx pipeline correctly propagates parameter variations to the lensed CMB; the Gaussian-lensed-$C_\ell$ pipeline only propagates the marginal $C_\ell^{\rm lensed}$, losing the correlated parameter dependence of the lensing kernel.
\end{itemize}
The remapping cost is dominated by two pol-aware SHTs at $N_{\rm side}=512$ (a fraction of a second per realisation on a single core through DUCC), so the upgrade is essentially free relative to map I/O and patch extraction. Empirically (notebook validation cell), individual lenspyx realisations recover the lensed CAMB $\{C_\ell^{TT},C_\ell^{EE}\}$ to within cosmic variance on the full sky; the lensing-induced $C_\ell^{BB}$ converges from below as the lensing bandwidth approaches the support of $C_L^{\phi\phi}\!\otimes\!C_\ell^{EE}$, an artefact of the finite-$\ell_{\max}$ remap that vanishes well before the noise level of Planck $143\,\mathrm{GHz}$.

\paragraph{Approximations and forward look: explicit lensing operator (Option 2).}
The current pipeline carries one explicit approximation: the unlensed CMB $(a^{T,\rm u},a^{E,\rm u},a^{B,\rm u})$ and the lensing potential $\phi_{LM}$ are drawn \emph{independently}, so the small ($\mathcal O(10^{-3})$) CMB$\times\phi$ cross-spectra $C_L^{\phi T}$ (from late-time ISW) and $C_L^{\phi E}$ are not imposed at the level of individual realisations. This is the standard convention used by Planck FFP10 \cite{planck2018overview} and CMB-S4 simulation libraries, and is negligible compared with cosmic variance at the multipole range used here ($\ell\lesssim 1500$). A subdominant second approximation is that $\phi$ is treated as Gaussian; the leading-order $\phi$ non-Gaussianity from non-linear large-scale structure is at the few-percent level on $C_L^{\phi\phi}$ \cite{lewis2006lensing} and well below the simulation accuracy required for the present problem.

A more principled treatment, planned as a follow-up extension when polarisation $B$-modes (and hence the tensor-to-scalar ratio $r$) become the science target, is to absorb the lensing remap into the forward model rather than into the prior. Concretely, Eq.~(\ref{eq:fwd_model}) is rewritten as
\begin{align}
  y \;=\; A\,\bigl[\,\mathcal L(\phi)\,f_{\rm u} \,+\, x\bigr] \,+\, n,
  \label{eq:fwd_model_explicit_lensing}
\end{align}
where $f_{\rm u}$ is the \emph{unlensed} CMB and $\mathcal L(\phi)$ is the gravitational-lensing operator built from the lensing potential $\phi$ (the same operator that \texttt{lenspyx.alm2lenmap} implements). The advantages of this ``Option 2'' are that
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item the prior $p(f_{\rm u})$ becomes a Gaussian on the unlensed CMB, which is the physically correct prior and whose density is closed-form---no need to train a neural prior to learn lensing non-Gaussianity, since it is now in the model;
  \item the lensing potential $\phi$ becomes a latent random field with a Gaussian prior $p(\phi)=\mathcal N(0,C_L^{\phi\phi})$ that can be jointly inferred from the data, yielding a calibrated posterior $p(f_{\rm u},x,\phi\mid y)$ rather than a marginal $p(f,x\mid y)$;
  \item cosmological-parameter marginalisation is straightforward because all dependence on $\Omega_m$, $\Omega_\Lambda$, $H_0$ is now carried by the closed-form Gaussian priors on $f_{\rm u}$ and $\phi$ rather than by an empirically learned non-Gaussian prior.
\end{enumerate}
Three sub-variants of Option 2 are natural and would re-use most of the present infrastructure:
\begin{itemize}[leftmargin=1.5em,itemsep=0.1em]
  \item \emph{Fixed-$\phi$.} Replace $\phi$ in Eq.~(\ref{eq:fwd_model_explicit_lensing}) with the public Planck PR4 lensing reconstruction \texttt{COM\_Lensing\_4096\_R4.00} \cite{carron2022planckpr4lensing}. The likelihood gradients used during posterior sampling become $\nabla_{f_{\rm u}}\log p(y\mid f_{\rm u},x)$ computed through the linear adjoint of $\mathcal L(\hat\phi)$, which lenspyx also exposes. Cheapest variant but ignores reconstruction noise on $\hat\phi$.
  \item \emph{Joint $\phi$ sampling.} Augment the latent state of the diffusion sampler with $\phi$ and add likelihood gradients $\nabla_{\phi}\log p(y\mid f_{\rm u},x,\phi)$ alongside the existing $\nabla_{f_{\rm u},x}\log p(y\mid f_{\rm u},x,\phi)$. This realises the MUSE / Anderes--Millea--Seljak scheme \cite{millea2020muse} in the diffusion-sampler framework and yields a fully calibrated joint posterior on $(f_{\rm u},x,\phi)$.
  \item \emph{Quadratic-marginalised $\phi$.} Marginalise $\phi$ to leading order analytically (Hu--Okamoto quadratic estimator). Useful as a fast consistency check; the residual non-Gaussianity in $f_{\rm u}$ after marginalisation is at the level of higher-order $\phi$ moments and is negligible at Planck noise levels.
\end{itemize}
The Option-1 sampler implemented in this work is the strict subset of the fixed-$\phi$ variant in which one further drops the dependence on $\hat\phi$ at sampling time and absorbs the resulting marginal lensed CMB into a learned prior. It is the right starting point for the present temperature-dominated analysis, and provides a precise quantitative baseline against which any future Option-2 extension will be validated.

\paragraph{Output.}
A bank of $N_f$ independent full-sky lenspyx-lensed realisations is generated by the routine \texttt{build\_prior\_patch\_datasets} (with \texttt{lensing\_method=\textquotesingle lenspyx\_remap\textquotesingle}, the default) and reduced to flat $(T,Q,U)$ patches by the common patch-extraction and standardisation pipeline of Sec.~\ref{sec:data}. The resulting training-ready array has shape $(N_{\rm patches},3,64,64)$ with $N_{\rm patches}=N_f\times N_{\rm patches/map}$. Selecting \texttt{lensing\_method=\textquotesingle gaussian\_lensed\_cl\textquotesingle} switches back to the legacy Gaussian-from-lensed-$C_\ell$ sampler with the same output schema, useful as an ablation control.

\subsection{\texorpdfstring{$p(x)$}{p(x)}: simulated foreground priors}
\label{sec:data_prior_x}

The Galactic-foreground prior is built from PySM\,3 \cite{thorne2017pysm,zonca2021pysm3}, instantiated at $N_{\rm side}=512$ with the small-scale component model \texttt{d10}+\texttt{s5}: \texttt{d10} is a modified-blackbody thermal-dust template whose dust temperature $T_d(\hat n)$ and spectral index $\beta_d(\hat n)$ are spatially varying maps derived from Planck data, and whose small-scale fluctuations below the resolution of the input templates ($\ell\!\gtrsim\!300$) are populated stochastically by a non-Gaussian small-scale model fitted to high-latitude Planck residuals; \texttt{s5} is the analogous spatially-varying-spectral-index synchrotron model whose small-scale modes are drawn stochastically from a fitted power-law amplitude. The choice of small-scale presets, rather than the deterministic \texttt{d1}+\texttt{s1} pair, is critical: it makes each foreground realisation a genuinely independent draw of the small-scale Galactic field, rather than a rotated copy of a single deterministic template, and is what allows the learned prior $p(x)$ to capture the patch-level variability of the Galactic emission. We exclude PySM's \texttt{c1} (lensed-CMB) component from the foreground sky used in $p(x)$, since the lensed CMB is already drawn independently as $p(f)$ and including it twice would bias the joint prior; an exploratory \texttt{d1}+\texttt{s1}+\texttt{c1} sky is instantiated at the top of the data section purely as a visual sanity check (Mollweide views of $I$ and $P=\sqrt{Q^2+U^2}$ at $143\,\mathrm{GHz}$) and is not used downstream.

\paragraph{Independent stochastic realisations.}
For each of $N_x$ realisations we re-seed the random number generator that drives PySM's small-scale models with a fresh seed derived deterministically from a master foreground seed, and then build a new \texttt{Sky} object. Because PySM samples its small-scale modes at \texttt{Sky}-construction time, this guarantees that successive realisations have \emph{statistically independent} small-scale Galactic structure while sharing the same large-scale template above the small-scale-model crossover multipole. In particular, both the morphology of the small-scale dust filaments and the small-scale polarisation amplitude vary realisation-to-realisation, in contrast with the deterministic \texttt{d1}+\texttt{s1} presets where these would be repeated identically. Emission is evaluated at the common observing frequency $143\,\mathrm{GHz}$ and converted from PySM's native Rayleigh--Jeans antenna units to thermodynamic $\mu\mathrm{K}_{\rm CMB}$ through the Planck blackbody equivalency, ensuring unit consistency with $y$ and $f$.

\paragraph{Random orientation augmentation.}
Each stochastic realisation is then optionally rotated by a uniformly sampled element $R\in\mathrm{SO}(3)$, drawn via Euler angles
\begin{equation}
  \alpha\sim\mathcal U(0,2\pi),\qquad
  \cos\beta\sim\mathcal U(-1,1),\qquad
  \gamma\sim\mathcal U(0,2\pi),
  \label{eq:so3_uniform}
\end{equation}
acting on the full $(T,Q,U)$ stack with the spin-correct rotation of polarisation under change of basis (so that $E$/$B$ are preserved). Because the Galactic plane is a strong morphological prior on the unrotated PySM sky, the rotation augmentation prevents the patch ensemble from being biased towards the small subset of orientations sampled by a fixed Galactic frame, and (combined with the by-map train/validation split below) avoids the trivial overfitting in which the model memorises specific Galactic features.

\paragraph{Common transfer-function application.}
The rotated $(T,Q,U)$ realisation is then transported into harmonic space, multiplied by the common transfer functions, and synthesised back to a HEALPix map. Specifically, polarised harmonic coefficients $(a^T_{\ell m},a^E_{\ell m},a^B_{\ell m})$ up to the band-limit $\ell_{\max}=3N_{\rm side}-1$ are filtered through
\begin{equation}
  a_{\ell m}^T\to B_\ell\,P_\ell^{T}\,a_{\ell m}^T,
  \qquad
  a_{\ell m}^{E,B}\to B_\ell\,P_\ell^{P}\,a_{\ell m}^{E,B},
\end{equation}
with the $10'$ FWHM Gaussian beam $B_\ell$ and the polarisation-aware HEALPix pixel windows $P_\ell^{T}$, $P_\ell^{P}$ defined in Sec.~\ref{sec:beam}--\ref{sec:pixwin}. This yields an effective transfer function for $p(x)$ that is \emph{identical} to the one applied to $p(f)$ in Sec.~\ref{sec:data_prior_f}, which is a prerequisite for any well-posed joint inference of $f$ and $x$ from the same observed $y$: a mismatch in the effective beams or pixel windows of the two priors would propagate as a bias on the relative amplitudes of the two components in the posterior.

The full pipeline for a single foreground realisation is therefore
\begin{equation}
  \underbrace{\text{PySM \texttt{d10}+\texttt{s5}@}\,143\,\mathrm{GHz}}_{\text{re-seeded \texttt{Sky}, independent draw}}
  \;\xrightarrow{\;\text{SO(3) rotation}\;}\;
  \;\xrightarrow{\;\text{beam + pixel window}\;}\;
  \;\xrightarrow{\;\text{patches}\;}\;
  \;\xrightarrow{\;\text{by-map split + standardise}\;}\;
  \text{saved \texttt{.npz}.}
\end{equation}
Patch extraction, splitting and per-channel standardisation proceed identically to $p(f)$ via the common pipeline of Sec.~\ref{sec:data}, ensuring that the two priors are ingested by the U-Net in the same numerical and angular domain. This is a prerequisite for the joint posterior sampler of Sec.~1, which assumes a single shared representation for $f$ and $x$.

\paragraph{Limitations.}
We note two limitations of this construction inherited from the underlying simulation choices. First, although \texttt{d10}+\texttt{s5} populates the small scales stochastically, the large-scale structure ($\ell\!\lesssim\!300$) is still anchored to a deterministic Planck-derived template; the rotation augmentation enriches the orientation distribution but not the large-scale stochasticity of the Galactic field, so the learned prior $p(x)$ is best interpreted as the distribution of $143\,\mathrm{GHz}$ foreground patches consistent with the Planck-derived large-scale templates and a fitted small-scale model, rather than a fully marginalised model over Galactic-emission parameters. Second, additional foreground species (AME, free-free, point sources) are deferred to future work. These restrictions are consistent with the scope of the present prior-learning stage and do not affect the validity of the pipeline that follows.

\subsection{Conversion to flat patches and saved prior datasets}
\label{sec:data_savedpriors}

We collect here, in one place, the full sequence of transformations applied to each prior between simulation and disk, together with the on-disk format of the resulting datasets. This is the data that is loaded by the diffusion-model training and sampling cells of Sec.~5, and the description that follows fixes everything the reader needs to know to reproduce them.

\paragraph{Per-realisation pipeline.} For a single full-sky realisation of $f$ or $x$, the transformations are applied in the following fixed order:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item \emph{Generate} a $(T,Q,U)$ HEALPix map at $N_{\rm side}=512$ in $\mu\mathrm{K}_{\rm CMB}$, including all relevant transfer functions. For $p(f)$, the default lensing strategy is the curved-sky lenspyx remap of Sec.~\ref{sec:data_prior_f}: an independent unlensed CMB realisation $(a^T,a^E,a^B)$ is drawn from the unlensed CAMB spectra together with an independent realisation $\phi_{LM}$ of the lensing potential, the unlensed sky is gravitationally lensed by $\phi$ on the curved sphere, and the common Gaussian beam $B_\ell$ and HEALPix pixel windows $P_\ell^{T,P}$ are applied in harmonic space to the lensed map. The legacy Gaussian-from-lensed-$C_\ell$ path shares the same beam-and-pixel-window step and remains available as an ablation control. For $p(x)$, each realisation re-seeds PySM's small-scale random number generator and instantiates a fresh \texttt{Sky}, so that the small-scale Galactic structure is statistically independent across realisations (Sec.~\ref{sec:data_prior_x}); the resulting full-sky map is rotated by a uniform $\mathrm{SO}(3)$ element and then propagated through the same harmonic-space beam-and-pixel-window step used for $p(f)$, so that the effective transfer functions of the two priors are identical by construction.
  \item \emph{Patch extraction} via \texttt{\_extract\_random\_patches}: a target of $N_{\rm patches/map}$ flat-sky patches is collected by rejection sampling, with candidate centres restricted to mask-clean HEALPix pixels and the full $64\!\times\!64$ projected footprint required to satisfy $M\!\geq\!0.9$ (Sec.~\ref{sec:data}). Each accepted patch is the output of \texttt{\_project\_patch\_tqu} with \texttt{rotate\_pol=True}, which scalar-resamples $T$, scalar-resamples $(Q,U)$, and then spin-2 rotates $(Q,U)$ to the local patch frame using a numerically-evaluated $\psi(\hat n)$ (Sec.~\ref{sec:flatsky}); non-finite values introduced at patch edges are replaced by zero.
  \item \emph{Concatenation} of the per-realisation patch banks into a single array of shape $(N_{\rm patches},3,64,64)$ with $N_{\rm patches}=N_{\rm maps}\!\times\!N_{\rm patches/map}$, dtype \texttt{float32}, channel order $(T,Q,U)$.
\end{enumerate}

\paragraph{Train/validation split by map index.} The split is performed at the level of \emph{full-sky realisations}, not at the level of individual patches. Concretely, the $N_{\rm maps}$ underlying full-sky realisations are first shuffled with a deterministic seed and partitioned $90/10$ into a training set of $N_{\rm maps}^{\rm train}$ realisations and a validation set of $N_{\rm maps}^{\rm val}$ realisations; the corresponding per-realisation patch banks are then concatenated separately into $\texttt{train\_raw}$ and $\texttt{val\_raw}$. This is essential. A patch-level split would routinely place patches drawn from the \emph{same} underlying realisation into both the training and validation sets, in which case the validation set would inherit the large-scale Fourier modes already seen at training time (a non-trivial fraction of the patch power lies at $\ell\!\lesssim\!\pi/L\!\approx\!50$, where $L$ is the patch extent). The validation diagnostics would then systematically underestimate the generalisation error, and any spectral checkpoint selection driven by validation metrics would be biased. Splitting by realisation enforces statistical independence between the two splits at all scales.

\paragraph{Per-channel standardisation.} The two raw arrays are then passed through a deterministic per-channel affine standardisation:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.1em]
  \item compute per-channel statistics
  \begin{equation}
    \mu_c = \mathrm{mean}_{(\rm patch,\,x,\,y)}(\texttt{train\_raw}_c),
    \qquad
    \sigma_c = \mathrm{std}_{(\rm patch,\,x,\,y)}(\texttt{train\_raw}_c),
    \qquad c\in\{T,Q,U\},
  \end{equation}
  on the training split only, with $\sigma_c$ floored at $10^{-6}$ before inversion to guard against degenerate channels;
  \item apply the affine map $\tilde X_c = (X_c-\mu_c)/\sigma_c$ to both splits to produce \texttt{train\_norm} and \texttt{val\_norm};
  \item shuffle the patches \emph{within} each split, with an independent seed, to break the per-realisation block ordering inherited from the by-map concatenation.
\end{enumerate}

\paragraph{On-disk format.} The two priors are written to disk in compressed NumPy archives,
\begin{equation}
  \texttt{cmb\_fg\_prior\_f\_cmb.npz},\qquad
  \texttt{cmb\_fg\_prior\_x\_fg.npz},
\end{equation}
each containing six float32 arrays:
\begin{center}
\begin{tabular}{ll}
  \toprule
  Key & Description \\
  \midrule
  \texttt{train\_raw} & Training patches in physical $\mu\mathrm{K}_{\rm CMB}$, shape $(N_{\rm train},3,64,64)$ \\
  \texttt{val\_raw}   & Validation patches in physical $\mu\mathrm{K}_{\rm CMB}$, shape $(N_{\rm val},3,64,64)$ \\
  \texttt{train\_norm} & Standardised training patches, shape $(N_{\rm train},3,64,64)$ \\
  \texttt{val\_norm}   & Standardised validation patches, shape $(N_{\rm val},3,64,64)$ \\
  \texttt{mean}       & Per-channel training mean $\mu_c$, shape $(1,3,1,1)$ \\
  \texttt{std}        & Per-channel training std  $\sigma_c$, shape $(1,3,1,1)$ \\
  \bottomrule
\end{tabular}
\end{center}
A companion human-readable \texttt{cmb\_fg\_meta.json} captures every parameter of the build (resolution, $\ell_{\max}$, beam, pixel-window flag, patch geometry, mask threshold, full-patch validity flag, $N_{\rm maps}$, foreground frequency and presets, rotation flag, train fraction, and all RNG seeds), so that the dataset is fully reproducible from the notebook state.

Storing both \texttt{*\_raw} (physical units) and \texttt{*\_norm} (standardised) variants serves two complementary purposes: training and U-Net inference are performed entirely in the normalised domain (where $T,Q,U$ have unit per-channel variance and zero mean by construction), while spectral diagnostics, posterior sampling, and any comparison against theoretical $C_\ell$ tables are most naturally expressed in physical $\mu\mathrm{K}_{\rm CMB}^2$ units and require the de-standardisation $X_c = \sigma_c\tilde X_c + \mu_c$, with $\mu_c,\sigma_c$ retrieved from the saved \texttt{mean},\texttt{std} arrays.

This concludes the data description. Everything entering the diffusion-model training and posterior-sampling stages of Sec.~5 is fully specified by these two \texttt{.npz} files together with the original $y,d$ half-ring maps of Sec.~\ref{sec:data_obs}, the common processing parameters of Table~\ref{tab:processing_params}, and the metadata file written alongside the priors.





\section{Deterministic flow matching for patch-based CMB priors}
\label{sec:fm}

The remainder of this note is devoted to the generative-model component of the pipeline. We learn the priors $p(f)$ and $p(x)$ on the standardised patch space
\[
  \xvec\in\Ndata,\qquad D = C\,H\,W = 3\!\times\!64\!\times\!64 = 12{,}288,
\]
by training, separately for each prior, a continuous-time generative model that transports samples from a tractable base distribution $\pinit=\mathcal N(0,I_D)$ to the empirical patch distribution $\pdata$ along an ODE driven by a learned vector field. We use the \emph{deterministic} flow-matching formulation of \cite{lipman2023flowmatching,liu2022rectified,albergo2023interpolants}, which makes sampling a noiseless map from base noise to data and is therefore the natural choice for an analytic prior to be plugged into a downstream posterior sampler with explicit likelihood gradients (Sec.~\ref{sec:fm_sample_pipeline}). For completeness we also explain how the same machinery degenerates, with $\sigma>0$, into the score-augmented SDE family that includes denoising diffusion as a special case, but emphasise that the production model in this work is run as a pure deterministic flow.

\subsection{Mathematical formalism: probability paths and flow matching}
\label{sec:fm_formalism}

\subsubsection{Probability paths and flow models}

\begin{definition}[Probability path]
\label{def:prob_path}
A \emph{probability path} on $\Ndata$ is a continuous family of probability densities $\{p_t\}_{t\in[0,1]}$ that interpolates between a tractable base $p_0=\pinit$ and the data distribution $p_1=\pdata$. We write $p_t(\xvec)$ for the density of the marginal at time $t$.
\end{definition}

\begin{definition}[Marginal velocity field]
\label{def:marg_vf}
A \emph{marginal velocity field} for the probability path $\{p_t\}$ is a measurable map $u:[0,1]\!\times\!\Ndata\!\to\!\Ndata$ such that the deterministic ODE
\begin{equation}
  \xvec_0\sim\pinit,
  \qquad
  \frac{\dd \xvec_t}{\dd t} = u_t(\xvec_t)
  \label{eq:flow_ode}
\end{equation}
generates $\xvec_t\sim p_t$ for every $t\in[0,1]$.
\end{definition}

The fundamental link between the path $\{p_t\}$ and the field $u_t$ is the continuity equation, which expresses local conservation of probability along the flow:
\begin{equation}
  \partial_t p_t(\xvec) \;+\; \nabla_{\xvec}\!\cdot\!\bigl[u_t(\xvec)\,p_t(\xvec)\bigr] \;=\; 0,
  \qquad t\in[0,1],
  \label{eq:continuity}
\end{equation}
\emph{interpreted as a partial differential equation in the distributional sense} \cite{lipman2023flowmatching}. Conversely, given any path $\{p_t\}$ for which Eq.~(\ref{eq:continuity}) admits a smooth solution $u_t$ with $u_t(\xvec)$ bounded, the ODE in Eq.~(\ref{eq:flow_ode}) defines the unique flow $\psi_t:\Ndata\to\Ndata$ such that $\psi_t(\xvec_0)\sim p_t$ for $\xvec_0\sim\pinit$. \emph{Sampling from $\pdata$ then reduces to numerical integration of Eq.~(\ref{eq:flow_ode}) up to $t=1$.}

The strategic choice of \cite{lipman2023flowmatching} is to design the path $\{p_t\}$ first, then \emph{learn} $u_t$ by regression. The difficulty is that even when $\{p_t\}$ is given by a closed-form recipe, the marginal $p_t$ is in general only available through a high-dimensional integral over the data distribution and the generating noise. Direct simulation-based regression of $u_t$ is therefore intractable. The \emph{conditional flow-matching} construction circumvents this by introducing a conditioning variable.

\subsubsection{Conditional probability path and the marginalisation theorem}

\begin{definition}[Conditional probability path]
\label{def:cond_path}
Let $q$ be a fixed distribution on a measurable conditioning space $\mathcal Z$. A \emph{conditional probability path} indexed by $q$ is a family $p_t(\xvec\mid\zvec)$ of densities on $\Ndata$, parametrised by $\zvec\sim q$, together with a \emph{conditional velocity field} $u_t(\xvec\mid\zvec)$ that satisfies the conditional continuity equation
\begin{equation}
  \partial_t p_t(\xvec\mid\zvec) + \nabla_{\xvec}\!\cdot\!\bigl[u_t(\xvec\mid\zvec)\,p_t(\xvec\mid\zvec)\bigr] = 0
  \qquad \forall\,\zvec\in\mathcal Z.
  \label{eq:cond_continuity}
\end{equation}
\end{definition}

We take the conditioning distribution to be the data distribution itself, $q=\pdata$, and design $p_t(\xvec\mid\zvec)$ so that $p_0(\xvec\mid\zvec) = \pinit(\xvec)$ for every $\zvec$ and $p_1(\xvec\mid\zvec) = \delta(\xvec-\zvec)$ in the distributional sense. The marginal path is then recovered by
\begin{equation}
  p_t(\xvec) = \int_{\mathcal Z} p_t(\xvec\mid\zvec)\,\pdata(\zvec)\,\dd\zvec,
  \label{eq:marg_path}
\end{equation}
and a direct consequence of integrating Eq.~(\ref{eq:cond_continuity}) against $\pdata(\zvec)\,\dd\zvec$ is the following marginalisation theorem.

\begin{theorem}[Marginal velocity field; \cite{lipman2023flowmatching}]
\label{thm:marg_vf}
For any conditional probability path satisfying Definition~\ref{def:cond_path}, the marginal path Eq.~(\ref{eq:marg_path}) admits the marginal velocity field
\begin{equation}
  u_t(\xvec) \;=\; \int_{\mathcal Z}
                u_t(\xvec\mid\zvec)\,
                \frac{p_t(\xvec\mid\zvec)\,\pdata(\zvec)}{p_t(\xvec)}\,\dd\zvec
            \;=\; \EE_{\zvec\sim p_t(\zvec\mid\xvec)}\bigl[u_t(\xvec\mid\zvec)\bigr],
  \label{eq:marg_vf}
\end{equation}
with $p_t(\zvec\mid\xvec) \propto p_t(\xvec\mid\zvec)\,\pdata(\zvec)$ the time-$t$ posterior of $\zvec$ given $\xvec$.
\end{theorem}

Equation~(\ref{eq:marg_vf}) is fundamental: it shows that the unknown marginal velocity field is the posterior expectation of a closed-form quantity. The minimum-mean-square-error (MMSE) estimator of $u_t(\xvec)$ given $\xvec$ is therefore precisely Eq.~(\ref{eq:marg_vf}), i.e.\ the unique minimiser of the regression
\begin{equation}
  \min_{f}\;\EE_{t,\zvec,\xvec\mid\zvec}\!\bigl[\|f(\xvec,t) - u_t(\xvec\mid\zvec)\|^2\bigr]
  \quad\text{is}\quad
  f^\star(\xvec,t) = u_t(\xvec).
  \label{eq:mmse}
\end{equation}

\subsubsection{The conditional flow-matching loss}

Theorem~\ref{thm:marg_vf} and Eq.~(\ref{eq:mmse}) suggest a tractable training objective. We introduce a parametric vector field $\uvec_t^{\thetavec}:\Ndata\!\times\![0,1]\to\Ndata$ (a neural network with parameters $\thetavec$, see Sec.~\ref{sec:fm_arch}) and minimise the \emph{conditional flow-matching loss}
\begin{equation}
  \boxed{\;\mathcal L_{\rm CFM}(\thetavec)
  \;=\;
  \EE_{t\sim\mathcal U[0,1]}\,
  \EE_{\zvec\sim\pdata}\,
  \EE_{\xvec\sim p_t(\cdot\mid\zvec)}
  \!\left[\,\bigl\|\uvec_t^{\thetavec}(\xvec) - u_t(\xvec\mid\zvec)\bigr\|^2\,\right].\;}
  \label{eq:cfm}
\end{equation}

\begin{proposition}[Equivalence of marginal and conditional flow matching]
\label{prop:fm_equiv}
Let
\begin{equation}
  \mathcal L_{\rm FM}(\thetavec) = \EE_{t,\xvec\sim p_t}\bigl[\|\uvec_t^{\thetavec}(\xvec)-u_t(\xvec)\|^2\bigr]
\end{equation}
be the (intractable) marginal flow-matching loss. Then $\mathcal L_{\rm CFM}(\thetavec) - \mathcal L_{\rm FM}(\thetavec)$ is a constant independent of $\thetavec$, and consequently
\[
  \nabla_{\thetavec}\,\mathcal L_{\rm CFM}(\thetavec)
  = \nabla_{\thetavec}\,\mathcal L_{\rm FM}(\thetavec).
\]
In particular, the unique global minimiser of $\mathcal L_{\rm CFM}$ over the population of all measurable maps satisfies $\uvec^\star_t(\xvec) = u_t(\xvec)$ almost everywhere along the marginal path.
\end{proposition}

The proof is a direct consequence of Theorem~\ref{thm:marg_vf}: the cross-term $\EE\,[\uvec_t^{\thetavec}(\xvec)\!\cdot\!u_t(\xvec\mid\zvec)]$ in $\mathcal L_{\rm CFM}$ equals $\EE\,[\uvec_t^{\thetavec}(\xvec)\!\cdot\!u_t(\xvec)]$ once the inner expectation over $\zvec$ is evaluated with $p_t(\zvec\mid\xvec)$, while the difference $\|u_t(\xvec\mid\zvec)\|^2 - \|u_t(\xvec)\|^2$ is $\thetavec$-independent. Proposition~\ref{prop:fm_equiv} is what makes the entire flow-matching programme tractable: \emph{minimising a closed-form, low-variance regression target with the same gradient as the intractable marginal regression yields a network whose posterior-mean prediction is the marginal velocity field that drives Eq.~(\ref{eq:flow_ode}).}

Conditional flow matching is therefore a generalisation of the classical denoising score-matching argument of \cite{vincent2011dsm,song2020scorematching}, replacing the score $\nabla_{\xvec}\log p_t(\xvec)$ by the velocity $u_t(\xvec)$. Both quantities are MMSE projections of a closed-form conditional onto $\xvec$.

\subsubsection{Score--velocity correspondence and the deterministic limit}

For any conditional path one can also define the conditional score $s_t(\xvec\mid\zvec) = \nabla_{\xvec}\log p_t(\xvec\mid\zvec)$ and, applying Theorem~\ref{thm:marg_vf}-style marginalisation, show that the marginal score $s_t(\xvec)=\nabla_{\xvec}\log p_t(\xvec)$ is similarly given by a posterior expectation. For the Gaussian conditional paths used in this work (Sec.~\ref{sec:fm_path}), the marginal velocity field and the marginal score are linked by a closed-form linear identity
\begin{equation}
  u_t(\xvec) = \Bigl(\beta_t^2\,\tfrac{\dot\alpha_t}{\alpha_t} - \dot\beta_t\,\beta_t\Bigr)\,s_t(\xvec)
              + \tfrac{\dot\alpha_t}{\alpha_t}\,\xvec,
  \label{eq:score_to_velocity}
\end{equation}
which means that learning $u_t$ and learning $s_t$ are equivalent up to a deterministic post-hoc rescaling \cite{lipman2023flowmatching}. This identity also explains why the entire family of denoising diffusion models \cite{ho2020ddpm,song2021sde} can be viewed as a special case of flow matching with a particular schedule choice and a fixed reparametrisation of the regression target.

For \emph{any} smooth $\sigma_t\geq 0$, the SDE
\begin{equation}
  \dd \xvec_t = \Bigl[u_t(\xvec_t) + \tfrac{\sigma_t^{\,2}}{2}\,s_t(\xvec_t)\Bigr]\,\dd t \;+\; \sigma_t\,\dd \bm W_t,
  \qquad
  \xvec_0\sim\pinit,
  \label{eq:flow_sde}
\end{equation}
generates the same marginals $\{p_t\}$ as the deterministic ODE in Eq.~(\ref{eq:flow_ode}) (this is a textbook consequence of the Fokker--Planck equation; see Theorem~17 of \cite{lipman2023flowmatching}). The deterministic flow corresponds to $\sigma_t\equiv 0$ and the variance-preserving DDPM SDE corresponds to a particular non-zero schedule of $\sigma_t$. The two ends of this family are mathematically equivalent statements about the same path $\{p_t\}$, but they have very different practical implications. We discuss this trade-off and our deterministic-flow choice next.

\subsubsection{Why deterministic flow matching for this work}

We sample exclusively from the deterministic ODE flow ($\sigma_t\equiv 0$), and use the score-augmented SDE in Eq.~(\ref{eq:flow_sde}) only as a theoretical scaffolding to relate our framework to denoising diffusion. Three reasons motivate this choice.
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{Calibrated, gradient-friendly priors for posterior sampling.} The downstream goal of the project (Sec.~\ref{sec:intro}) is component separation by joint sampling of $(f,x)$ from $p(f,x\mid y)\propto p(y\mid f,x)\,p(f)\,p(x)$. The natural posterior sampler in this setting is a guided ODE that integrates the prior velocity field plus a likelihood-gradient correction $\nabla_{(f,x)}\log p(y\mid f,x)$ at each step \cite{chung2023dps,saharia2022sr3}. A deterministic flow gives a smooth, fully differentiable map from base noise to sample, so the likelihood-gradient guidance never collides with extra stochastic noise injected by the sampler itself.
  \item \emph{Lower-variance training and sampling.} For a Gaussian conditional path with a smooth schedule, the conditional velocity target $u_t(\xvec\mid\zvec)$ has variance bounded by a $t$-independent constant, while the equivalent score target diverges as $\beta_t\to 0$ (see Sec.~\ref{sec:fm_path}). The CFM loss therefore avoids the per-time-bin reweighting that DDPM-style score matching needs near the data end of the path \cite{ho2020ddpm,karras2022elucidating}. Empirically, this manifests as smaller training-loss variance and more stable validation diagnostics (Sec.~\ref{sec:fm_train}).
  \item \emph{Half the network evaluations at sampling time.} Because the flow is deterministic, second-order ODE solvers such as Heun (Sec.~\ref{sec:fm_sample_pipeline}) achieve $\mathcal O(h^3)$ local error with two velocity-field evaluations per step. The corresponding stochastic SDE solvers can only reach $\mathcal O(h^2)$ at the same compute budget without specialised splitting schemes.
\end{enumerate}
The price for the deterministic choice is that the sampler is a one-to-one map: distinct base draws $\xvec_0\sim\pinit$ are mapped to distinct samples, and there is no in-sampler stochasticity that could mask a residual training imperfection by adding noise. We compensate by using a sufficiently fine integration grid and by validating with the spectral diagnostics of Sec.~\ref{sec:fm_diagnostics}.

\subsection{Probability path: Gaussian interpolant with linear schedule}
\label{sec:fm_path}

We instantiate the conditional path of Definition~\ref{def:cond_path} with the family of \emph{Gaussian conditional paths} parametrised by two real-valued schedule functions $\alpha,\beta\in C^1([0,1])$:
\begin{equation}
  \boxed{\;p_t(\xvec\mid\zvec) \;=\; \mathcal N\!\bigl(\xvec\,;\,\alpha_t\zvec,\;\beta_t^{\,2}\,I_D\bigr),\;}
  \qquad
  \alpha_t,\beta_t\geq 0,
  \label{eq:gauss_cond_path}
\end{equation}
with boundary conditions $\alpha_0=0,\;\alpha_1=1$ and $\beta_0=1,\;\beta_1=0$ that enforce $p_0(\cdot\mid\zvec)=\pinit=\mathcal N(0,I_D)$ and $p_1(\cdot\mid\zvec)=\delta(\cdot-\zvec)$. The marginal of Eq.~(\ref{eq:gauss_cond_path}) under $\zvec\sim\pdata$ is the noisy data distribution $p_t(\xvec)=\int p_t(\xvec\mid\zvec)\pdata(\zvec)\dd\zvec$, which interpolates between the standard Gaussian at $t=0$ and $\pdata$ at $t=1$.

\subsubsection{Conditional velocity field and noise reparametrisation}

A draw from $p_t(\cdot\mid\zvec)$ is obtained by the reparametrisation
\begin{equation}
  \xvec_t = \alpha_t\,\zvec + \beta_t\,\e, \qquad \e\sim\mathcal N(0,I_D),
  \label{eq:noising}
\end{equation}
which directly yields $\xvec_t\sim\mathcal N(\alpha_t\zvec,\beta_t^2 I_D)$. Differentiating Eq.~(\ref{eq:noising}) in $t$ along the deterministic interpolant (i.e.\ holding $\zvec$ and $\e$ fixed) gives the canonical \emph{conditional velocity field} for this family,
\begin{equation}
  u_t(\xvec_t\mid\zvec) = \dot\alpha_t\,\zvec + \dot\beta_t\,\e,
  \label{eq:cond_vf_noise}
\end{equation}
which we may equivalently rewrite in $\xvec$-only form by eliminating $\e=(\xvec_t-\alpha_t\zvec)/\beta_t$:
\begin{equation}
  u_t(\xvec\mid\zvec)
  \;=\; \Bigl(\dot\alpha_t - \tfrac{\dot\beta_t}{\beta_t}\,\alpha_t\Bigr)\zvec + \tfrac{\dot\beta_t}{\beta_t}\,\xvec.
  \label{eq:cond_vf}
\end{equation}
The conditional score and the conditional posterior mean (\emph{a.k.a.}\ Tweedie's formula) are equally explicit:
\begin{equation}
  s_t(\xvec\mid\zvec) = \nabla_{\xvec}\log p_t(\xvec\mid\zvec) = -\frac{\xvec-\alpha_t\zvec}{\beta_t^{\,2}},
  \qquad
  \EE\bigl[\zvec\mid\xvec_t\!=\!\xvec\bigr]
  = \tfrac{\xvec-\beta_t\,\EE[\e\mid\xvec_t\!=\!\xvec]}{\alpha_t}.
  \label{eq:cond_score}
\end{equation}
\emph{Crucially}, all three quantities -- $u_t(\xvec\mid\zvec)$, $s_t(\xvec\mid\zvec)$, and the linear maps that recover $\zvec$ from $(\xvec,\e)$ or from $(\xvec,u)$ -- are closed-form once $(\alpha_t,\beta_t,\dot\alpha_t,\dot\beta_t)$ are known. Conditional flow matching can therefore be implemented with no extra simulation cost beyond a single sample of $\zvec$ and $\e$ per training step.

A direct consequence of Theorem~\ref{thm:marg_vf} and Eq.~(\ref{eq:cond_vf_noise}) is the following.

\begin{proposition}[Marginal velocity for Gaussian paths]
\label{prop:marg_vf_gauss}
For the Gaussian conditional path of Eq.~(\ref{eq:gauss_cond_path}), the marginal velocity field admits the equivalent representations
\begin{equation}
  u_t(\xvec) \;=\; \EE\bigl[\dot\alpha_t\,\zvec + \dot\beta_t\,\e \,\big|\, \xvec_t=\xvec\bigr]
            \;=\; \Bigl(\dot\alpha_t - \tfrac{\dot\beta_t}{\beta_t}\alpha_t\Bigr)\,\EE[\zvec\mid\xvec_t=\xvec] + \tfrac{\dot\beta_t}{\beta_t}\,\xvec,
\end{equation}
i.e.\ it is an affine combination of the data-posterior mean $\EE[\zvec\mid\xvec_t]$ and $\xvec_t$, with coefficients fixed by the schedule.
\end{proposition}

This proposition makes it transparent that learning the velocity field is, up to a known affine map, the same as learning the data-posterior mean: a network that achieves $\uvec_t^{\thetavec}(\xvec)\approx u_t(\xvec)$ implicitly performs MMSE denoising of $\xvec_t$ at every $t$, which is the standard interpretation of denoising score matching \cite{vincent2011dsm,karras2022elucidating}.

\subsubsection{Linear schedule}

We adopt the \emph{linear schedule}
\begin{equation}
  \boxed{\;\alpha_t = t,\qquad \beta_t = 1-t,\qquad \dot\alpha_t=1,\qquad \dot\beta_t=-1,\;}
  \label{eq:linear_schedule}
\end{equation}
which is the canonical rectified-flow / linear-interpolant choice of \cite{liu2022rectified,albergo2023interpolants,lipman2023flowmatching}. Substituting into Eq.~(\ref{eq:cond_vf_noise}) yields the strikingly simple form of the conditional velocity target,
\begin{equation}
  u_t(\xvec_t\mid\zvec) \;=\; \zvec - \e,
  \qquad
  \xvec_t = t\,\zvec + (1-t)\,\e,
  \label{eq:linear_target}
\end{equation}
i.e.\ a \emph{$t$-independent} vector pointing from the noise sample $\e$ to the data sample $\zvec$. Geometrically, the conditional flow associated with the linear schedule is the segment $t\mapsto t\zvec+(1-t)\e$, traversed at constant velocity $\zvec-\e$. The marginal flow that the network learns is the optimal-transport-like quantity that interpolates the two distributions in expectation \cite{liu2022rectified}.

The linear schedule has three properties that make it the natural default for direct flow matching on standardised data.
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{$t$-independent regression target.} The right-hand side of Eq.~(\ref{eq:linear_target}) does not depend on $t$ explicitly (the $t$-dependence enters only through $\xvec_t$). The CFM loss therefore has homogeneous per-time-bin variance and uniform sampling $t\sim\mathcal U[0,1]$ is unbiased with respect to all multipoles of the patch power spectrum -- no per-$t$ reweighting is required, which simplifies the optimisation.
  \item \emph{Bounded variance throughout the path.} The marginal variance is $\Var(\xvec_t\mid\zvec)=\beta_t^2=(1-t)^2$ at any $t$, with a controlled departure from the variance-preserving condition: $\alpha_t^2+\beta_t^2=1-2t(1-t)\in[\tfrac12,1]$. After the per-channel standardisation of Sec.~\ref{sec:data_savedpriors} the data has unit variance per channel, so $\Var(\xvec_t)\leq 1$ at every $t$ and the network never sees activations beyond the dynamic range it was initialised for.
  \item \emph{Endpoint singularity is harmless.} The factor $1/\beta_t$ in the conditional score and in the alternative form of $u_t$ in Eq.~(\ref{eq:cond_vf}) is finite for any $t<1$, and at sampling time we integrate strictly inside the open interval $(0,1)$ and analytically close the residual gap (Sec.~\ref{sec:fm_sample_pipeline}).
\end{enumerate}
A trigonometric schedule $\alpha_t=\sin(\pi t/2)$, $\beta_t=\cos(\pi t/2)$ -- which is exactly variance-preserving, $\alpha_t^2+\beta_t^2=1$, and aligns with the canonical variance-preserving DDPM family of \cite{ho2020ddpm,song2021sde} -- and a bounded variant that bounds $\beta_t,\alpha_t\geq 0.02$ are also implemented as alternative regularisers; we have verified that they reach the same spectral validation score as the linear schedule in the present setting and report only the linear-schedule production runs below.

\subsubsection{Implications for training and sampling}

Equations~(\ref{eq:cond_vf})--(\ref{eq:cond_score}) collectively imply that, for the Gaussian conditional path with the linear schedule, the training pipeline reduces to a single line of code -- sample $(\zvec,\e,t)$, build $\xvec_t=t\zvec+(1-t)\e$, regress $\uvec_t^{\thetavec}(\xvec_t)$ towards $\zvec-\e$ -- and the sampling pipeline reduces to integrating $\dd\xvec_t/\dd t = \uvec_t^{\thetavec}(\xvec_t)$ from $\xvec_0\sim\pinit$. Both operations are deterministic given the random draws made at training time; in particular, sampling has \emph{no} extra randomness beyond the initial $\xvec_0$, which is the formal expression of the deterministic-flow choice of Sec.~\ref{sec:fm_formalism}. The next subsections detail the parametric form of $\uvec_t^{\thetavec}$, the precise loss function used to train it, and the numerical integration of the resulting ODE.

\subsection{Network architecture}
\label{sec:fm_arch}

The unknown marginal velocity field $u_t:\Ndata\!\times\![0,1]\to\Ndata$ is parametrised by a deep neural network $\uvec_t^{\thetavec}$ with parameters $\thetavec$. The architecture is a translation-equivariant U-Net \cite{ronneberger2015unet,ho2020ddpm}, adapted to single-channel temperature patches by $\mathrm{D}_4$ data augmentation (Sec.~\ref{sec:fm_train}) and by a Karras-style \cite{karras2022elucidating} time-embedding pathway. The model is unconditional in the sense that the only side information presented to the network is the time variable $t\in[0,1]$. Inputs and outputs share the shape $(B,1,64,64)$, where $B$ is the mini-batch size and the lone channel is the standardised $T$ patch of Sec.~\ref{sec:data_savedpriors}. The upstream simulation pipeline still produces full $(T,Q,U)$ maps; the present $p(f)$ run trains and validates on the temperature sub-problem only.

\subsubsection{Why a U-Net for CMB patches}

The choice of a convolutional U-Net is motivated by three structural properties of the patch data.
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{Approximate translation invariance.} On flat patches of angular extent $L\sim 7^\circ$, the gnomonic projection error is $\mathcal O((L/\mathrm{rad})^2)\sim 10^{-2}$ (Sec.~\ref{sec:flatsky}), so the patch statistics inherit the translation invariance of the underlying spherically-isotropic field to better than one percent. Convolutional architectures realise this symmetry exactly.
  \item \emph{Multi-scale structure of the power spectrum.} The patch power spectrum spans approximately four decades between the largest patch mode and the patch Nyquist multipole, with several physical features (acoustic peaks in $TT$, the polarisation peak structure in $EE$, the lensing-induced $BB$ tail) at well-separated angular scales. The pyramidal encoder--decoder topology of a U-Net with skip connections is natively well-suited to producing a velocity field that simultaneously captures all of these scales.
  \item \emph{Rotational/reflection equivariance.} On the small gnomonic patches used here, the scalar temperature field is approximately $\mathrm{D}_4$-equivariant: random rotations and reflections are valid symmetry augmentations with no spin bookkeeping. This is applied at the data-augmentation level (Sec.~\ref{sec:fm_train}) rather than baked into the layers.
\end{enumerate}

\subsubsection{Macro-structure}

The U-Net has a five-stage encoder--bottleneck--decoder layout with channel schedule $C=(32,64,128,256)$ and two residual layers per stage:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item A \emph{stem} that lifts the single-channel input patch to $C_0=32$ feature channels via a single $3\!\times\!3$ convolution.
  \item An \emph{encoder} of three downsampling stages: at stage $\ell$ the feature map is processed by two residual layers at width $C_\ell$ and then halved in spatial extent by a strided $3\!\times\!3$ convolution that simultaneously projects to width $C_{\ell+1}$. The bottleneck feature map is $(B,256,8,8)$.
  \item A \emph{midcoder} of two residual layers at the bottleneck width $C_3=256$, which globally mixes the $64$ bottleneck pixels.
  \item A \emph{decoder} that mirrors the encoder: at each stage the bottleneck output is added to the corresponding encoder skip (additive skips, not concatenations), upsampled by a factor of two by bilinear interpolation, and refined by two residual layers at the next-lower width.
  \item An \emph{output head} that projects the final $C_0$-channel feature map back to a single-channel output, producing the predicted velocity field $\uvec_t^{\thetavec}(\xvec)$.
\end{enumerate}

\subsubsection{Time conditioning}

The time variable enters through a learnable Fourier feature embedding \cite{karras2022elucidating}: a fixed-dimension vector
\begin{equation}
  \mathrm{Embed}(t) = \sqrt{2}\bigl[\sin(2\pi\,t\,\bm{w}),\;\cos(2\pi\,t\,\bm{w})\bigr]\in\mathbb R^{d_t},\qquad d_t=64,
  \label{eq:fourier_emb}
\end{equation}
where the frequencies $\bm{w}\in\mathbb R^{d_t/2}$ are trainable parameters initialised as $\bm w\sim\mathcal N(0,I)$. The embedding is then mapped, by a small two-layer perceptron with one hidden layer, to a per-channel bias $\bm h_\ell(t)\in\mathbb R^{C_\ell}$ at every stage $\ell$, and broadcast over the spatial dimensions before being added to the post-convolution feature map of every residual layer. This is a FiLM-style conditioning \cite{perez2018film} restricted to its additive component. Each residual layer has the form
\begin{equation}
  \xvec\;\mapsto\;\xvec + \mathrm{Block}_2\!\bigl(\mathrm{Block}_1(\xvec) + \bm h_\ell(t)\bigr),
  \qquad
  \mathrm{Block}_i = \mathrm{Conv}_{3\times 3}\circ\mathrm{GroupNorm}\circ\mathrm{SiLU},
\end{equation}
with GroupNorm \cite{wu2018groupnorm} to avoid batch-statistic instabilities and SiLU activations for smooth gradients. We do not use self-attention layers: at $64\!\times\!64$ resolution the receptive field of the bottleneck already covers the full patch, and we verified empirically that bottleneck attention did not measurably improve the spectral diagnostics of Sec.~\ref{sec:fm_diagnostics} while approximately doubling the per-step training cost.

\subsubsection{Parameter count}

With the channel schedule $C=(32,64,128,256)$ and two residual layers per stage, the network has $\sim 3.5$\,MiB of float-32 parameters. The patch resolution is moderate ($64\!\times\!64$), the model is unconditional, and the per-step compute is dominated by the convolutional backbone rather than the time-conditioning MLP, so training easily fits in $\!\lesssim\!4$\,GB of GPU memory at the production batch size $B=250$ used below.

\subsection{Training: pure CFM loss and optimisation}
\label{sec:fm_train}

\subsubsection{Loss function}

The production $p(f)$ run uses \emph{pure} conditional flow matching: the network $\uvec_t^{\thetavec}$ is trained as a direct vector-field predictor by minimising only the CFM mean-squared error of Eq.~(\ref{eq:cfm}). No auxiliary regularisers enter the objective. At each minibatch step we draw a Monte-Carlo sample $(\zvec,\e,t)$ with $\zvec\sim\pdata$ (a clean $T$ patch from the training split), $\e\sim\mathcal N(0,I_D)$, and $t$ sampled on $[t_{\min},t_{\max}]$ as described below; we then form the linear-path state $\xvec_t=\alpha_t\zvec+\beta_t\e$ and regress the predicted velocity against the closed-form conditional target $\dot\alpha_t\zvec+\dot\beta_t\e$. The training objective is
\begin{equation}
  \boxed{\;
  \mathcal L(\thetavec)
  \;=\;
  \mathcal L_{\rm CFM}(\thetavec)
  \;=\;
  \EE_{t,\zvec,\e}\Bigl[\,\bigl\|\uvec_t^{\thetavec}(\xvec_t)
                  - (\dot\alpha_t\zvec+\dot\beta_t\e)\bigr\|^2_2\,\Bigr].
  \;}
  \label{eq:total_loss}
\end{equation}
For the linear schedule of Eq.~(\ref{eq:linear_schedule}) this reduces to the equivalent form
\begin{equation}
  \mathcal L_{\rm CFM}(\thetavec)
  \;=\;
  \EE_{t,\zvec,\e}\Bigl[\,\bigl\|\uvec_t^{\thetavec}(\xvec_t)
                  - (\zvec-\e)\bigr\|^2_2\,\Bigr].
  \label{eq:cfm_linear}
\end{equation}
By Proposition~\ref{prop:fm_equiv} this is the canonical, simulation-free flow-matching objective: minimising it makes $\uvec_t^{\thetavec}(\xvec)$ converge to the marginal velocity $u_t(\xvec)$ of the path, which by Eq.~(\ref{eq:flow_ode}) drives the deterministic ODE integrated at sampling time. The implementation in \texttt{FlowMatchingTrainer.get\_train\_loss} (cell~53) is exactly this single regression step.

\paragraph{Relation to earlier, over-regularised runs.}
Earlier notebook iterations augmented Eq.~(\ref{eq:cfm_linear}) with an endpoint-reconstruction (``$x_0$'') anchor and a log-power radial Fourier penalty on patch spectra. Those terms are no longer used: they were found to overpin low-$\ell$ amplitude well before the CFM optimum was reached, after which the validation spectral score drifted upward even as the training loss kept falling. The present pure-CFM setup instead relies on EMA evaluation plus best-validation-spectral checkpointing (Sec.~\ref{sec:fm_ema_and_best}) to select a snapshot whose generated $D_\ell^{TT}$ matches the held-out reference. For diagnostics and sampling we still map a noisy state $(\xvec_t,t)$ to a clean-patch estimate via the linear posterior-mean projector
\begin{equation}
  \hat\zvec_{\thetavec}(\xvec_t,t)
  \;=\;
  \frac{\dot\beta_t\,\xvec_t - \beta_t\,\uvec_t^{\thetavec}(\xvec_t)}
       {\alpha_t\dot\beta_t - \dot\alpha_t\,\beta_t},
  \label{eq:x0_recon}
\end{equation}
but this map is \emph{not} included in the training loss.

\subsubsection{Symmetry-aware data augmentation}

Temperature is a scalar field on the patch. At each training step we apply a uniformly drawn element of the dihedral group $\mathrm{D}_4$---a random $k\!\times\!90^\circ$ rotation followed by an optional reflection across the vertical axis---to the clean batch $\{\zvec_i\}$ before constructing $\xvec_t$. No spin-2 sign bookkeeping is required. This is exact at the angular scale of our patches (where curvature corrections to spherical isotropy are subdominant; Sec.~\ref{sec:flatsky}) and provides an exact $8\!\times$ multiplication of the effective training-set size at zero memory cost.

\subsubsection{Time-sampling distribution}

Training and sampling share the interval $[t_{\min},t_{\max}]=[0,\,0.995]$. The upper truncation avoids the $1/(1-t)$ singularity in the conditional score and preserves a one-step margin from the boundary that matches the Heun integration grid of Sec.~\ref{sec:fm_sample_pipeline}. Rather than drawing $t$ uniformly, we use the power-law bias implemented in \texttt{\_sample\_train\_t}: draw $u\sim\mathcal U[0,1]$, set
\begin{equation}
  \tau \;=\;
  \begin{cases}
    u & \text{if the bias power is }1,\\[0.3em]
    1-(1-u)^{p} & \text{otherwise,}
  \end{cases}
  \qquad
  t \;=\; t_{\min} + (t_{\max}-t_{\min})\,\tau,
\end{equation}
with production bias power $p=0.4$ (\texttt{train\_t\_bias\_power=0.4}). Values $p<1$ slightly overweight low-$t$ regions of the path; $p=1$ recovers uniform sampling. Because the linear-path velocity target Eq.~(\ref{eq:linear_target}) is $t$-independent, any reasonable sampling density on $[t_{\min},t_{\max}]$ remains unbiased in expectation with respect to all multipoles; the mild $p=0.4$ choice is retained from the production checkpoint rather than being a strong inductive bias.

\subsubsection{Optimiser}

We optimise $\mathcal L(\thetavec)$ with AdamW~\cite{loshchilov2019adamw} at base learning rate $10^{-4}$ and weight decay $10^{-4}$, with the gradient norm clipped to unity before each update. The learning rate is held constant for the first $5{,}000$ epochs and then cosine-annealed~\cite{loshchilov2017sgdr} to $0.1\times$ the base value over the remaining epochs. The mini-batch size is $B=250$ and the production budget is $10{,}000$ epochs (Sec.~\ref{sec:fm_ema_and_best} explains why this budget is paired with validation-spectral checkpointing rather than with early stopping). Training is launched from cell~58 with \texttt{run\_spectral=True}, so the flat-sky $TT$ score of Sec.~\ref{sec:fm_spectral_score} is evaluated in-loop every $500$ epochs.

\subsubsection{Hyperparameter summary}

Table~\ref{tab:fm_hparams} reports the production hyperparameters for the $p(f)$ run documented here.

\begin{table}[h]
  \centering
  \begin{tabular}{ll}
    \toprule
    Hyperparameter & Value \\
    \midrule
    Prior / channels & $p(f)$, single-channel $T$ only \\
    Schedule & Linear: $\alpha_t=t,\,\beta_t=1-t$ \\
    $t$-support & $[t_{\min},t_{\max}]=[0,\,0.995]$ \\
    $t$-sampling bias power $p$ & $0.4$ \\
    Sampling regime & deterministic flow, $\sigma=0$ \\
    Training objective & pure CFM MSE, Eq.~(\ref{eq:total_loss}) \\
    Augmentation & $\mathrm{D}_4$ on $T$ patches \\
    Optimiser & AdamW, weight decay $10^{-4}$ \\
    Base learning rate & $10^{-4}$ \\
    Gradient clip & unit Frobenius norm \\
    Mini-batch size & $250$ \\
    Number of epochs & $10{,}000$ \\
    LR schedule & constant for the first $5{,}000$ epochs, cosine-annealing to $0.1\times$ base afterwards \\
    EMA decay & $\rho=0.99$ \\
    EMA evaluation warmup & $300$ steps \\
    In-training spectral diagnostic & $N_{\rm s}=32$ samples, $N_{\rm steps}=1600$ Heun substeps, every $500$ epochs \\
    Validation-spectral checkpointing & best-spectral EMA snapshot kept as final answer (Sec.~\ref{sec:fm_ema_and_best}) \\
    State clipping at sampling & $|\xvec|\!\leq\!5$ \\
    Output checkpoint & \texttt{checkpoints/unet\_cmb\_t\_pure\_cfm\_linear\_heun\_ema\_10.pt} \\
    \bottomrule
  \end{tabular}
  \caption{Production hyperparameters for the deterministic flow-matching $p(f)$ prior. The foreground prior $p(x)$ reuses the same \texttt{FlowMatchingTrainer} class and most of these settings; its training cell and objective are summarised separately in Sec.~\ref{sec:fg_training_config}.}
  \label{tab:fm_hparams}
\end{table}

\subsection{EMA averaging and validation-driven checkpointing}
\label{sec:fm_ema_and_best}

The bare optimiser trajectory $\{\thetavec_n\}_{n\geq 0}$ produced by AdamW is, at its best, a stochastic walk around the local minimum of the training objective. Two practical issues follow: (i) the per-step weights have a non-negligible amount of optimisation noise that hurts evaluation; (ii) the training loss is only a noisy proxy for the quantity we ultimately care about, namely the agreement of the generated patches with the validation $D_\ell^{TT}$. We address both with a single mechanism: an exponential moving average (EMA) of the weights that is hot-swapped at validation time, plus a validation-time selection rule that keeps the best EMA snapshot across the entire run rather than the last one.

\subsubsection{Exponential moving average of the weights}
\label{sec:fm_ema}

\paragraph{Update rule.}
At each optimiser step $n$ we maintain a shadow copy $\bar\thetavec_n\in\mathbb R^p$ of the model parameters, updated by
\begin{equation}
  \bar\thetavec_{n+1} \;=\; \rho\,\bar\thetavec_n + (1-\rho)\,\thetavec_{n+1},\qquad \rho\in(0,1),
  \label{eq:ema_update}
\end{equation}
with $\rho=0.99$ in our runs (\texttt{ema\_decay=0.99} passed to \texttt{FlowMatchingTrainer} in cell~58). Unrolling \eqref{eq:ema_update} from the initial shadow $\bar\thetavec_0=\thetavec_0$ gives the equivalent geometric-weight representation
\begin{equation}
  \bar\thetavec_n \;=\; \rho^{\,n}\,\thetavec_0 + (1-\rho)\sum_{k=1}^{n}\rho^{\,n-k}\,\thetavec_k,
  \label{eq:ema_unroll}
\end{equation}
i.e.\ a low-pass filter on the optimiser trajectory with effective averaging horizon
\begin{equation}
  \tau_{\rm EMA}\;=\;\frac{1}{1-\rho},
\end{equation}
so for $\rho=0.99$ that is $\tau_{\rm EMA}\approx 100$ steps. Eq.~(\ref{eq:ema_unroll}) is a Polyak-style running average, and for any twice-differentiable loss with bounded Hessian near the optimum this estimator has strictly lower variance than the instantaneous iterate $\thetavec_n$ at the same training token count.

\paragraph{Implementation.}
The EMA shadow only mirrors floating-point parameters of the model. Integer buffers (e.g.\ batch counters, batch-norm running stats with integer types) are excluded from \eqref{eq:ema_update} so that the multiplication by $\rho$ is well-defined. Concretely, the \texttt{ModelEMA} helper in cell 45 of the notebook stores
\begin{equation}
  \mathrm{shadow}[k] \;=\; \rho\,\mathrm{shadow}[k] \;+\; (1-\rho)\,\theta[k] \quad\text{for every floating-point key }k,
  \label{eq:ema_inplace}
\end{equation}
which is the in-place form of \eqref{eq:ema_update}. The \texttt{update} method is called once per optimiser step from inside the trainer's training loop (cell 53). The same helper exposes the methods \texttt{store}, \texttt{copy\_to}, \texttt{restore}: this triplet implements the hot-swap evaluation pattern described next.

\paragraph{Hot-swap evaluation.}
At each diagnostic step we evaluate the EMA weights, not the live weights, by the canonical store/copy/restore protocol:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \texttt{store}: snapshot the live floating-point parameters $\thetavec_n$ into a backup buffer.
  \item \texttt{copy\_to}: overwrite the live parameters with the EMA shadow $\bar\thetavec_n$.
  \item Run the validation-time forward passes (vector-field MSE, spectral sampling, etc.).
  \item \texttt{restore}: write the backup buffer back into the live parameters so that the next optimiser step continues on the original trajectory.
\end{enumerate}
This block is implemented at lines $3260$--$3277$ of the notebook (see the in-loop guard \texttt{idx >= self.ema\_eval\_warmup\_steps}). The $300$-step EMA warmup avoids using a poorly-conditioned shadow early in training, where the running average is still biased towards the random initialisation. After the warmup, the EMA copy is systematically smoother than the live weights at the same compute, and substantially reduces the variance of the spectral diagnostic of the next subsection, in line with the empirical findings of~\cite{karras2022elucidating,salimans2022progressive}.

\subsubsection{The spectral selection score}
\label{sec:fm_spectral_score}

The training-loss proxy $\mathcal L_{\rm CFM}$ is locally minimised by the optimal marginal velocity $u_t^{\star}$, but locally the noise floor of the regression is set by the irreducible Gaussian-conditional MMSE
$(\dot\alpha_t\beta_t-\dot\beta_t\alpha_t)^2/(\alpha_t^2+\beta_t^2)$, which carries no preference for any particular $D_\ell^{TT}$ shape. Two distinct snapshots can therefore have indistinguishable $\mathcal L_{\rm CFM}$ but visibly different generated $D_\ell^{TT}$ curves. We therefore score each EMA snapshot by an end-to-end test that mirrors the inference pipeline as closely as possible.

Let $\bar\thetavec$ be the EMA snapshot to be scored. We sample $N_{\rm s}$ patches by integrating the deterministic ODE
\begin{equation}
  \xvec_0\sim\mathcal N(0,I_{1\times 64\times 64}),\qquad
  \frac{\dd\xvec_t}{\dd t}\;=\;\uvec_t^{\bar\thetavec}(\xvec_t),\qquad t\in[t_{\min},t_{\max}],
  \label{eq:flow_ode_diag}
\end{equation}
with the same Heun substep grid used at inference (Sec.~\ref{sec:fm_sample_pipeline}), reconstruct $\hat\zvec=\hat\xvec_0$ from the endpoint $\xvec_{t_{\max}}$ via the linear posterior-mean projector of Eq.~(\ref{eq:x0_recon}), and apply the same Gaussian pixel-space smoothing kernel $\sigma_{\rm pix}$ that is applied to the validation patches. Denote the resulting smoothed patch ensemble by $\{\hat\zvec_i\}_{i=1}^{N_{\rm s}}$.

\paragraph{Two log-space discrepancies.}
We then form two scalar discrepancies between the generated ensemble and the held-out validation ensemble $\{\zvec_j\}_{j=1}^{N_{\rm ref}}$:

\begin{description}[leftmargin=1.6em,itemsep=0.2em]
\item[(a) Bandpower-shape discrepancy.]
Using the flat-sky bandpower estimator $\hat P_b$ of Sec.~\ref{sec:fm_diag_estimator}, compute the binned $D_\ell^{TT}$ of the generated and reference ensembles, $\hat D_b^{\rm gen}$ and $\hat D_b^{\rm ref}$, on the band $\ell\leq\ell_{\max}=1200$. Define
\begin{equation}
  \boxed{\;\mathcal S_{\rm shape}(\bar\thetavec)
  \;=\;
  \sqrt{\frac{1}{|\mathcal B|}\sum_{b\in\mathcal B}\bigl[\log\hat D_b^{\rm gen}(\bar\thetavec)
                                                       -\log\hat D_b^{\rm ref}\bigr]^2},\;}
  \label{eq:S_shape}
\end{equation}
the RMS log-relative bandpower error, where $\mathcal B$ collects the bandpowers with strictly positive $\hat D_b$ on both sides. This is the term \texttt{spectral\_shape\_log\_rmse} computed inside \texttt{run\_spectral\_diagnostics} (cell 53). Working in log-space matches the dynamic range of the patch power spectrum (about four decades from large-scale modes to the patch Nyquist) and means a $10\%$ relative deficit at any single $\ell$ contributes the same amount whether it sits at low or high $\ell$.

\item[(b) Pixel-variance discrepancy.]
Let $\sigma_c^{\rm gen},\sigma_c^{\rm ref}$ denote the per-channel pixel-level standard deviations on the generated and reference ensembles ($c=1$ for the temperature-only run; $c=1,2,3$ in the full $TQU$ extension). Define
\begin{equation}
  \boxed{\;\mathcal S_{\rm var}(\bar\thetavec)
  \;=\;
  \sqrt{\frac{1}{C}\sum_{c=1}^{C}\bigl[\log\sigma_c^{\rm gen}(\bar\thetavec)
                                       -\log\sigma_c^{\rm ref}\bigr]^2}.\;}
  \label{eq:S_var}
\end{equation}
This is the term \texttt{spectral\_std\_log\_l2} of cell 53. It pins the integrated patch-level variance, which is essentially $\sum_b D_b/(2\pi)$ but evaluated directly in pixel space and therefore robust to small $\ell$-binning artefacts.
\end{description}

\paragraph{The combined score.}
The scalar used for checkpoint selection is the sum
\begin{equation}
  \boxed{\;\mathcal S(\bar\thetavec)\;=\;\mathcal S_{\rm shape}(\bar\thetavec)\;+\;\mathcal S_{\rm var}(\bar\thetavec).\;}
  \label{eq:S_total}
\end{equation}
Both summands are L2 norms in log-space and therefore unitless and roughly comparable in magnitude after $D_\ell^{TT}$ is restricted to $\ell\leq\ell_{\max}$. \eqref{eq:S_total} is computed inside \texttt{run\_spectral\_diagnostics} as
\begin{verbatim}
spectral_selection_score = (
    spectral_shape_log_rmse + spectral_std_log_l2
)
\end{verbatim}
A monotone decrease of $\mathcal S$ during training is what we want to see; in the over-regularised run that motivated the present pure-CFM ablation, $\mathcal S$ reached a global minimum near epoch $\sim 1000$ and then drifted back up monotonically to $\sim 2.5\times$ that value, as the auxiliary log-power loss continued to push gradient into low-$\ell$ once the CFM term had already saturated.

\paragraph{Sampling configuration.}
For the production $p(f)$ run we use $N_{\rm s}=32$ patches and $N_{\rm steps}=1600$ Heun substeps in the spectral diagnostic (the constructor arguments \texttt{spectral\_n\_samples=32, spectral\_n\_steps=1600} of cell~58). The smoothing scale $\sigma_{\rm pix}$ matches the kernel applied at the pre-comparison step in \texttt{run\_spectral\_diagnostics} (cell~53); in the $TT$ pipeline this is the same $\sigma_{\rm pix}=1.2$ pixels we use for the post-training $D_\ell^{TT}$ figure. The reference ensemble is the held-out validation split of the saved $p(f)$ patches.

\subsubsection{Best-validation-spectral checkpointing}
\label{sec:fm_best_spectral_protocol}

\paragraph{Selection rule.}
A scalar \texttt{best\_spectral\_score} is initialised to $\texttt{None}$ at the start of training. At every diagnostic step where the spectral score $\mathcal S$ is finite we update
\begin{equation}
  \mathcal S^\star \;\leftarrow\; \min\!\bigl(\mathcal S^\star,\,\mathcal S(\bar\thetavec_n)\bigr),
  \label{eq:running_min}
\end{equation}
and whenever a new minimum is achieved we additionally snapshot the live and the EMA weights:
\begin{equation}
  \texttt{best\_spectral\_model\_state\_dict}\;\leftarrow\;\thetavec_n,\qquad
  \texttt{best\_spectral\_ema\_state\_dict}\;\leftarrow\;\bar\thetavec_n,
\end{equation}
together with the epoch index \texttt{best\_spectral\_epoch}. This logic lives at lines $4116$--$4128$ of cell 53.

\paragraph{What the saved checkpoint contains.}
At the end of training, cell 58 writes a single file with both the last-epoch weights and the best-spectral snapshot:
\begin{equation}
  \mathcal C \;=\; \bigl\{\thetavec_{\rm last},\,\bar\thetavec_{\rm last},\,
                                  \thetavec_n^\star,\,\bar\thetavec_n^\star,\,
                                  \mathcal S^\star,\,n^\star,\,
                                  \text{schedule \& sampler config},\,
                                  \text{training history}\bigr\},
\end{equation}
saved as \texttt{checkpoints/unet\_cmb\_t\_pure\_cfm\_linear\_heun\_ema\_10.pt}. In the production run documented here the best score $\mathcal S^\star\approx 0.042$ is reached at epoch $n^\star=7500$. The downstream evaluation cell prefers $\bar\thetavec^\star$ over $\bar\thetavec_{\rm last}$ whenever it is present (cell~59 uses the key \texttt{best\_spectral\_ema\_state\_dict} when available; cell~53 implements the same fallback in the post-training sampling block). The ``final answer'' of the run is therefore the EMA snapshot at the epoch at which $\mathcal S$ was lowest, not the last EMA snapshot.

\paragraph{Why the best-along-the-way and not early stopping.}
The two protocols are operationally similar but conceptually different. Early stopping halts training when $\mathcal S$ stops improving for a patience window and discards everything afterwards; the best-along-the-way protocol used here continues training to the full $10{,}000$-epoch budget and only \emph{retains} the best snapshot. The two coincide when $\mathcal S$ is monotonically non-increasing and diverge whenever it is not. With a generic non-convex loss surface and a finite-batch estimator of $\mathcal S$, the latter is essentially never the case, and continuing to train past a local plateau lets the optimiser explore a different basin while keeping the previous best in cold storage. For our problem this matters: the best-spectral epoch is typically reached well before the AdamW-with-cosine-anneal trajectory has fully relaxed, and the loss landscape continues to evolve. The cost of the extra epochs is small (the bulk of the per-step cost is already spent on the U-Net forward pass) and the upside is non-trivial robustness against late-stage optimisation drift.

\paragraph{Why $\mathcal S$ rather than the validation $\mathcal L_{\rm CFM}$.}
The validation $\mathcal L_{\rm CFM}$ is dominated by mid-$t$ contributions where $\langle\|u_t\|^2\rangle$ is largest, and even an optimal model retains the irreducible MMSE there. Two snapshots with identical validation $\mathcal L_{\rm CFM}$ can therefore have visibly different $\hat D_\ell^{TT}$. The spectral score $\mathcal S$ is computed end-to-end through the same solver and same flat-sky bandpower estimator as the post-training $D_\ell^{TT}$ figure, so it is monotonically related to the user-facing deliverable. Selecting on $\mathcal S$ is therefore the cheapest way to ensure that the saved checkpoint matches the reference ensemble in the same metric space in which the result is ultimately reported.

\subsubsection{End-to-end protocol summary}

Concretely, at every diagnostic step $n$ where the training-time spectral diagnostic fires:
\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{EMA hot-swap.} \texttt{store} the live $\thetavec_n$, then \texttt{copy\_to} the shadow $\bar\thetavec_n$ (\eqref{eq:ema_update}).
  \item \emph{Reverse sampling.} Draw $N_{\rm s}=32$ initial states $\xvec_0\sim\mathcal N(0,I)$ and integrate \eqref{eq:flow_ode_diag} from $t_{\min}=0$ to $t_{\max}=0.995$ with $N_{\rm steps}=1600$ Heun substeps and state clip $|\xvec_t|\leq 5$.
  \item \emph{Endpoint denoising.} Reconstruct $\hat\xvec_0$ from $\xvec_{t_{\max}}$ using Eq.~(\ref{eq:x0_recon}); the simulator returns $\xvec_{t_{\max}}$ rather than clean data.
  \item \emph{Spectral comparison.} Smooth both generated and reference patches with $\sigma_{\rm pix}$, compute $\hat D_b^{\rm gen}$ and $\hat D_b^{\rm ref}$, and form $\mathcal S = \mathcal S_{\rm shape} + \mathcal S_{\rm var}$ via \eqref{eq:S_shape}--\eqref{eq:S_total}.
  \item \emph{Best-snapshot update.} If $\mathcal S<\mathcal S^\star$, update $\mathcal S^\star\leftarrow\mathcal S$ and snapshot $(\thetavec_n^\star,\bar\thetavec_n^\star,n^\star)$ via \eqref{eq:running_min}.
  \item \emph{EMA hot-restore.} \texttt{restore} the backup so the next optimiser step continues on the live trajectory.
\end{enumerate}
At the end of the $10{,}000$-epoch run the saved checkpoint $\mathcal C$ contains both the last-epoch and the best-spectral snapshots, and the downstream sampler defaults to $\bar\thetavec^\star$.

\subsection{Sampling: deterministic ODE integration}
\label{sec:fm_sample_pipeline}

Once $\thetavec$ is trained, drawing samples from the learned prior reduces to numerically integrating the deterministic ODE
\begin{equation}
  \xvec_0\sim\pinit=\mathcal N(0,I_D),
  \qquad
  \frac{\dd\xvec_t}{\dd t} = \uvec_t^{\thetavec}(\xvec_t),
  \qquad t\in[0,t_{\max}],
  \label{eq:sampling_ode}
\end{equation}
with $t_{\max}=0.995$ as in training. The endpoint $\xvec_{t_{\max}}$ is then mapped to a clean-patch estimate $\hat\zvec$ by the linear posterior-mean projector of Eq.~(\ref{eq:x0_recon}). Because the deterministic flow has no in-sampler stochasticity, the entire procedure is a smooth, fully differentiable map from the base draw $\xvec_0\sim\pinit$ to the final patch -- a property that we will rely on for the downstream guided-posterior sampler of the component-separation pipeline.

\subsubsection{Numerical integrator: Heun's second-order method}

We use Heun's second-order ODE method on a uniform grid of $N_{\rm steps}=1200$ time steps. For step size $h$ and current state $(\xvec_t,t)$, Heun's method advances to $t+h$ via
\begin{align}
  \xvec_t^{\,\prime} &= \xvec_t + h\,\uvec_t^{\thetavec}(\xvec_t)
    & \blacktriangleright\ \text{predictor (Euler step)} \\
  \xvec_{t+h} &= \xvec_t + \tfrac{h}{2}\,\bigl[\uvec_t^{\thetavec}(\xvec_t)+\uvec_{t+h}^{\thetavec}(\xvec_t^{\,\prime})\bigr]
    & \blacktriangleright\ \text{corrector (trapezoidal)}.
\end{align}
This achieves $\mathcal{O}(h^3)$ local truncation error against the $\mathcal{O}(h^2)$ of the plain Euler method, at the cost of two velocity-field evaluations per step. The choice over Euler is motivated by the spectral diagnostics of Sec.~\ref{sec:fm_diagnostics}: a first-order Euler integrator at the same compute budget produces a small but visible $\mathcal{O}(1\%)$ amplitude bias on the patch power spectrum at intermediate $\ell$, consistent with the standard analysis of \cite{karras2022elucidating}. With Heun the residual integrator bias is comparable to the cosmic-variance scatter of our finite ensemble of generated patches, and is therefore invisible in the validation diagnostic.

\subsubsection{Terminal-state reconstruction}

The integration is stopped one step short of the boundary, at $t_{\max}=0.995$, to avoid the $1/(1-t)$ singularity that the conditional vector field of the linear path acquires at $t=1$. The residual $1-t_{\max}=0.005$ is closed analytically by the linear posterior-mean estimator
\begin{equation}
  \hat\zvec
  \;=\;
  \frac{\dot\beta_{t_{\max}}\,\xvec_{t_{\max}} - \beta_{t_{\max}}\,\uvec_{t_{\max}}^{\thetavec}(\xvec_{t_{\max}})}
       {\alpha_{t_{\max}}\,\dot\beta_{t_{\max}} - \dot\alpha_{t_{\max}}\,\beta_{t_{\max}}}
  \;\overset{\text{linear}}{=}\;
  \xvec_{t_{\max}} + (1-t_{\max})\,\uvec_{t_{\max}}^{\thetavec}(\xvec_{t_{\max}}),
\end{equation}
which is exact to first order in the residual. This is preferable to integrating the ODE all the way to $t=1$, because it avoids polluting the final state with the $\mathcal{O}((1-t)^{-1})$ amplification of any small per-step bias near the data end of the path.

\subsubsection{Stability safeguards}

Two minor numerical safeguards complete the sampler. (i) After every Heun step the state is element-wise clipped to $|\xvec_t|\leq 5$ in standardised units. The fraction of clipped components in production runs is below $10^{-3}$, so the clip is a soft safety net against rare, unphysical excursions of the ODE rather than an active constraint. (ii) The base draw $\xvec_0\sim\pinit$ uses a fixed seed so that the entire sampler is bit-reproducible given the trained checkpoint.

\subsubsection{Production sampling configuration}

For the figures shown below, we draw $N_{\rm gen}=64$ samples with $N_{\rm steps}=1200$, $t\in[0,0.995]$, $\sigma=0$ (deterministic flow), $|\xvec|\leq 5$ clipping, and the best-spectral checkpoint of Sec.~\ref{sec:fm_train}.

\subsection{Diagnostics: validating the learned prior}
\label{sec:fm_diagnostics}

The natural figure of merit for a learned CMB prior is the agreement of the second moments of the patch ensemble between the generated samples and an independent reference ensemble drawn from the same underlying physics. The notebook is currently restricted to the temperature/intensity sub-problem, so the diagnostic discussion below specialises to the $TT$ channel; the polarisation $EE/BB/TE$ counterparts are recovered in the same form once the polarisation pipeline is reintroduced. The same logic applies to the foreground prior $p(x)$, evaluated on its own held-out validation split.

\subsubsection{Reference ensembles}

We compare the generated samples against two independent reference ensembles, so that systematic biases of the trained network can be separated from systematic biases of the patch / flat-sky estimator pipeline.

\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{Validation patches.} The held-out validation split of the saved prior dataset (Sec.~\ref{sec:data_savedpriors}). These patches share the entire upstream pipeline -- CAMB spectra (with curved-sky lensing for $p(f)$), HEALPix pixelisation, beam, scalar pixel window, gnomonic projection, mask, per-channel standardisation -- with the training set, but are drawn from full-sky realisations that are statistically independent from the training realisations because of the by-map split. They are therefore a faithful empirical realisation of the target distribution $\pdata$ that the network is asked to learn.
  \item \emph{Monte Carlo theory band.} An independent batch of clean-patch realisations is constructed from a freshly drawn ensemble of full-sky Gaussian CMB realisations, generated from the lensed CAMB $TT$ spectrum at the same beam, pixel window, and $N_{\rm side}$ as the training set, and processed through the identical patch-extraction and flat-sky estimator pipeline. This MC ensemble does \emph{not} pass through any neural network: the only difference between it and the validation ensemble is the absence of the lenspyx remap, which is irrelevant for $TT$ at the percent level on $\ell\!<\!1200$.
\end{enumerate}
A feature of the generated curve that is also present in the MC theory band must be an artefact of the patch / flat-sky estimator pipeline itself; conversely, a feature that is present only in the generated band is a genuine bias of the trained prior.
\subsubsection{\texorpdfstring{Flat-sky $TT$ bandpower estimator}{Flat-sky TT bandpower estimator}}
\label{sec:fm_diag_estimator}

For a flat-sky patch of side $L=N\Delta x$ with pixel resolution $\Delta x$, write its discrete Fourier transform
\begin{equation}
  \widetilde T(\bm k)
  \;=\;\sum_{\bm x} T(\bm x)\,\exp(-\mathrm i\bm k\!\cdot\!\bm x)\,(\Delta x)^2,\qquad
  \bm k\in\Bigl\{\tfrac{2\pi}{L}(n_x,n_y):n_{x,y}=-\tfrac{N}{2},\ldots,\tfrac{N}{2}-1\Bigr\}.
  \label{eq:patch_fft}
\end{equation}
The flat-sky multipole assigned to each Fourier pixel is
\begin{equation}
  \ell(\bm k)\;=\;2\pi\,|\bm k|,
\end{equation}
which is the leading-order curved-to-flat-sky correspondence valid on the patch sizes we use (Sec.~\ref{sec:flatsky}). Given a batch $\{T_i\}_{i=1}^B$ of patches and a partition of the multipole axis into bandpowers $\{\mathcal B_b\}$, the isotropic two-point estimator at bin $b$ is
\begin{equation}
  \hat P_b
  \;=\;\frac{(\Delta x)^2}{N^2}\,
  \frac{1}{N^{(b)}_{\rm modes}}
  \sum_{\bm k:\ell(\bm k)\in\mathcal B_b}
  \frac{1}{B}\sum_{i=1}^B
  \bigl|\widetilde T_i(\bm k)\bigr|^2,
  \label{eq:flatsky_tt_estimator}
\end{equation}
with $N^{(b)}_{\rm modes}=|\{\bm k:\ell(\bm k)\in\mathcal B_b\}|$ the number of Fourier pixels in bandpower $b$. The factor $(\Delta x)^2/N^2$ is the standard discrete-Fourier normalisation that makes $\hat P_b$ a consistent estimator of the continuum patch power $C_\ell^{TT}$ on the central band of the Fourier grid \cite{hivon2002master}. We additionally plot the CMB-style power
\begin{equation}
  \hat D_b
  \;=\;\frac{\ell_b(\ell_b+1)}{2\pi}\,\hat P_b,\qquad
  \ell_b\equiv\text{bin centre},
  \label{eq:patch_dell}
\end{equation}
which is the conventional axis on which the $TT$ acoustic peaks are most easily read off.

The number of Fourier pixels falling in an annulus $\mathcal B_b=[\ell_b^L,\ell_b^R)$ is
\begin{equation}
  N^{(b)}_{\rm modes}
  \;=\;\frac{\pi\bigl((\ell_b^R)^2-(\ell_b^L)^2\bigr)}{(\Delta k)^2}
  \;=\;\frac{\bigl((\ell_b^R)^2-(\ell_b^L)^2\bigr)\,L^2}{4\pi},
  \label{eq:n_modes_per_bin}
\end{equation}
where $\Delta k=2\pi/L$ is the Fourier-grid spacing. We use Eq.~(\ref{eq:n_modes_per_bin}) heavily below; it controls the per-bin cosmic variance and the relative information content of different binning conventions.
\subsubsection{Bandpower binning conventions used in the notebook}
\label{sec:fm_diag_binning}

Two bandpower partitions of the multipole axis are used in parallel because they have very different statistical properties. Both estimators take the same input batch of patches and the same Eq.~(\ref{eq:flatsky_tt_estimator}); they differ only in the choice of bin edges $\{\ell_b^L,\ell_b^R\}$ and bin centres $\ell_b$.

\paragraph{Linear bandpowers (\texttt{flat\_sky\_tt\_spectrum}).} The default linear estimator places $N_{\rm bin}=\min(N/2,30)$ bins of equal width on $[0,\ell_{\max}]$, with $\ell_{\max}=\max_{\bm k}\ell(\bm k)$ the maximum Fourier multipole on the diagonal of the patch grid. Bin centres are arithmetic midpoints,
\begin{equation}
  \ell_b^L=b\Delta\ell,\qquad
  \ell_b^R=(b{+}1)\Delta\ell,\qquad
  \ell_b=\tfrac12(\ell_b^L+\ell_b^R),\qquad
  \Delta\ell=\frac{\ell_{\max}}{N_{\rm bin}},
  \label{eq:linear_binning}
\end{equation}
for $b=0,\ldots,N_{\rm bin}{-}1$, with the right edge of the last bin closed. Inserting Eq.~(\ref{eq:linear_binning}) into Eq.~(\ref{eq:n_modes_per_bin}) gives, at fixed $\Delta\ell$,
\begin{equation}
  N^{(b),{\rm lin}}_{\rm modes}\;=\;\frac{(2\ell_b)\,\Delta\ell\,L^2}{4\pi}
  \;\propto\;\ell_b\,\Delta\ell,
\end{equation}
so the mode count per bin grows linearly with the bin centre. In particular, the lowest bin contains few modes (only those Fourier pixels at $\ell\!\leq\!\Delta\ell$, including the patch fundamental at $\ell_{\rm fund}=2\pi/L$), while the highest bin aggregates many.

\paragraph{Logarithmic bandpowers (\texttt{flat\_sky\_tt\_spectrum\_binned}).} The log-binned estimator places $N_{\rm bin}=48$ bins of equal logarithmic width on $[\ell_{\min},\ell_{\max}]$, with $\ell_{\min}=\min_{\bm k:\bm k\neq 0}\ell(\bm k)=\ell_{\rm fund}$ and bin centres set to the geometric midpoint,
\begin{equation}
  \ell_b^L=\ell_{\min}\,e^{b\,\Delta\ln\ell},\qquad
  \ell_b^R=\ell_{\min}\,e^{(b+1)\,\Delta\ln\ell},\qquad
  \ell_b=\sqrt{\ell_b^L\,\ell_b^R},\qquad
  \Delta\ln\ell=\frac{\ln\ell_{\max}-\ln\ell_{\min}}{N_{\rm bin}}.
  \label{eq:log_binning}
\end{equation}
For each bin, $\ell_b^R-\ell_b^L=\ell_b\,(e^{\Delta\ln\ell/2}-e^{-\Delta\ln\ell/2})\approx \ell_b\,\Delta\ln\ell$, and so Eq.~(\ref{eq:n_modes_per_bin}) gives
\begin{equation}
  N^{(b),{\rm log}}_{\rm modes}\;\approx\;\frac{\ell_b^2\,\Delta\ln\ell\,L^2}{2\pi}
  \;\propto\;\ell_b^2,
\end{equation}
so the mode count per bin grows quadratically with the bin centre. Empty bins (those for which $N^{(b)}_{\rm modes}=0$ on the discrete grid) are dropped from the output. The log estimator is the convention adopted in most generative-model patch-spectrum figures and is therefore retained for stylistic compatibility, but it should be read with the cosmic-variance considerations of Sec.~\ref{sec:fm_diag_cosmic_variance} in mind.

\paragraph{Legacy single-input estimator (\texttt{flat\_sky\_tt\_power\_spectrum}).} For backward compatibility with the earliest notebook drafts, a single-input variant operating on $(N,H,W)$ arrays of bare temperature patches is retained. It implements Eq.~(\ref{eq:flatsky_tt_estimator}) with the linear binning of Eq.~(\ref{eq:linear_binning}); the only difference from \texttt{flat\_sky\_tt\_spectrum} is the input layout and a uniform-batch averaging convention. The two estimators give numerically identical results on $(N,1,H,W)$ inputs.
\subsubsection{Pixel-space Gaussian smoothing (\texttt{gaussian\_smooth\_patches\_t})}
\label{sec:fm_diag_smoothing}

Both the reference and the generated patches are passed through a mild isotropic Gaussian filter before the spectrum is estimated. With $G_{\sigma}$ the unit-area normalised 2D Gaussian of width $\sigma_{\rm pix}$ pixels,
\begin{equation}
  G_{\sigma}(\bm x)\;=\;\frac{1}{2\pi\sigma_{\rm pix}^2(\Delta x)^2}\,
  \exp\!\Bigl(-\frac{|\bm x|^2}{2\sigma_{\rm pix}^2(\Delta x)^2}\Bigr),
\end{equation}
the smoothed patch is the convolution
\begin{equation}
  T_\sigma(\bm x)\;=\;(G_\sigma\!\ast\!T)(\bm x),
  \qquad
  \widetilde T_\sigma(\bm k)\;=\;\widetilde G_\sigma(\bm k)\,\widetilde T(\bm k),
\end{equation}
with Fourier-space transfer function
\begin{equation}
  \widetilde G_\sigma(\bm k)
  \;=\;\exp\!\bigl(-\tfrac12\sigma_{\rm pix}^2(\Delta x)^2|\bm k|^2\bigr).
\end{equation}
Equivalently, the smoothed bandpower estimator is
\begin{equation}
  \hat P_b^{\sigma}\;=\;
  \frac{1}{N^{(b)}_{\rm modes}}
  \sum_{\bm k:\ell(\bm k)\in\mathcal B_b}
  e^{-\sigma_{\rm pix}^2(\Delta x)^2|\bm k|^2}\,
  \frac{1}{B}\sum_{i=1}^B
  \bigl|\widetilde T_i(\bm k)\bigr|^2\,(\Delta x)^2/N^2.
  \label{eq:smoothed_bandpower}
\end{equation}
The kernel half-power scale is at $\ell^*\!\approx\!2\pi/(\sigma_{\rm pix}\Delta x)$. With the production setting $\sigma_{\rm pix}=1.2$ at $\Delta x=6.7'$ this places $\ell^*\!\approx\!1.3\!\times\!10^3$, deeply inside the band of validity of the small-patch flat-sky approximation. The smoothing acts as a high-pass suppression of artefacts of the deterministic ODE integrator and of pixel discretisation that contaminate the very-high-$\ell$ tail of the unsmoothed spectrum; because it is applied identically to the generated and reference batches, it does not bias the comparison and can be turned off by setting $\sigma_{\rm pix}=0$. In what follows, $P$ refers to the unsmoothed power and $P^{\sigma}$ to the smoothed power.

\subsubsection{Integrated and band-limited variance diagnostics}
\label{sec:fm_diag_variance}

In addition to the binned spectra, two scalar variance diagnostics are recorded from the very same patches. Let $\sigma_{\rm tot}^2$ denote the empirical patch variance, and $\sigma_{\rm sm}^2$ the variance of the same patches after the Gaussian smoothing of Sec.~\ref{sec:fm_diag_smoothing}. By Parseval, the population versions satisfy
\begin{align}
  \sigma_{\rm tot}^2 \;&=\; \int\!\frac{d^2k}{(2\pi)^2}\,P(\bm k), \label{eq:sigma_tot}\\
  \sigma_{\rm sm}^2 \;&=\; \int\!\frac{d^2k}{(2\pi)^2}\,P(\bm k)\,
    e^{-\sigma_{\rm pix}^2(\Delta x)^2|\bm k|^2}, \label{eq:sigma_sm}
\end{align}
with empirical estimates obtained by direct pixel-domain $\Var(\cdot)$ on the (smoothed) patch arrays, which is what the notebook prints. Equation~(\ref{eq:sigma_tot}) is the integrated power, dominated by the high-$\ell$ tail (where most modes live); Eq.~(\ref{eq:sigma_sm}) suppresses high-$\ell$ exponentially and is therefore dominated by the low-to-medium $\ell$ band, with effective cutoff $\ell\!\lesssim\!\ell^*$. The two scalar ratios
\begin{equation}
  r_{\rm tot}\;=\;\frac{\sigma^{\rm gen}_{\rm tot}}{\sigma^{\rm ref}_{\rm tot}},\qquad
  r_{\rm sm}\;=\;\frac{\sigma^{\rm gen}_{\rm sm}}{\sigma^{\rm ref}_{\rm sm}},
  \label{eq:variance_ratios}
\end{equation}
together provide a coarse but powerful test of whether any amplitude bias is uniform across angular scales. If the model is biased by an overall multiplicative factor on $P$, both ratios are equal. If $r_{\rm sm}<r_{\rm tot}$, the bias is concentrated at the larger angular scales weighted by Eq.~(\ref{eq:sigma_sm}); if $r_{\rm sm}>r_{\rm tot}$, the bias is concentrated at higher $\ell$. The expected ``power-equivalent'' under a uniform amplitude rescale, namely $r_{\rm tot}^2$, is also reported as a sanity reference.

\subsubsection{Variance-matched comparison}
\label{sec:fm_diag_vm}

To isolate spectral shape mismatches from overall amplitude biases, we additionally compute a variance-matched generated batch
\begin{equation}
  T^{\rm vm}_i(\bm x)\;=\;\frac{\sigma^{\rm ref}_{\rm tot}}{\sigma^{\rm gen}_{\rm tot}}\,T^{\rm gen}_i(\bm x),
  \label{eq:vm_field}
\end{equation}
with the rescaling factor estimated from the same batches used for the spectral comparison (this is the line \texttt{gen\_vm\_sp = gen\_sp * \_sc.reshape(1,1,1,1)} of \texttt{run\_spectral\_diagnostics}). Its bandpower follows immediately from linearity of Eq.~(\ref{eq:flatsky_tt_estimator}),
\begin{equation}
  \hat P^{\rm vm}_b
  \;=\;\biggl(\frac{\sigma^{\rm ref}_{\rm tot}}{\sigma^{\rm gen}_{\rm tot}}\biggr)^{\!2}\,\hat P^{\rm gen}_b.
  \label{eq:vm_bandpower}
\end{equation}
By construction the variance-matched and reference batches agree on $\sigma_{\rm tot}^2=\int P\,d^2k/(2\pi)^2$. Any residual disagreement of $\hat P^{\rm vm}_b$ with the reference $\hat P^{\rm ref}_b$ is therefore a pure shape bias: a redistribution of power across $\ell$ at fixed integrated variance, that no overall rescale can absorb. We also record the corresponding spectral mismatch metrics on both the raw and the variance-matched bandpowers, so that the contribution of amplitude and shape to the overall residual can be tracked independently across training.
\subsubsection{Cosmic variance per bin}
\label{sec:fm_diag_cosmic_variance}

For a Gaussian random field, the bandpower estimator of Eq.~(\ref{eq:flatsky_tt_estimator}) follows a chi-squared distribution and its variance is, to leading order in $1/N^{(b)}_{\rm modes}$ and $1/B$,
\begin{equation}
  \frac{\Var(\hat P_b)}{P_b^2}
  \;\approx\;\frac{2}{B\,N^{(b)}_{\rm modes}},
  \label{eq:cosmic_variance_per_bin}
\end{equation}
where $B$ is the number of patches and $N^{(b)}_{\rm modes}$ is given by Eq.~(\ref{eq:n_modes_per_bin}). Three remarks follow.

\begin{enumerate}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{Linear vs.\ logarithmic binning at low $\ell$.} The same patch ensemble carries very different per-bin uncertainties under the two binnings of Sec.~\ref{sec:fm_diag_binning}. At the patch fundamental $\ell\!\approx\!\ell_{\rm fund}\!\approx\!50$ for our $7^\circ$ patches, the linear estimator's first bin extends from $\ell=0$ to $\ell=\Delta\ell\!\approx\!76$ and aggregates roughly $\bigl(\ell_{\rm fund}+\Delta\ell\bigr)\Delta\ell\,L^2/(4\pi)\!\approx\!7$ Fourier pixels. With $B=64$ patches this gives $\sigma(\hat P_0)/P_0 \approx \sqrt{2/(64\cdot 7)}\approx 6.7\%$, a small enough scatter that any low-$\ell$ deficit of order $\sim\!10\%$ is statistically resolved at first-bin level. The log estimator, with $\Delta\ln\ell\approx 0.083$, places the same low-$\ell$ region across roughly $\sim\!10$ very narrow bins, each containing $\lesssim\!1$ Fourier pixel and a per-bin scatter of $\sigma(\hat P_b)/P_b\approx \sqrt{2/64}\approx 18\%$. The two visualisations therefore highlight different things: the linear plot exposes any low-$\ell$ amplitude deficit as a single high-precision data point, while the log plot resolves the same deficit across many independently-noisy points and is visually softened by the eye's tendency to trend-match.
  \item \emph{Visual interpretation.} A model bias smaller than the per-bin $1\sigma$ scatter of Eq.~(\ref{eq:cosmic_variance_per_bin}) is statistically indistinguishable from a fluctuation. The Monte Carlo theory band of Sec.~5.6.1 provides a direct numerical estimate of this floor at every $\ell$, by replicating the per-bin scatter with independent realisations of the target distribution, and is the recommended visual ruler against which the generated curve is read.
  \item \emph{Practical recommendation.} For a strict go/no-go test on the $TT$ amplitude at low $\ell$, the linear-binned plot is the relevant diagnostic. The log-binned plot is the appropriate visualisation when comparing across decades of $\ell$ on a single axis, and should be read in conjunction with the per-bin uncertainty implied by Eq.~(\ref{eq:cosmic_variance_per_bin}).
\end{enumerate}

\subsubsection{Why this is the relevant test for a CMB prior}

For a Gaussian field, the angular power spectrum is a sufficient statistic and an exact match of the second moments is mathematically equivalent to learning the distribution. For the lensed CMB the field is mildly non-Gaussian (the lensing-induced 4-point function leaves an imprint at the level of $\sim\!1\%$ of the variance at the Planck angular resolution), but on the angular scales resolved by the patch ($\ell\!\lesssim\!1200$) the second-moment test still captures the bulk of the information content. For the Galactic foregrounds the field is more strongly non-Gaussian, but the second-moment comparison remains the standard first-line diagnostic and is the quantity that downstream component-separation analyses are most sensitive to. We therefore treat the $D_\ell^{TT}$ comparison, together with the integrated and band-limited variance diagnostics of Sec.~\ref{sec:fm_diag_variance} and the variance-matched comparison of Sec.~\ref{sec:fm_diag_vm}, as the principal go/no-go tests of the trained prior, and reserve higher-order tests (cross-power spectrum residuals, pixel-domain histograms, and one-point statistics) for follow-up work.

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{spectral_comparison7.png}
  \caption{Post-training flat-sky $D_\ell$ comparison for the trained $p(f)$ prior in $\mu\mathrm{K}_{\rm CMB}$-based map units (after de-standardisation), shown here on the legacy four-panel layout produced by an earlier $(T,Q,U)$-channel run of the notebook for visual context. The current notebook is restricted to temperature, and the relevant panel is the top-left $D_\ell^{TT}$. Solid black: validation patches drawn from the saved $p(f)$ prior, the empirical reference distribution. Dashed coloured: patches generated by the trained deterministic flow-matching model, sampled with the Heun second-order ODE integrator at $N_{\rm steps}=1200$, $t\in[0,0.995]$, and a state-clipping bound $|\xvec|\!\leq\!5$. Dotted grey: Monte Carlo theory band built from independent Gaussian full-sky realisations of the lensed CAMB spectra passed through the identical patch and flat-sky pipeline. The agreement between the validation and theory bands is a calibration check on the patch / flat-sky estimator pipeline itself; the agreement between the generated and validation bands is the direct test of the trained prior.}
  \label{fig:dl_panel_phys}
\end{figure}

\begin{figure}[h]
  \centering
  \includegraphics[width=0.95\linewidth]{spectral_comparison6.png}
  \caption{Same as Fig.~\ref{fig:dl_panel_phys} but in normalised patch units, i.e.\ before applying the inverse standardisation of Sec.~\ref{sec:data_savedpriors}. This is the domain in which the network is trained.}
  \label{fig:dl_panel_norm}
\end{figure}

\subsubsection{Reading the diagnostics: amplitude vs.\ shape biases}

Combining the binned spectra of Sec.~\ref{sec:fm_diag_binning}, the variance ratios of Eq.~(\ref{eq:variance_ratios}), and the variance-matched comparison of Eq.~(\ref{eq:vm_bandpower}) provides a sharp decomposition of any failure mode of the trained prior into two limiting cases.
\begin{itemize}[leftmargin=2.0em,itemsep=0.15em]
  \item \emph{Amplitude bias} (uniform multiplicative error on $P$). Characterised by $r_{\rm tot}\!\approx\!r_{\rm sm}\!\neq\!1$ and a variance-matched curve $\hat P^{\rm vm}_b$ that closely tracks the reference at all $\ell$. Action items: train longer and/or rely on the validation-spectral checkpoint of Sec.~\ref{sec:fm_ema_and_best} to pick a better EMA snapshot.
  \item \emph{Shape bias} (scale-dependent error on $P$). Characterised by $r_{\rm sm}\!\neq\!r_{\rm tot}$ and by a variance-matched curve $\hat P^{\rm vm}_b$ that fails to track the reference in the same multipole range where $\hat P^{\rm gen}_b$ does. The sign of $r_{\rm sm}-r_{\rm tot}$ localises the failure: $r_{\rm sm}\!<\!r_{\rm tot}$ indicates a deficit at low $\ell$, $r_{\rm sm}\!>\!r_{\rm tot}$ at high $\ell$. Action items: increase model capacity or training budget; revisit the $t$-sampling bias; or add a targeted post-hoc spectral correction rather than reintroducing the auxiliary training losses abandoned in the pure-CFM setup.
\end{itemize}
The current state of the trained $p(f)$ prior at the production run reported in Fig.~\ref{fig:dl_panel_phys} is most consistent with a shape bias localised at $\ell\!\lesssim\!200$, with $r_{\rm sm}\!\approx\!0.82$ noticeably smaller than $r_{\rm tot}\!\approx\!0.92$. The variance-matched curve closes most but not all of the residual at low $\ell$, confirming that the dominant failure mode is a redistribution of power at fixed integrated variance and is therefore a pure shape problem rather than a uniform amplitude undershoot.







% ---------- minimal preamble if compiling standalone ----------
% \documentclass[11pt]{article}
% \usepackage[margin=1in]{geometry}
% \usepackage{amsmath, amssymb, amsthm, mathtools}
% \usepackage{enumitem}
% \usepackage{listings}
% \usepackage{xcolor}
% \newcommand{\xvec}{\mathbf{x}}
% \newcommand{\bm}[1]{\boldsymbol{#1}}
% \newcommand{\dd}{\mathrm{d}}
% \DeclareMathOperator*{\Var}{Var}
% \lstdefinestyle{nb}{basicstyle=\ttfamily\small,breaklines=true,
%   keywordstyle=\color{blue!60!black},commentstyle=\color{gray!70!black},
%   stringstyle=\color{purple!60!black}}
% \begin{document}
% ---------------------------------------------------------------

\section{\texorpdfstring{From the trained vector field to the $D_\ell^{TT}$ plot}{From the trained vector field to the D\_ell\^{}TT plot}}
\label{sec:vf_to_dl}

This section walks through, step by step, what the notebook does between
``the network has finished training'' and ``the $D_\ell^{TT}$ figure is on the
screen''. Each subsection states the underlying mathematics and points to the
exact construct in \texttt{dm\_cmb.ipynb} that implements it. The central
object is the estimator \texttt{flat\_sky\_tt\_spectrum}, which converts a
batch of generated temperature patches into a binned isotropic power
spectrum $\hat P_b$; the $D_\ell^{TT}$ curve is a one‑line cosmetic rescale
of that estimator output.

The pipeline is the composition
\begin{equation}
  \underbrace{\xvec_0\sim\mathcal N(0,I)}_{\text{step 1}}
  \;\xrightarrow{\ \text{Heun ODE}\ }\;
  \underbrace{\xvec_{t_{\max}}}_{\text{step 2}}
  \;\xrightarrow{\ \hat\xvec_0=R(\xvec_t,t)\ }\;
  \underbrace{\hat T_i^{\text{norm}}}_{\text{step 3}}
  \;\xrightarrow{\ \mu+\sigma\,\cdot\ }\;
  \underbrace{T_i}_{\text{step 4}}
  \;\xrightarrow{\ G_\sigma\ast\ }\;
  \underbrace{T_i^{\sigma}}_{\text{step 5}}
  \;\xrightarrow{\ \text{FFT+bin}\ }\;
  \underbrace{\hat P_b}_{\text{step 6}}
  \;\xrightarrow{\ \ell(\ell+1)/2\pi\ }\;
  \underbrace{\hat D_b^{TT}}_{\text{step 7}}.
  \label{eq:vf_pipeline}
\end{equation}

\subsection{Step 1 -- Initial noise in normalised patch units}
\label{sec:vf_step_noise}

A batch of $B=N_{\rm GEN}$ initial states is drawn from the simple base
distribution
\begin{equation}
  \xvec_0 \sim \mathcal N(0, I_{1\times 64\times 64}), \qquad B = N_{\rm GEN},
  \label{eq:init_noise}
\end{equation}
exactly as the network was trained against (Sec.~\ref{sec:fm} of
the main text). In the notebook this is the line
\begin{lstlisting}[style=nb]
x0 = torch.randn(N_GEN, 1, 64, 64, device=device)
\end{lstlisting}
The single channel encodes temperature in the \emph{normalised patch units}
of the prior $p(f)$: the patches the network was trained on were
standardised per‑channel by $\mu,\sigma$ stored in \texttt{prior\_f}, so
$\xvec_0$ lives in that same standardised space.

\subsection{Step 2 -- Heun integration of the flow ODE}
\label{sec:vf_step_heun}

The trained network is the conditional vector field
$u^\theta:\mathbb R^{1\times 64\times 64}\times[0,1]\to
\mathbb R^{1\times 64\times 64}$, which approximates the marginal velocity
that transports $p_{\rm init}=\mathcal N(0,I)$ to the data distribution
$p_{\rm data}$ along the Gaussian probability path
\begin{equation}
  \xvec_t \;=\; \alpha_t\,\xvec_{\rm data} + \beta_t\,\bm\epsilon,
  \qquad \alpha_t=1-t,\quad \beta_t=t,\quad t\in[0,1].
  \label{eq:linear_path}
\end{equation}
At inference time, samples are produced by integrating the deterministic
flow ODE
\begin{equation}
  \frac{\dd}{\dd t}\xvec_t \;=\; u^\theta(\xvec_t, t), \qquad
  \xvec_{t_{\min}} \;=\; \xvec_0,
  \label{eq:flow_ode_postproc}
\end{equation}
forward from $t_{\min}{=}\,0.05$ to $t_{\max}{=}\,0.995$ on a uniform grid of
$N_{\rm steps}{=}\,1200$ points,
\begin{equation}
  \{t_n\}_{n=0}^{N_{\rm steps}-1} \;=\; t_{\min} + n\,h, \qquad
  h \;=\; \frac{t_{\max}-t_{\min}}{N_{\rm steps}-1}.
  \label{eq:time_grid}
\end{equation}
This is set up by
\begin{lstlisting}[style=nb]
sde       = trainer.build_sampling_sde()      # wraps unet as u^theta
simulator = build_sampler(sde)                # -> HeunSimulator (sigma=0)
ts_1d     = torch.linspace(SAMPLE_T_MIN, SAMPLE_T_MAX, N_STEPS, device=device)
ts        = ts_1d.view(1,-1,1,1,1).expand(N_GEN,-1,-1,-1,-1)
\end{lstlisting}
The Heun second‑order step is implemented in
\texttt{HeunSimulator.step} as the standard predictor--corrector pair
\begin{align}
  \tilde\xvec_{n} \;&=\; \xvec_n + h\,u^\theta(\xvec_n, t_n)
    && \text{(Euler predictor)}, \label{eq:heun_predictor}\\
  \xvec_{n+1} \;&=\; \xvec_n + \tfrac{h}{2}\Bigl[u^\theta(\xvec_n,t_n) +
                 u^\theta(\tilde\xvec_{n}, t_n+h)\Bigr]
    && \text{(trapezoidal corrector)}, \label{eq:heun_step}
\end{align}
giving $\mathcal O(h^3)$ local truncation error vs.\ the $\mathcal O(h^2)$ of
plain Euler. After each step, \texttt{Simulator.simulate} optionally clips
the state to a hard box for numerical stability,
\begin{equation}
  \xvec_{n+1} \;\leftarrow\; \operatorname{clamp}(\xvec_{n+1},
  -C,\,+C), \qquad C \;=\; \texttt{STATE\_CLIP\_ABS}\;=\;5,
  \label{eq:state_clip}
\end{equation}
which acts only on rare runaway pixels (the fraction of clipped pixels is
printed by \texttt{clip\_frac}). The full loop is
\begin{lstlisting}[style=nb]
with torch.no_grad():
    xt_final = simulator.simulate(x0, ts, state_clip_abs=STATE_CLIP_ABS)
\end{lstlisting}
and returns the terminal state $\xvec_{t_{\max}}\in\mathbb R^{B\times 1\times
64\times 64}$ in normalised patch units.

\subsection{\texorpdfstring{Step 3 -- Endpoint denoising: $\xvec_{t_{\max}}\to\hat\xvec_0$}{Step 3 -- Endpoint denoising: x\_tmax -> x0\_hat}}
\label{sec:vf_step_x0hat}

Because $t_{\max}=0.995<1$, the terminal state is still slightly noisy:
$\xvec_{t_{\max}} = (1-t_{\max})\,\xvec_{\rm data} + t_{\max}\,\bm\epsilon$ in
the exact-path picture of Eq.~(\ref{eq:linear_path}). The notebook therefore
applies the closed‑form endpoint reconstruction implied by the vector field
and the linear schedule.
For any path $(\alpha_t,\beta_t)$ one has identically
\begin{equation}
  u(\xvec_t,t)
  \;=\; \frac{\dot\alpha_t\,\beta_t - \alpha_t\,\dot\beta_t}{\beta_t}\,
        \xvec_{\rm data}
        + \frac{\dot\beta_t}{\beta_t}\,\xvec_t,
\end{equation}
which can be solved for $\xvec_{\rm data}=\hat\xvec_0$ when the determinant
$\det \equiv \alpha_t\dot\beta_t - \dot\alpha_t\beta_t$ is non‑zero:
\begin{equation}
  \hat\xvec_0(\xvec_t,t)
  \;=\;\frac{\dot\beta_t\,\xvec_t \;-\; \beta_t\,u^\theta(\xvec_t,t)}
       {\alpha_t\,\dot\beta_t - \dot\alpha_t\,\beta_t}.
  \label{eq:x0_from_vf_general}
\end{equation}
For the linear schedule $\alpha_t=1-t$, $\beta_t=t$, the determinant is
$\det=(1-t)\cdot 1 - (-1)\cdot t = 1$, so Eq.~(\ref{eq:x0_from_vf_general})
collapses to
\begin{equation}
  \hat\xvec_0(\xvec_t,t) \;=\; \xvec_t \;-\; t\,u^\theta(\xvec_t,t).
  \label{eq:x0_from_vf_linear}
\end{equation}
The implementation lives in
\texttt{GaussianConditionalProbabilityPath.reconstruct\_x0\_from\_vector\_field}
and is called via the thin wrapper \texttt{trainer.reconstruct\_x0}:
\begin{lstlisting}[style=nb]
t_final  = torch.full((N_GEN,1,1,1), SAMPLE_T_MAX, device=device)
generated = trainer.reconstruct_x0(xt_final, t_final).cpu().numpy()
\end{lstlisting}
The output \texttt{generated} is $\{\hat\xvec_0^{(i)}\}_{i=1}^{B}\in
\mathbb R^{B\times 1\times 64\times 64}$ -- a batch of denoised patches in
\emph{normalised patch units}.

\subsection{Step 4 -- De-standardisation to physical units}
\label{sec:vf_step_denorm}

When the comparison is to be made in $\mu\mathrm K_{\rm CMB}$-based units
(\texttt{COMPARE\_IN\_PHYSICAL\_UNITS=True}), each pixel of each patch is
mapped back through the channel-wise mean/std stored in the prior dictionary,
\begin{equation}
  T_i(\bm x) \;=\; \mu \;+\; \sigma\,\hat\xvec_0^{(i)}(\bm x),
  \qquad (\mu,\sigma)=(\texttt{prior\_f['mean']},\texttt{prior\_f['std']}),
  \label{eq:denorm}
\end{equation}
implemented in
\begin{lstlisting}[style=nb]
def _denormalize_from_prior(arr, prior_dict):
    mean = np.asarray(prior_dict['mean'], dtype=np.float32)
    std  = np.asarray(prior_dict['std' ], dtype=np.float32)
    return (arr * std + mean).astype(np.float32)
generated_base = _denormalize_from_prior(generated, prior_f)
\end{lstlisting}
In the normalised-domain comparison mode the inverse standardisation is
skipped and the spectrum is computed directly on $\hat\xvec_0^{(i)}$. In
both modes the reference batch \texttt{ref\_base} is fed through the
\emph{same} de-standardisation/identity branch, so the generated and the
reference are always in the same units before the spectrum is taken.

\subsection{Step 5 -- Optional Gaussian pixel smoothing}
\label{sec:vf_step_smooth}

Both batches are passed through a mild isotropic 2D Gaussian filter of width
$\sigma_{\rm pix}=\texttt{SMOOTH\_SIGMA\_PIX}=1.2$ pixels (production
setting),
\begin{equation}
  T_i^{\sigma}(\bm x) \;=\; (G_{\sigma_{\rm pix}} \ast T_i)(\bm x),
  \qquad
  \widetilde T_i^{\sigma}(\bm k) \;=\;
   e^{-\tfrac12\sigma_{\rm pix}^2(\Delta x)^2|\bm k|^2}\,
   \widetilde T_i(\bm k),
  \label{eq:smooth_kernel}
\end{equation}
implemented as
\begin{lstlisting}[style=nb]
generated_sp = gaussian_smooth_patches_t(generated_base, SMOOTH_SIGMA_PIX)
ref_sp       = gaussian_smooth_patches_t(ref_base,       SMOOTH_SIGMA_PIX)
\end{lstlisting}
(the routine \texttt{gaussian\_smooth\_patches\_t} is a thin
\texttt{scipy.ndimage.gaussian\_filter} wrapper). Because the same kernel is
applied to generated and reference, the comparison is unbiased; the smoothing
serves only to suppress sub-pixel artefacts of the ODE integrator at the
patch Nyquist scale (Sec.~\ref{sec:fm_diag_smoothing}). Setting
\texttt{SMOOTH\_SIGMA\_PIX=0} reproduces the unsmoothed spectrum.

\subsection{Step 6 -- The estimator \texttt{flat\_sky\_tt\_spectrum}}
\label{sec:vf_step_estimator}

This is the heart of the section. The call
\begin{lstlisting}[style=nb]
ells_gen, spectra_gen = flat_sky_tt_spectrum(generated_sp, reso_arcmin=RESO_ARCMIN)
\end{lstlisting}
returns $(\ell_b)_{b=0}^{N_{\rm bin}-1}$ and $\{TT:\hat P_b\}$. Below we
walk through the function body, equation by equation. Throughout this
subsection $T_i$ denotes the (possibly smoothed) input patch
\texttt{T\_patches[i]} of shape $(H,W)=(64,64)$ and $B=N$ is the number of
patches.

\paragraph{(6a) Pixel geometry.} The pixel scale is converted to radians,
\begin{equation}
  \Delta x \;=\; \texttt{reso\_rad}
  \;=\; \frac{\pi}{180}\cdot\frac{\texttt{reso\_arcmin}}{60},
\end{equation}
which for \texttt{RESO\_ARCMIN}=6.7 gives $\Delta x \approx 1.949\times
10^{-3}\,\mathrm{rad}$. The patch side length in radians is $L=W\,\Delta x
\approx 7.46^\circ$, so the patches are small enough for the flat-sky
approximation (Sec.~\ref{sec:flatsky}).
\begin{lstlisting}[style=nb]
reso_rad = np.deg2rad(reso_arcmin / 60.0)
\end{lstlisting}

\paragraph{(6b) Fourier grid.} \texttt{np.fft.fftfreq(N,d=$\Delta x$)} returns
the cycles-per-radian grid
\begin{equation}
  k_m \;=\; \begin{cases}
    \dfrac{m}{N\,\Delta x}, & 0 \le m \le \lfloor N/2\rfloor,\\[0.3em]
    \dfrac{m-N}{N\,\Delta x}, & \lfloor N/2\rfloor < m < N,
  \end{cases}
  \qquad m=0,\ldots,N-1,
\end{equation}
i.e.\ frequencies measured in inverse radians (not angular frequencies). The
multipole assigned to each Fourier pixel is then $\ell(\bm k) = 2\pi|\bm k|$
(this is the standard flat-sky correspondence; the factor of $2\pi$ converts
from cycles/radian to radians$^{-1}$):
\begin{equation}
  k_x[m] \;=\; \texttt{fftfreq}(W,\Delta x)[m],\quad
  k_y[n] \;=\; \texttt{fftfreq}(H,\Delta x)[n],\quad
  \ell(\bm k) \;=\; 2\pi\sqrt{k_x^2+k_y^2}.
  \label{eq:ell_map}
\end{equation}
\begin{lstlisting}[style=nb]
kx = np.fft.fftfreq(W, d=reso_rad)
ky = np.fft.fftfreq(H, d=reso_rad)
KX, KY  = np.meshgrid(kx, ky)
K_mag   = np.sqrt(KX**2 + KY**2)
ell_map = 2 * np.pi * K_mag
\end{lstlisting}
The largest multipole on the grid is $\ell_{\max} = 2\pi\sqrt{2}/(2\Delta x)
= \pi\sqrt{2}/\Delta x$ (corner of the FFT grid) and the patch fundamental is
$\ell_{\rm fund} = 2\pi/L = 2\pi/(W\Delta x)$.

\paragraph{(6c) Linear bandpower partition.} The multipole axis is divided
into
\begin{equation}
  N_{\rm bin} \;=\; \min(H/2,\,30)
\end{equation}
linear bins of equal width $\Delta\ell=\ell_{\max}/N_{\rm bin}$:
\begin{equation}
  \ell_b^L = b\,\Delta\ell,\quad
  \ell_b^R = (b{+}1)\,\Delta\ell,\quad
  \ell_b   = \tfrac12(\ell_b^L+\ell_b^R),\quad
  b=0,\ldots,N_{\rm bin}-1.
  \label{eq:linear_bins_estim}
\end{equation}
\begin{lstlisting}[style=nb]
ell_max     = ell_map.max()
num_bins    = min(H // 2, 30)
bin_edges   = np.linspace(0, ell_max, num_bins + 1)
ell_centres = 0.5 * (bin_edges[:-1] + bin_edges[1:])
\end{lstlisting}
The mode counts $N^{(b)}_{\rm modes}$ implied by this binning are given by
Eq.~(\ref{eq:n_modes_per_bin}) of the main text.

\paragraph{(6d) Discrete periodogram with continuum normalisation.} For
patch $i$, the function computes the unitary 2D DFT
\begin{equation}
  \widetilde T_i[\bm k] \;=\; \sum_{\bm x} T_i(\bm x)\,
   \exp\!\bigl(-2\pi i\,(k_x m + k_y n)\,\Delta x\bigr),
\end{equation}
where $\bm x=(m,n)\,\Delta x$, and then forms the patch periodogram with the
DFT--continuum bridge factor
\begin{equation}
  P^{(i)}(\bm k) \;=\; |\widetilde T_i[\bm k]|^2\,\cdot\,
  \underbrace{\frac{(\Delta x)^2}{HW}}_{\equiv\;\text{norm}}.
  \label{eq:periodogram_norm}
\end{equation}
\begin{lstlisting}[style=nb]
norm = reso_rad**2 / (H * W)              # = (Delta x)^2 / (H W)
T_fft = np.fft.fft2(T_patches[i])
P_TT  = np.abs(T_fft) ** 2 * norm
\end{lstlisting}
The factor $(\Delta x)^2/(HW)$ is the standard normalisation that makes
$P^{(i)}(\bm k)$ a consistent estimator of the continuum patch power on the
central band of the FFT grid:\footnote{With NumPy's
non-unitary DFT, Parseval reads $\sum_{\bm k}|\widetilde T[\bm k]|^2 = HW
\sum_{\bm x}|T(\bm x)|^2$. The pixel-domain variance is then
$\sigma_T^2 = (\Delta x)^2\sum_{\bm x}|T|^2/L^2 = \sum_{\bm k}P^{(i)}(\bm k)
/[(2\pi)^2(\Delta k)^{-2}]\cdot(2\pi)^2$ -- i.e.\ the standard
$\sigma_T^2 = \int P(\bm k)\,d^2k/(2\pi)^2$ Parseval identity in the
$2\pi|\bm k|\!=\!\ell$ convention used here.} the per-patch population power
satisfies
$\langle P^{(i)}(\bm k)\rangle \;\to\; C^{TT}_{\ell(\bm k)}$ as $H,W\to\infty$
at fixed $L=W\Delta x$, in the same convention as the binned estimator of
Eq.~(\ref{eq:flatsky_tt_estimator}).

\paragraph{(6e) Radial annular average.} For each bin $b$, the function takes
the unweighted mean of the periodogram over Fourier pixels falling in
$\mathcal B_b = [\ell_b^L,\ell_b^R)$:
\begin{equation}
  \bar P_b^{(i)}
  \;\equiv\; \frac{1}{N^{(b)}_{\rm modes}}
            \sum_{\bm k:\ell(\bm k)\in\mathcal B_b} P^{(i)}(\bm k),
  \qquad
  N^{(b)}_{\rm modes} \;=\; \#\{\bm k:\ell(\bm k)\in\mathcal B_b\}.
  \label{eq:per_patch_bin_mean}
\end{equation}
\begin{lstlisting}[style=nb]
for b in range(num_bins):
    mask = (ell_map >= bin_edges[b]) & (ell_map < bin_edges[b + 1])
    if mask.any():
        accum_TT[b] += P_TT[mask].mean()        # = bar{P}_b^{(i)}
        if i == 0:
            counts[b] += 1
\end{lstlisting}
The auxiliary array \texttt{counts} records, at $i=0$, whether bin $b$
contains at least one Fourier pixel; bins with $N^{(b)}_{\rm modes}=0$ are
flagged as invalid and returned as \texttt{NaN}.

\paragraph{(6f) Batch average.} After the loop over patches, the function
divides by $N=B$,
\begin{equation}
  \hat P_b \;=\; \frac{1}{B}\sum_{i=1}^{B}\bar P_b^{(i)}
  \;=\; \frac{1}{B}\sum_{i=1}^{B}\frac{1}{N^{(b)}_{\rm modes}}
        \sum_{\bm k:\ell(\bm k)\in\mathcal B_b}
        |\widetilde T_i[\bm k]|^2\,\frac{(\Delta x)^2}{HW}.
  \label{eq:final_bandpower}
\end{equation}
\begin{lstlisting}[style=nb]
valid         = counts > 0
arr_TT        = np.full(num_bins, np.nan)
arr_TT[valid] = accum_TT[valid] / max(N, 1)     # divide by B
return ell_centres, {'TT': arr_TT}
\end{lstlisting}
Equation~(\ref{eq:final_bandpower}) is the same estimator as
Eq.~(\ref{eq:flatsky_tt_estimator}) of the main text, written in the order
``radial mean first, batch mean second'' rather than ``batch mean first,
radial mean second''. The two orderings are algebraically identical because
each Fourier pixel carries equal weight in each bin and each patch carries
equal weight in the batch.

\paragraph{Return value.} The tuple
$(\texttt{ells\_gen},\texttt{spectra\_gen})=
((\ell_b)_b,\;\{TT:(\hat P_b)_b\})$ is a discrete sampling of the binned
isotropic patch power spectrum. It is what the downstream plotting code
consumes.

\subsection{\texorpdfstring{Step 7 -- From $\hat P_b$ to $\hat D_b^{TT}$}{Step 7 -- From P\_b to D\_b\^{}TT}}
\label{sec:vf_step_dell}

The CMB-style display variable is
\begin{equation}
  \hat D_b^{TT} \;=\; \frac{\ell_b(\ell_b+1)}{2\pi}\,\hat P_b,
  \label{eq:patch_dell_estim}
\end{equation}
multiplied element-wise at the bin centres of
Eq.~(\ref{eq:linear_bins_estim}). In the notebook this is literally
\begin{lstlisting}[style=nb]
dl_fac_ref = ells_ref * (ells_ref + 1) / (2 * np.pi)
dl_fac_gen = ells_gen * (ells_gen + 1) / (2 * np.pi)
ref_vals   = dl_fac_ref * spectra_ref['TT']
gen_vals   = dl_fac_gen * spectra_gen['TT']
ax.plot(ells_ref, ref_vals, 'k-',  label=reference_label)
ax.plot(ells_gen, gen_vals, 'C0--', label='generated')
ax.set_xlabel(r'$\ell$'); ax.set_ylabel(r'$D_\ell^{TT}$'); ax.set_xscale('log')
\end{lstlisting}
The same construction is applied to the optional Monte‑Carlo theory band
$\hat P_b^{\rm th}$,
\begin{equation}
  \hat D_b^{TT,\,\rm th} \;=\; \frac{\ell_b(\ell_b+1)}{2\pi}\,\hat P_b^{\rm th},
\end{equation}
and overlaid as the dotted grey curve in the comparison figure
(Fig.~\ref{fig:dl_panel_phys}).

\subsection{End-to-end summary}
\label{sec:vf_summary}

Combining Eqs.~(\ref{eq:init_noise})--(\ref{eq:patch_dell_estim}), the
quantity actually plotted on the vertical axis of the $D_\ell^{TT}$ figure is
\begin{equation}
\boxed{
  \hat D_b^{TT}
  \;=\; \frac{\ell_b(\ell_b+1)}{2\pi}\;
        \frac{1}{B}\sum_{i=1}^{B}\frac{1}{N^{(b)}_{\rm modes}}
        \!\!\!\sum_{\bm k:\ell(\bm k)\in\mathcal B_b}\!\!\!
        \Bigl|\mathcal F\bigl[\mu + \sigma\,
              R\!\bigl(\Phi_{t_{\min}\to t_{\max}}^{u^\theta}(\xvec_0^{(i)}),\,
                       t_{\max}\bigr)\bigr](\bm k)\Bigr|^2\,
        \frac{(\Delta x)^2}{HW},
}
  \label{eq:end_to_end}
\end{equation}
where
\begin{itemize}[leftmargin=2.0em,itemsep=0.15em]
  \item $\xvec_0^{(i)}\sim\mathcal N(0,I_{64\times 64})$ is the $i$-th
    initial-noise patch (step 1, Eq.~\ref{eq:init_noise});
  \item $\Phi_{t_{\min}\to t_{\max}}^{u^\theta}$ is the Heun flow map of the
    learned vector field $u^\theta$, integrating
    Eq.~(\ref{eq:flow_ode}) from $t_{\min}$ to $t_{\max}$ on the grid of
    Eq.~(\ref{eq:time_grid}), with optional state clipping
    (Eq.~\ref{eq:state_clip});
  \item $R(\xvec_t,t)=\xvec_t-t\,u^\theta(\xvec_t,t)$ is the linear-schedule
    endpoint reconstruction of Eq.~(\ref{eq:x0_from_vf_linear});
  \item $\mu+\sigma\,\cdot$ is the per-channel de-standardisation of
    Eq.~(\ref{eq:denorm}), present only in physical-units mode;
  \item $\mathcal F$ is the 2D DFT \texttt{np.fft.fft2}, with the multipole
    grid of Eq.~(\ref{eq:ell_map});
  \item the inner double sum is the linearly-binned isotropic estimator of
    Eq.~(\ref{eq:final_bandpower}), i.e.\ the return value of
    \texttt{flat\_sky\_tt\_spectrum};
  \item the prefactor $\ell_b(\ell_b+1)/(2\pi)$ converts the patch power
    $\hat P_b$ to the CMB-style $\hat D_b^{TT}$ of
    Eq.~(\ref{eq:patch_dell_estim}).
\end{itemize}
The optional pixel-domain Gaussian smoothing of Eq.~(\ref{eq:smooth_kernel})
multiplies the integrand of the inner sum by
$\exp[-\sigma_{\rm pix}^2(\Delta x)^2|\bm k|^2]$ (cf.\ the smoothed
bandpower of Eq.~\ref{eq:smoothed_bandpower}).

The reference curve $\hat D_b^{\rm ref}$ in the same figure is obtained by
applying steps~4--7 to the validation patches $\{T_i^{\rm ref}\}$, with
steps~1--3 (the neural sampling) replaced by a direct draw from the saved
$p(f)$ prior. Because both branches share the identical de-standardisation,
smoothing, FFT, and binning, any disagreement between $\hat D_b^{\rm gen}$
and $\hat D_b^{\rm ref}$ is attributable to the trained vector field
$u^\theta$ -- which is the point of the diagnostic.

% \end{document}    % uncomment if compiling standalone




\paragraph{Inputs.}
At each diagnostic step, with EMA weights $\bar\thetavec_n$ active, we
generate $N_{\rm s}=32$ patches $\{\hat\zvec_i\}_{i=1}^{N_{\rm s}}$ by Heun
integration of the deterministic flow ODE plus the linear posterior-mean
endpoint reconstruction. The reference is a fixed held-out validation
batch $\{\zvec_j\}_{j=1}^{N_{\rm ref}}$. Both ensembles are passed
through the same Gaussian pixel smoothing kernel $\sigma_{\rm pix}$
before any spectrum is taken.

\paragraph{Bandpower estimates and band selection.}
We denote by $\hat D_b^{\rm gen}(\bar\thetavec_n)$ and
$\hat D_b^{\rm ref}$ the binned flat-sky $TT$ bandpowers
\begin{equation}
  \hat D_b
  \;=\;
  \frac{\ell_b(\ell_b+1)}{2\pi}\,\hat P_b,
\end{equation}
returned by \texttt{flat\_sky\_tt\_spectrum} on the generated and
reference ensembles, restricted to multipoles $\ell\le\ell_{\max}=1200$.
Define the bin index set
\begin{equation}
  \mathcal M
  \;=\;
  \bigl\{\,b\,:\,
       \hat D_b^{\rm gen},\hat D_b^{\rm ref}\text{ finite and }>0
  \bigr\},
\end{equation}
which excludes bandpowers below the floor of the estimator (typically
the $\ell$-bin containing the patch fundamental).

\paragraph{Bandpower-shape term.}
The first summand is the root-mean-square \emph{log-relative}
discrepancy across $\mathcal M$,
\begin{equation}
  \boxed{\;
  \mathcal S_{\rm shape}(\bar\thetavec_n)
  \;=\;
  \sqrt{
    \frac{1}{|\mathcal M|}
    \sum_{b\in\mathcal M}
      \bigl[\,\log\hat D_b^{\rm gen}(\bar\thetavec_n)
            -\log\hat D_b^{\rm ref}\,\bigr]^2
  }
  \;}
  \label{eq:S_shape_def}
\end{equation}
i.e.\ $\mathcal S_{\rm shape}=\sqrt{\,\overline{\mathrm{MSE}_{\log D}}\,}$.
This is the quantity stored as
\texttt{spectral\_shape\_log\_rmse} in
\texttt{run\_spectral\_diagnostics}, and is a band-by-band measure of
relative shape mismatch in $D_\ell^{TT}$.

\paragraph{Pixel-variance term.}
The second summand pins the integrated patch-level variance,
\begin{equation}
  \boxed{\;
  \mathcal S_{\rm var}(\bar\thetavec_n)
  \;=\;
  \sqrt{
    \frac{1}{C}
    \sum_{c=1}^{C}
      \bigl[\,\log\sigma_c^{\rm gen}(\bar\thetavec_n)
            -\log\sigma_c^{\rm ref}\,\bigr]^2
  }
  \;}
  \label{eq:S_var_def}
\end{equation}
where $\sigma_c^{\rm gen},\sigma_c^{\rm ref}$ are the per-channel
pixel-level standard deviations on the generated and reference
ensembles (here $C=1$). This is the \texttt{spectral\_std\_log\_l2}
field in the diagnostic.

\paragraph{Combined score.}
The scalar used for checkpoint selection is the unweighted sum of the
two terms,
\begin{equation}
  \boxed{\;
  \mathcal S(\bar\thetavec_n)
  \;=\;
  \mathcal S_{\rm shape}(\bar\thetavec_n)
  \;+\;
  \mathcal S_{\rm var}(\bar\thetavec_n).
  \;}
  \label{eq:S_total_def}
\end{equation}
Both summands are dimensionless L$^2$ norms in log-space and so are
roughly comparable in magnitude after $D_\ell^{TT}$ is restricted to
$\ell\le\ell_{\max}$. The minimum of $\mathcal S$ across the run is
recorded as $\mathcal S^\star$ together with the corresponding EMA
snapshot $\bar\thetavec^\star$, which is the checkpoint returned at
inference time.


\section{Foreground prior \texorpdfstring{$p(x)$}{p(x)}: training and post-training diagnostics}
\label{sec:fg}

So far the formalism of Sec.~\ref{sec:fm} has been instantiated for the CMB
prior $p(f)$. The same machinery is then trained from scratch on the
foreground prior $p(x)$ (PySM3 thermal-dust + synchrotron at 143~GHz,
projected and standardised exactly as for $p(f)$ in
Sec.~\ref{sec:data_savedpriors}). Concretely, the only changes between the
two runs are the data sampler (\texttt{prior\_x} instead of
\texttt{prior\_f}) and the on-disk checkpoint name; the U-Net architecture,
the linear schedule, the conditional flow-matching loss
(Eq.~\ref{eq:cfm_linear}), the optimiser, the EMA, and the Heun ODE sampler
are reused verbatim. Distinct objects \texttt{path\_x},
\texttt{unet\_x}, \texttt{trainer\_x} are used so that the $p(f)$ run is
preserved in memory and on disk.

\subsection{Training configuration}
\label{sec:fg_training_config}

The training cell is \texttt{cell~66}; it instantiates a fresh
\texttt{FlowMatchingTrainer} and calls \texttt{train()} with
\texttt{run\_spectral=False}. Foreground patches are dominated by
non-Gaussian morphology (galactic-plane filaments, log-normal-like
one-point PDF), so the in-training flat-sky $TT$ score that drives
best-validation-spectral checkpointing for $p(f)$
(Sec.~\ref{sec:fm_spectral_score}) is no longer sufficient by itself, and we
postpone the spectral evaluation to a dedicated post-training step. The
live diagnostic panel therefore collapses to four sub-plots: training
loss, validation vector-field MSE (Eq.~\ref{eq:cfm_linear} on a fixed
validation batch), the $L_{\rm val,x_0}$ posterior-mean reconstruction
(Eq.~\ref{eq:x0_recon}), and the validation MSE by timestep bin. All
hyperparameters match the $p(f)$ run of
Table~\ref{tab:fm_hparams}; in particular the training objective is the
plain CFM mean-squared error
\begin{equation}
  \mathcal L_{\rm CFM}^{(x)}(\thetavec)
  \;=\;
  \mathbb E_{t,\,z\sim p(x),\,\varepsilon\sim\mathcal N(0,I)}\!
  \bigl\|\,
    u_t^{\thetavec_x}(\xvec_t)
    -\bigl(\dot\alpha_t z + \dot\beta_t\varepsilon\bigr)
  \bigr\|^2,
\end{equation}
with $\xvec_t=\alpha_t z + \beta_t\varepsilon$ and the linear schedule
$\alpha_t=1-t,\beta_t=t$. The output checkpoint is
\texttt{checkpoints/unet\_fg\_t\_pure\_cfm\_linear\_heun\_ema.pt} and
records, alongside the last-epoch weights and EMA shadow, the
\texttt{prior:'foreground'} tag in its \texttt{training\_config}.

\subsection{Per-\texorpdfstring{$t$}{t} velocity-field diagnostic}
\label{sec:fg_pertdiag}

The first post-training diagnostic, in \texttt{cell~68}, is the same
single-curve check used for $p(f)$: it pits the trained vector field
against the analytic CFM target on a fine $t$-grid, with the optimal
linear estimator under matched second-order statistics as a lower bound.
Two curves are plotted versus $t\in[t_{\min},t_{\max}]$:

\paragraph{Trained MSE.}
At each $t$ on the grid we draw $N_{\rm batches}$ independent batches
$\{(\zvec^{(b,m)}, \varepsilon^{(b,m)})\}$ of size $N_{\rm per\,t}$ from the
held-out validation foreground patches and standard Gaussian noise,
form $\xvec_t^{(b,m)}=\alpha_t\zvec^{(b,m)}+\beta_t\varepsilon^{(b,m)}$,
and measure
\begin{equation}
  \mathrm{MSE}_{\rm trained}^{(b)}(t)
  \;=\;
  \frac{1}{N_{\rm per\,t}\,N_{\rm pix}}
  \sum_{m=1}^{N_{\rm per\,t}}
  \Bigl\|
    \hat u_t^{\thetavec_x}\!\bigl(\xvec_t^{(b,m)}\bigr)
    -
    \bigl(\dot\alpha_t\zvec^{(b,m)}+\dot\beta_t\varepsilon^{(b,m)}\bigr)
  \Bigr\|^2.
\end{equation}
The plot shows the mean over batches and a $\pm1\sigma$ band over the
batch-to-batch spread, providing an explicit Monte-Carlo uncertainty band.

\paragraph{Wiener (Gaussian-prior) MMSE floor.}
A natural lower bound for $\mathrm{MSE}_{\rm trained}(t)$ is the per-pixel
MSE achievable by the optimal \emph{linear} estimator of $u_t$ given
$\xvec_t$ when $\zvec$ is replaced by a Gaussian field with the same
second-order statistics. Working in the 2D Fourier basis with the
empirical patch power spectrum $P(\bm k)$,
\begin{equation}
  P(\bm k)
  \;=\;
  \frac{1}{N_{\rm patches}}\sum_{i=1}^{N_{\rm patches}}
    \frac{|\widetilde z^{(i)}(\bm k)|^2}{N_{\rm pix}},
\end{equation}
the 2D Wiener filter on each independent mode gives a closed-form MMSE
\begin{equation}
  \mathrm{MMSE}_{\rm W}(t)
  \;=\;
  \frac{1}{N_{\rm pix}}\sum_{\bm k}
    \frac{(\dot\alpha_t\beta_t-\dot\beta_t\alpha_t)^2\,P(\bm k)}
         {\alpha_t^2\,P(\bm k)+\beta_t^2}.
\end{equation}
The trained curve should track or sit slightly below the Wiener floor:
gaps below it quantify the gain from exploiting non-Gaussian foreground
structure that the linear estimator cannot capture, while sitting
visibly above it at any $t$ is a tell-tale of undertraining at that
point of the probability path.

\subsection{Post-training summary statistics: power spectrum and Minkowski
functionals}
\label{sec:fg_summary_stats}

Because $p(x)$ is heavily non-Gaussian, two-point statistics alone are not
sufficient to evaluate the generative model. We therefore reproduce the
two-panel diagnostic of Heurtel-Depeiges \emph{et\,al.}\
2023~\cite{heurtel2023dust} (their Fig.~C.2): a flat-sky angular power
spectrum and the three Minkowski functionals of standardised excursion
sets. Both diagnostics compare $N_{\rm gen}$ unconditionally generated
patches $\{\hat\xvec^{(i)}\}_{i=1}^{N_{\rm gen}}$, sampled by integrating
the deterministic ODE driven by $\hat u_t^{\thetavec_x}$ from
$\xvec_0\sim\mathcal N(0,I)$ on the time grid
$[t_{\min},t_{\max}]$ with the Heun integrator and applying the
linear-schedule endpoint reconstruction
(Eq.~\ref{eq:x0_from_vf_linear}), against $N_{\rm ref}$ held-out
validation patches $\{\xvec^{(i)}\}_{i=1}^{N_{\rm ref}}$ drawn directly
from \texttt{prior\_x['val\_norm']}. The implementation is in
\texttt{cell~70}.

\subsubsection{Flat-sky angular power spectrum}
\label{sec:fg_dl}

The bandpower estimator is exactly the one defined in
Sec.~\ref{sec:fm_diag_estimator},
\begin{equation}
  \hat C_\ell^{TT,\,\rm patch}
  \;=\;
  \frac{1}{N_{\rm modes}(\ell)}\!\!
  \sum_{|\bm k|\in b_\ell}\!
    \frac{|\widetilde T(\bm k)|^2\,(\Delta x)^2}{HW},
  \qquad \ell=2\pi|\bm k|,
  \label{eq:fg_clest}
\end{equation}
implemented by \texttt{flat\_sky\_tt\_spectrum\_binned} (\texttt{cell~52})
with the log-spaced binning of Eq.~(\ref{eq:log_binning}). A fixed
isotropic Gaussian smoothing of width $\sigma_{\rm pix}$ is applied to
both ensembles before the estimator is taken; the corresponding
pixel-domain kernel is the one of Eq.~(\ref{eq:smooth_kernel}) and the
helper is \texttt{gaussian\_smooth\_patches\_t}. We report the
ensemble-mean $D_\ell^{TT}=\ell(\ell+1)C_\ell^{TT}/2\pi$ for both
ensembles, restricted to multipoles $\ell\le\ell_{\max}=1200$ for plotting
clarity.

A single scalar shape-and-amplitude mismatch is reported alongside the
curve as the log-relative bandpower RMS,
\begin{equation}
  \mathrm{RMSE}_{\log D}
  \;=\;
  \sqrt{\frac{1}{|\mathcal M|}
        \sum_{b\in\mathcal M}
        \bigl[\,\log\hat D_b^{\rm gen}-\log\hat D_b^{\rm ref}\,\bigr]^2},
  \label{eq:fg_dl_logrmse}
\end{equation}
with $\mathcal M$ the set of bins where both estimators are finite and
positive. Because $\log(D^{\rm gen}/D^{\rm ref})\approx
(D^{\rm gen}-D^{\rm ref})/D^{\rm ref}$ at small mismatch,
$100\,(\exp(\mathrm{RMSE}_{\log D})-1)\,\%$ is interpretable
approximately as a per-bin amplitude mismatch in percent.

\subsubsection{Minkowski functionals}
\label{sec:fg_minkowski}

The Minkowski functionals are the canonical morphological non-Gaussianity
diagnostic for 2D random fields~\cite{mecke1994minkowski,
schmalzing1997minkowski}. For each patch $x$, we standardise
$\tilde x=(x-\langle x\rangle)/\mathrm{std}(x)$ and define, for each
threshold $\nu\in\mathbb R$, the excursion set
\begin{equation}
  A_\nu(x) \;=\; \bigl\{\,\bm p\in\Omega:\, \tilde x(\bm p)\ge\nu\,\bigr\},
\end{equation}
where $\Omega$ is the patch domain ($H\times W$ pixels). The three
functionals are
\begin{equation}
  V_0(\nu)\;=\;\frac{|A_\nu|}{|\Omega|},\qquad
  V_1(\nu)\;=\;\frac{\mathrm{Per}(A_\nu)}{4\,|\Omega|},\qquad
  V_2(\nu)\;=\;\frac{\chi(A_\nu)}{2\pi\,|\Omega|},
  \label{eq:mf_def}
\end{equation}
with $|A_\nu|$ the area, $\mathrm{Per}$ the perimeter and $\chi$ the
Euler characteristic of the excursion set; the prefactors are the
conventional 2D Hadwiger normalisations. We compute each functional
patch-by-patch and report the mean and $\pm1\sigma$ band over the
ensemble.

\paragraph{Discrete estimators on a square pixel grid.}
With pixel side $\Delta x=1$ in patch units and $|\Omega|=N_{\rm pix}=HW$,
the discrete estimators implemented by \texttt{\_minkowski\_functionals}
in \texttt{cell~70} are:
\begin{itemize}[leftmargin=2.0em,itemsep=0.15em]
  \item Excursion area:
    $\hat V_0(\nu)=\dfrac{1}{N_{\rm pix}}
      \sum_{\bm p}\mathbf 1[\tilde x(\bm p)\ge\nu]$,
    the fraction of pixels above threshold.
  \item Perimeter:
    $\hat V_1(\nu)=\dfrac{1}{4\,N_{\rm pix}}\,
      \#\bigl\{(\bm p,\bm p'):\bm p\sim_4\bm p',\,
      \mathbf 1[\tilde x(\bm p)\ge\nu]
      \neq\mathbf 1[\tilde x(\bm p')\ge\nu]\bigr\}$,
    the count of 4-connectivity edges that cross the level set,
    normalised by $4|\Omega|$.
  \item Euler characteristic:
    $\hat V_2(\nu)=\dfrac{1}{2\pi N_{\rm pix}}
      \bigl[\,\#\mathrm{components}(A_\nu)
              - \#\mathrm{holes}(A_\nu)\,\bigr]$,
    with components extracted by 4-connectivity flood-fill
    (\texttt{scipy.ndimage.label}) and holes defined as connected
    components of the complement that do not touch the patch boundary.
\end{itemize}
Per-patch standardisation makes these estimators amplitude-independent
and isolates morphological information: any rescaling
$x\mapsto\lambda x+c$ with $\lambda>0$ leaves $\tilde x$ invariant, so the
$V_k$ depend only on the shape of the field. Departures of $V_0,V_1$ from
symmetry around $\nu=0$ -- and of $V_2$ from a perfect odd function of
$\nu$ -- are direct signatures of the non-Gaussian morphology of the
foreground field that the power spectrum (\ref{eq:fg_clest}) cannot
detect.

\paragraph{Reading the diagnostic.}
A trained $p(x)$ prior that captures both the second-order spectrum and
the non-Gaussian morphology should yield (i) $\hat D_\ell^{\rm gen}$
tracking $\hat D_\ell^{\rm ref}$ with $\mathrm{RMSE}_{\log D}$ small (a
few tens of percent or below per bin), and (ii) all three Minkowski
functionals with mean curves and $\pm1\sigma$ bands overlapping the
reference. Mismatches partition cleanly:
\begin{itemize}[leftmargin=2.0em,itemsep=0.15em]
  \item Power spectrum off but Minkowski functionals match: a global
    amplitude/shape mismatch in second-order statistics. Most often
    indicates undertraining at high $\ell$ (visible as a deficit at
    small angular scales).
  \item Power spectrum matches but Minkowski functionals deviate
    (especially asymmetry of $V_0,V_1$ around $\nu=0$ or sign of $V_2$ at
    moderate $\nu$): the model has learned the second-order statistics
    but is generating closer-to-Gaussian fields than the reference. This
    is the typical failure mode of pure CFM on highly non-Gaussian
    foregrounds and motivates more elaborate priors (scattering
    transforms, conditional models) in subsequent work.
\end{itemize}


\pagebreak



\begin{thebibliography}{99}

\bibitem{zaldarriaga1997}
M.~Zaldarriaga and U.~Seljak,
``All-sky analysis of polarization in the microwave background,''
\emph{Physical Review D} \textbf{55} (1997), 1830--1840.

\bibitem{kamionkowski1997}
M.~Kamionkowski, A.~Kosowsky, and A.~Stebbins,
``Statistics of cosmic microwave background polarization,''
\emph{Physical Review D} \textbf{55} (1997), 7368--7388.

\bibitem{gorski2005}
K.~M.~G\'orski, E.~Hivon, A.~J.~Banday, B.~D.~Wandelt, F.~K.~Hansen,
M.~Reinecke, and M.~Bartelmann,
``HEALPix: A Framework for High-Resolution Discretization and Fast Analysis of Data Distributed on the Sphere,''
\emph{The Astrophysical Journal} \textbf{622} (2005), 759--771.

\bibitem{lewis2000camb}
A.~Lewis, A.~Challinor, and A.~Lasenby,
``Efficient Computation of Cosmic Microwave Background Anisotropies in Closed Friedmann--Robertson--Walker Models,''
\emph{The Astrophysical Journal} \textbf{538} (2000), 473--476.

\bibitem{planck2018overview}
N.~Aghanim et al. (Planck Collaboration),
``Planck 2018 results. I. Overview and the cosmological legacy of Planck,''
\emph{Astronomy \& Astrophysics} \textbf{641} (2020), A1.

\bibitem{thorne2017pysm}
B.~Thorne, J.~Dunkley, D.~Alonso, and S.~N{\ae}ss,
``The Python Sky Model: software for simulating the Galactic microwave sky,''
\emph{Monthly Notices of the Royal Astronomical Society} \textbf{469} (2017), 2821--2833.

\bibitem{zonca2021pysm3}
A.~Zonca, T.~A.~J.~Louis, B.~Thorne, N.~Krachmalnicoff, and J.~Borrill,
``PySM 3: A Python Sky Model for simulation of Galactic microwave emission,''
software/documentation release, 2021.

\bibitem{ho2020ddpm}
J.~Ho, A.~Jain, and P.~Abbeel,
``Denoising Diffusion Probabilistic Models,''
in \emph{Advances in Neural Information Processing Systems 33} (2020).

\bibitem{song2021sde}
Y.~Song, J.~Sohl-Dickstein, D.~P.~Kingma, A.~Kumar, S.~Ermon, and B.~Poole,
``Score-Based Generative Modeling through Stochastic Differential Equations,''
in \emph{International Conference on Learning Representations} (2021).

\bibitem{lipman2023flowmatching}
Y.~Lipman, R.~T.~Q.~Chen, H.~Ben-Hamu, M.~Nickel, and M.~Le,
``Flow Matching for Generative Modeling,''
in \emph{International Conference on Learning Representations} (2023).

\bibitem{sullivan2024methods}
R.~M.~Sullivan, L.~T.~Hergt, and D.~Scott,
``Methods for CMB map analysis,''
\emph{arXiv:2410.12951} (2024), \href{https://doi.org/10.48550/arXiv.2410.12951}{doi:10.48550/arXiv.2410.12951}.

\bibitem{scott2024pdg}
D.~Scott and G.~F.~Smoot,
``Cosmic Microwave Background,''
in \emph{Review of Particle Physics}, Particle Data Group collaboration,
\emph{Physical Review D} \textbf{110} (2024), 030001, pp.~524--534,
\href{https://doi.org/10.1103/PhysRevD.110.030001}{doi:10.1103/PhysRevD.110.030001}.

\bibitem{zonca2019healpy}
A.~Zonca, L.~P.~Singer, D.~Lenz, M.~Reinecke, C.~Rosset, E.~Hivon, and K.~M.~G\'orski,
``\texttt{healpy}: equal area pixelization and spherical harmonics transforms for data on the sphere in Python,''
\emph{Journal of Open Source Software} \textbf{4} (2019), 1298,
\href{https://doi.org/10.21105/joss.01298}{doi:10.21105/joss.01298}.

\bibitem{hivon2002master}
E.~Hivon, K.~M.~G\'orski, C.~B.~Netterfield, B.~P.~Crill, S.~Prunet, and F.~Hansen,
``MASTER of the Cosmic Microwave Background Anisotropy Power Spectrum: A Fast Method for Statistical Analysis of Large and Complex Cosmic Microwave Background Data Sets,''
\emph{The Astrophysical Journal} \textbf{567} (2002), 2--17.

\bibitem{alonso2023namaster}
D.~Alonso, J.~Sanchez, A.~Slosar, and the LSST Dark Energy Science Collaboration,
``A unified pseudo-$C_\ell$ framework: \texttt{NaMaster},''
Astrophysics Source Code Library, ascl:2307.048 (2023).

\bibitem{challinor2000beam}
A.~Challinor, P.~Fosalba, D.~Mortlock, M.~Ashdown, B.~Wandelt, and K.~G\'orski,
``All-sky convolution for polarimetry experiments,''
\emph{Physical Review D} \textbf{62} (2000), 123002.

\bibitem{seljak1997}
U.~Seljak and M.~Zaldarriaga,
``Signature of Gravity Waves in the Polarization of the Microwave Background,''
\emph{Physical Review Letters} \textbf{78} (1997), 2054--2057.

\bibitem{heurtel2023dust}
D.~Heurtel-Depeiges, B.~Burkhart, R.~Ohana, and B.~R\'egaldo-Saint Blancard,
``Removing Dust from CMB Observations with Diffusion Models,''
in \emph{Machine Learning and the Physical Sciences Workshop, NeurIPS} (2023),
\emph{arXiv:2310.16285}.

\bibitem{mecke1994minkowski}
K.~R.~Mecke, T.~Buchert, and H.~Wagner,
``Robust morphological measures for large-scale structure of the Universe,''
\emph{Astronomy \& Astrophysics} \textbf{288} (1994), 697--704,
\emph{arXiv:astro-ph/9312028}.

\bibitem{schmalzing1997minkowski}
J.~Schmalzing and T.~Buchert,
``Beyond Genus Statistics: A Unifying Approach to the Morphology of Cosmic Structure,''
\emph{The Astrophysical Journal Letters} \textbf{482} (1997), L1--L4,
\emph{arXiv:astro-ph/9702130}.

\bibitem{reinecke2023ducc}
M.~Reinecke, S.~Belkner, and J.~Carron,
``Improved cosmic microwave background (de-)lensing using a generalized
non-uniform fast Fourier transform,''
\emph{Astronomy \& Astrophysics} \textbf{678} (2023), A165,
\href{https://doi.org/10.1051/0004-6361/202346717}{doi:10.1051/0004-6361/202346717},
\emph{arXiv:2304.10431}.
Software: \href{https://github.com/carronj/lenspyx}{\texttt{lenspyx}} (wraps
\href{https://gitlab.mpcdf.mpg.de/mtr/ducc}{\texttt{DUCC}}).

\bibitem{lewis2006lensing}
A.~Lewis and A.~Challinor,
``Weak gravitational lensing of the CMB,''
\emph{Physics Reports} \textbf{429} (2006), 1--65,
\href{https://doi.org/10.1016/j.physrep.2006.03.002}{doi:10.1016/j.physrep.2006.03.002},
\emph{arXiv:astro-ph/0601594}.

\bibitem{carron2022planckpr4lensing}
J.~Carron, M.~Mirmelstein, and A.~Lewis,
``CMB lensing from Planck PR4 maps,''
\emph{Journal of Cosmology and Astroparticle Physics} \textbf{09} (2022), 039,
\href{https://doi.org/10.1088/1475-7516/2022/09/039}{doi:10.1088/1475-7516/2022/09/039},
\emph{arXiv:2206.07773}.

\bibitem{millea2020muse}
M.~Millea, E.~Anderes, and B.~D.~Wandelt,
``Sampling-based inference of the primordial CMB and gravitational lensing,''
\emph{Physical Review D} \textbf{102} (2020), 123542,
\href{https://doi.org/10.1103/PhysRevD.102.123542}{doi:10.1103/PhysRevD.102.123542},
\emph{arXiv:2002.00965}.

\bibitem{ronneberger2015unet}
O.~Ronneberger, P.~Fischer, and T.~Brox,
``U-Net: Convolutional Networks for Biomedical Image Segmentation,''
in \emph{Medical Image Computing and Computer-Assisted Intervention -- MICCAI 2015},
Lecture Notes in Computer Science \textbf{9351} (2015), 234--241,
\href{https://doi.org/10.1007/978-3-319-24574-4_28}{doi:10.1007/978-3-319-24574-4\_28},
\emph{arXiv:1505.04597}.

\bibitem{karras2022elucidating}
T.~Karras, M.~Aittala, T.~Aila, and S.~Laine,
``Elucidating the Design Space of Diffusion-Based Generative Models,''
in \emph{Advances in Neural Information Processing Systems} \textbf{35} (2022), 26565--26577,
\emph{arXiv:2206.00364}.

\bibitem{salimans2022progressive}
T.~Salimans and J.~Ho,
``Progressive Distillation for Fast Sampling of Diffusion Models,''
in \emph{International Conference on Learning Representations} (2022),
\emph{arXiv:2202.00512}.

\bibitem{perez2018film}
E.~Perez, F.~Strub, H.~de Vries, V.~Dumoulin, and A.~Courville,
``FiLM: Visual Reasoning with a General Conditioning Layer,''
in \emph{Proceedings of the AAAI Conference on Artificial Intelligence} \textbf{32} (2018), 3942--3951,
\emph{arXiv:1709.07871}.

\bibitem{wu2018groupnorm}
Y.~Wu and K.~He,
``Group Normalization,''
in \emph{Proceedings of the European Conference on Computer Vision (ECCV)} (2018), 3--19,
\emph{arXiv:1803.08494}.

\bibitem{loshchilov2017sgdr}
I.~Loshchilov and F.~Hutter,
``SGDR: Stochastic Gradient Descent with Warm Restarts,''
in \emph{International Conference on Learning Representations} (2017),
\emph{arXiv:1608.03983}.

\bibitem{loshchilov2019adamw}
I.~Loshchilov and F.~Hutter,
``Decoupled Weight Decay Regularization,''
in \emph{International Conference on Learning Representations} (2019),
\emph{arXiv:1711.05101}.

\bibitem{liu2022rectified}
X.~Liu, C.~Gong, and Q.~Liu,
``Flow Straight and Fast: Learning to Generate and Transfer Data with Rectified Flow,''
\emph{arXiv:2209.03003} (2022).

\bibitem{albergo2023interpolants}
M.~S.~Albergo, N.~M.~Boffi, and E.~Vanden-Eijnden,
``Stochastic Interpolants: A Unifying Framework for Flows and Diffusions,''
\emph{arXiv:2303.08797} (2023).

\bibitem{vincent2011dsm}
P.~Vincent,
``A Connection Between Score Matching and Denoising Autoencoders,''
\emph{Neural Computation} \textbf{23}, 1661 (2011).

\bibitem{song2020scorematching}
Y.~Song and S.~Ermon,
``Generative Modeling by Estimating Gradients of the Data Distribution,''
in \emph{Advances in Neural Information Processing Systems} \textbf{32} (2019),
\emph{arXiv:1907.05600}.

\bibitem{chung2023dps}
H.~Chung, J.~Kim, M.~T.~McCann, M.~L.~Klasky, and J.~C.~Ye,
``Diffusion Posterior Sampling for General Noisy Inverse Problems,''
in \emph{International Conference on Learning Representations} (2023),
\emph{arXiv:2209.14687}.

\bibitem{saharia2022sr3}
C.~Saharia \emph{et al.},
``Image Super-Resolution via Iterative Refinement,''
\emph{IEEE Transactions on Pattern Analysis and Machine Intelligence} \textbf{45}, 4713 (2023),
\emph{arXiv:2104.07636}.

\end{thebibliography}

\end{document}
